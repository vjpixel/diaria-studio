Review automatizado (1 agente, effort low): a branch está desatualizada em relação a master e o merge reverteria silenciosamente #7567/#7568 — reject.

## Achados

### 1. Branch desatualizada reverte #7567 e #7568 inteiros (confiança alta, severidade P0)
`git merge-base 6f502ef5(=#7567) 22b85273(=head desta PR)` = `44205ff0`, mostrando que esta branch foi cortada de `44205ff0`, ANTES dos commits `c9fe7fe8` (#7568 — guard de promoção do modelo local) e `6f502ef5` (#7567 — label/owner de PR rejeitada) entrarem em master. O diff desta PR contra o head atual de master portanto DELETA:
- `scripts/lib/continuo-model-promotion-guard.ts` + `test/continuo-model-promotion-guard.test.ts` + a seção correspondente em `test/hermes-config-writer.test.ts` + o guard embutido em `scripts/write-hermes-config.ts` (bloqueio de promoção do modelo local durante alarme de fabricação, #7537/#7568) + a seção em `docs/goal-modelo-local-continuo.md`.
- `scripts/check-continuo-reject-label.ts` + `scripts/lib/continuo-reject-owner.ts` + `test/continuo-reject-owner.test.ts` + `test/continuo-pr-review-reject-label-npx-stderr-leak*.{sh,test.ts}` + o bloco de label/notificação em `hermes/scripts/continuo-pr-review.sh` + a seção em `hermes/skills/hermes-diaria-continuo/SKILL.md` (dono declarado pra PR rejeitada, #7567).

Nada disso é mencionado no título/corpo da PR (#7606/#7610, só sobre check-wrangler-auth). Se esta PR for mergeada como está, master perde os dois guards mecânicos já revisados e mergeados — regressão silenciosa de segurança (o guard de #7568 existe justamente para bloquear promoção perigosa do modelo local do contínuo). Ação necessária: rebase/merge da branch com o master atual antes de mergear, para que o diff final contenha só a mudança de check-wrangler-auth.ts/purge-leaderboard.ts.

### 2. Falta teste de integração combinando resolveBin+exec no mesmo caso (confiança média, severidade P3)
checkWranglerAuth agora resolve resolveBin dentro de try/catch e separadamente faz parsing de stdout por regex. Cada caminho tem teste isolado, mas não há teste cobrindo resolveBin OK + wrangler autenticado no mesmo teste — risco baixo, lógica simples e sequencial, registrando por completude.

### 3. O fix de check-wrangler-auth.ts/purge-leaderboard.ts em si, isolado do achado 1: correto (confiança alta, sem problema)
Lazy-resolve + detecção por stdout bem coberta por regressão (test/check-wrangler-auth-6900.test.ts), justificativa no docstring, edição cirúrgica. Sem o achado 1, esta parte seria approve.

<!-- continuo-review: run=1788839171-1456088-16350 at=2026-09-08T03:46:11Z verdict=reject -->
