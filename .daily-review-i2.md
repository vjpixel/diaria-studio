## Achado (review consolidado diário, range 11c201b7c..2656e4c3e) — interação entre 2 commits do range

Dois commits do range tocam a lista de fontes, e o segundo deixou o artefato gerado defasado:

1. `6b34164d2` (PR #8669) adicionou The Verge (AI) e Wired (AI) em `seed/sources.csv` **e** regenerou `context/sources.md` (header "Total: 52 fontes").
2. `785a52bb3` (PR #8644, #8631) removeu `Blog do Google Brasil (IA)` de `seed/sources.csv:43` — **sem** rodar `npm run sync-sources`.

Estado em `master` (`2656e4c3e`):

```
$ tail -n +2 seed/sources.csv | grep -c .   → 51
$ grep -c "^### " context/sources.md        → 52
$ grep -n "Blog do Google Brasil" context/sources.md
261:### Blog do Google Brasil (IA)
```

`context/sources.md` é o arquivo que os agentes de pesquisa leem (o CSV é só a seed). O resultado é que a fonte "desativada" continua anunciada ao pipeline, e o header afirma 52 fontes quando existem 51 — ou seja, a desativação do #8631 não tem efeito no lado que importa.

## Correção esperada
Rodar `npm run sync-sources` e commitar `context/sources.md` regenerado. Opcionalmente, um guard em CI (`seed/sources.csv` vs `context/sources.md` fora de sync → falha), que é o que evitaria essa classe de drift em vez de corrigi-la caso a caso.

## Prioridade
**P2** — afeta produção (fonte desativada segue sendo pesquisada, contagem errada no contexto) mas o dano é desperdício de pesquisa, não conteúdo corrompido; workaround é trivial (um comando).

<!-- origem: pr=8644 trilha=continuo commit=785a52bb3 -->
