37:0. **Resume**: antes de assumir que é retomada, resolver o path pelo mesmo verbo do passo 7 (#6328 — generaliza pro overnight o mecanismo do #6309, que já protegia o `/diaria-develop`; a checagem de colisão precisa valer também nesta porta de entrada, não só na 1ª escrita): rodar `npx tsx scripts/resolve-overnight-plan-path.ts --aammdd {AAMMDD}` (`--session-id` auto-injetado, standalone, nunca em `&&`/`;`/pipe/heredoc — item 18 de `context/overnight-dispatch-rules.md`). `mode: "resume"` → é de fato ESTA sessão continuando (mesmo `session_id`); ler o `plan.json` no `path` devolvido e **pular o briefing** — retomar a partir dos status do próprio `plan.json` usando a tabela de estados abaixo. `mode: "fresh"` ou `"derived-after-collision"` → **NÃO é resume desta sessão**, mesmo que algum `plan.json` já exista pra `{AAMMDD}` — é de outra sessão em voo, **inclusive de OUTRA MÁQUINA** compartilhando o mesmo `data/` via OneDrive (o cenário concreto que motivou o #6328: uma 2ª máquina rodando `/diaria-overnight` no mesmo dia encontrava o plano da 1ª e assumia "é retomada" pelo `existsSync` puro, pulando o briefing e corrompendo mutuamente o plano da rodada viva, desassistida, sem alarme) — seguir para o passo 1 como sessão NOVA, briefing incluído; o `path` (já com sufixo, se `derived-after-collision`) é o mesmo que o passo 7 vai confirmar de novo antes da 1ª escrita — chamada idempotente, sem side effect, não há custo em resolver duas vezes. Se `plan.json` contém `resume_state` estruturado (ver passo 7), usar seus campos para retomada determinística (phase + pending_issues + next_action) em vez de parsing de texto livre, confirmando ao editor o que falta antes de prosseguir. **Re-armar o guard mecânico da Regra 1 no Resume (#4450):** toda linha da tabela abaixo pula direto pra Fase 1, Fase 1.5 ou Fase 2 — nunca pro briefing (passos 2-8) — mas o passo 1 (Sync, logo abaixo) ainda roda `--start` nesse caminho, que grava `phase: "briefing"` no marker. Como o passo 8 (onde normalmente se arma `--phase autonomous`) nunca é alcançado num Resume, rodar `npx tsx scripts/overnight-session-marker.ts --phase autonomous` explicitamente aqui, logo após o passo 1, **antes** de agir sobre qualquer linha da tabela — sem isso, o guard fica desarmado pelo resto de uma rodada retomada, justamente o cenário do incidente de origem (#4450: gap real de sessão seguido de retomada):
50:1. **Sync**: `git checkout master && git pull` (o coordenador pode estar em outra branch — pull na branch errada faria os worktrees forkarem de HEAD não-master e os PRs da noite carregarem diff alheio). **Capturar `base_sha = git rev-parse HEAD` AGORA (pós-pull)** — é o ponto de partida do diff consolidado da Fase 1.5; capturado antes do pull, o diff incluiria commits alheios. **Capturar `started_at` = timestamp ISO 8601 REAL desta rodada AGORA** (`date -u +%Y-%m-%dT%H:%M:%SZ`, ou equivalente) — é o valor gravado em `started_at` no `plan.json` do passo 7 abaixo. **Nunca gravar a string `{AAMMDD}` nesse campo** (#3841 — bug de raiz do painel `/rodada` mostrando "INICIADA EM: 01/01, 00:00" e comparações quebradas tipo `created_at >= started_at` no passo 4: `{AAMMDD}` não é uma data parseável, `started_at` precisa ser). `{AAMMDD}` (com sufixo `b`/`c`/... quando há colisão do mesmo dia — desde o #6328, resolvido automaticamente por `scripts/resolve-overnight-plan-path.ts` nos passos 0 e 7, não mais convenção manual dependendo de alguém lembrar) continua nomeando só o DIRETÓRIO da sessão (`data/overnight/{AAMMDD}/plan.json`) — são dois campos com papéis diferentes, não o mesmo valor reaproveitado. Verificar `gh auth status`. **Marcar sessão overnight ativa nesta máquina (#3322)**: rodar `npx tsx scripts/overnight-session-marker.ts --start` — grava `data/overnight/.active-session-{hostname}.json`, o sinal que `.claude/hooks/pr-create-review.mjs` (`isOvernightRoundActive`) usa pra dar `low` effort no `/code-review` pós-PR independente de naming de branch (fallback do #3321). Idempotente — chamar de novo em resume não quebra nada. Removido na Fase 2 passo 7 (fim da rodada, #6758 — movido do passo 0 pra evitar recriação fantasma pelo `session-beacon.mjs` nas chamadas de ferramenta intermediárias da fase); se a rodada crashar antes de chegar lá, o marker expira sozinho depois de 24h (staleness embutida no hook — nunca fica "ativo" indefinidamente). **`--session-id` nunca é passado manualmente** — `.claude/hooks/inject-session-id.mjs` injeta automaticamente o `session_id` deste hook no comando antes de executar (#5156); isso é o que permite ao guard de `AskUserQuestion` e ao desconto de effort distinguirem esta rodada de uma sessão `/diaria-develop` rodando em paralelo na mesma máquina — ver seção "Paralelismo entre sessões (#5156)" no fim deste arquivo, nenhuma ação extra necessária aqui. **Registrar também no registro compartilhado de sessões**: `npx tsx scripts/lib/session-registry.ts register --kind overnight` (mesma injeção automática de `--session-id`) — é o registro que o item 3 (claim de issue), o item 4 (merge lock) e o item 9 (guard de cleanup de worktree) consultam; coexiste com o marker acima, não o substitui (ver seção final). **Esta skill chama `heartbeat` só para publicar `--active-worktrees` (#6299, abertura/fechamento de onda — ver passos 2 e 3 da Fase 1) — não para manter `lastHeartbeat` fresco por si; entre essas duas chamadas, quem mantém o registro fresco é o beacon** (#6327): `.claude/hooks/session-beacon.mjs` (`PreToolUse`) escreve no MESMO registro a cada chamada de ferramenta desta rodada, sem criar um paralelo. Não reduzir/desligar esse hook sem primeiro ler a docstring de `heartbeat` em `scripts/lib/session-registry.ts` — a claim desta rodada depende dele pra não ficar presa em `claimed_issues` de outra sessão por até 90min (`SOFT_STALE_MS`) além do necessário. **Log de startup do modelo/effort do coordenador (#2993):** logar no run-log um evento com o modelo/effort **CONFIGURADO** pelo frontmatter desta skill — não o auto-relatado pelo assistente, que não é 100% confiável (#2993) —, ex:
63:2. **PRs abertos remanescentes**: listar PRs abertos. Auto-resolver **só os de autoria desta skill** (branch prefix `overnight/`): CI verde e não-draft → merge (fluxo da Fase 1, passo 3). Qualquer outro PR aberto (bot, WIP do editor, draft de noite anterior) → vira pergunta no briefing; **nunca** auto-mergear PR que a skill não criou. **Um PR alheio já aberto NÃO é por si só razão pra deixar a issue correspondente de fora do lote elegível (#5484)** — antes de tratar "já tem PR" como bloqueio na classificação do passo 4, aplicar o checklist de 3 perguntas de `context/overnight-dispatch-rules.md` item 16 (autor conhecido? CI rodando/verde? atualizado nas últimas ~24-48h?); falhando qualquer uma, a issue segue elegível normalmente, avaliando implementação independente.
72:3. **Varredura**: `npx tsx scripts/fetch-open-issues-for-triage.ts` (#7018 — CLI sobre `scripts/lib/issue-triage-fetch.ts`, substitui o `gh issue list --json ...` escrito à mão) — devolve cada issue já com `execTrack` calculado via `classifyExecTrackFromListItem` (`scripts/lib/issue-exec-track.ts`), que **falha alto** (lança) se o `body` estiver ausente do payload, em vez de degradar `agendada`→`overnight` em silêncio (o próprio bug do #7018: rodada 260901b dispachou 4 issues fora da janela porque a varredura tinha saído sem `body`). **Nunca remontar a chamada `gh issue list` à mão nem reclassificar com `classifyExecTrack` sobre um corpo reconstruído** — o ponto do #7018 não é "lembrar de passar `body`", é tornar impossível classificar sem ele; só o CLI fino garante isso. `updatedAt` continua no retorno por outros consumidores (comparação com `decided_at`/`recorded_at` no passo 4 abaixo, exibição no plano); o passo 4 NÃO compara `decided_at`/`recorded_at` contra ele — #6961. `--bugs`/`--priority` continuam suportados como flags do script (filtragem idêntica à descrita abaixo). **Com `--bugs` (#3375)**: filtra o resultado para só issues com label `bug` — as demais nem entram na classificação do passo 4, não precisam de comentário/skip explícito (não são "puladas por bloqueio", estão simplesmente fora de escopo do modo). Registrar em `plan.json` (campo `bugs_only: true`) e no relatório final quantas issues foram excluídas por esse motivo, pra visibilidade. **Com `--priority` (#3499)**: filtra adicionalmente o resultado para só issues cujo label de prioridade ∈ conjunto passado (ex: `--priority P0,P1` mantém só `P0`/`P1`) — mesmo tratamento: as demais nem entram na classificação do passo 4, fora de escopo do modo, não "puladas por bloqueio". Registrar em `plan.json` (campo `priority_filter`, ex: `["P2"]`, ou `null` se a flag não foi passada) e no relatório final quantas issues foram excluídas por esse motivo. Compõe com `--bugs` — os dois filtros se aplicam em conjunto (interseção).
73:4. **Classificar cada issue** em:
248:4.5. **Tabela da fila completa** — imprimir ANTES do briefing, para o editor ver o escopo inteiro da noite e poder resgatar exclusões imediatamente:
267:5. **Briefing**: para as `precisa-resposta`, fazer **todas** as perguntas de uma vez via `AskUserQuestion`, agrupadas por issue (máximo 4 perguntas por chamada e 4 opções por pergunta; header = `#NNNN`). Cada pergunta oferece opções concretas + trade-off, **e sempre inclui a opção "decido depois (pular esta issue)"**. AskUserQuestion é bloqueante — não existe "detectar que o editor não respondeu"; o briefing pressupõe editor presente (limitação documentada: se ele sair no meio, a pergunta fica pendente até ele voltar). Issue respondida → promover a `elegivel` e **postar a resposta como comentário na issue** (`gh issue comment`) — é a fonte durável, visível de qualquer máquina/sessão; `plan.json` é cache. "Decido depois" → status `pulada` (sem comentário — a issue não foi prometida); se a issue for da família (b) do passo 4, a label `trade-off-real` **fica**, e é ela que traz a mesma pergunta de volta ao briefing da próxima rodada em vez de a issue reaparecer como se ninguém a tivesse olhado (#7493).
305:6. **Agrupamento em lotes (#2024, teto revisado em #2754, heurística mais agressiva em #3453)**: agrupar as `elegivel` em **lotes coesos**. Dois critérios de agrupamento, ambos válidos:
310:7. **Antes de escrever, resolver colisão de path (#6328 — porta pro overnight o mesmo mecanismo que o #6265/#6309 deu ao `/diaria-develop`)**: rodar `npx tsx scripts/resolve-overnight-plan-path.ts --aammdd {AAMMDD}` (`--session-id` auto-injetado, mesma convenção do item 18 de `context/overnight-dispatch-rules.md` — nunca em comando encadeado) — devolve `{ path, suffix, mode, collisions? }`. `mode: "fresh"`/`"resume"` → usar `path` normalmente (é o slot esperado, `data/overnight/{AAMMDD}/plan.json`). `mode: "derived-after-collision"` → **outra sessão overnight já tem um plano em voo neste mesmo dia** — nesta máquina OU noutra, compartilhando `data/` via OneDrive (`plan.json` existente com `session_id` diferente do desta sessão, ou sem `session_id` — plano legado tratado com a mesma cautela); usar o `path` com sufixo devolvido (`data/overnight/{AAMMDD}{suffix}/plan.json`, ex: `260826b`) em vez de sobrescrever o alheio, e mencionar a colisão no relatório final (Fase 2). **Gravar `session_id`** (do registro já feito no passo 1 — o `session_id` desta sessão) **e `machine_id`** (via `npx tsx scripts/lib/machine-id.ts`) **no nível raiz de `plan.json` a partir deste primeiro write** — mesmo padrão que o `/diaria-develop` já segue desde #6265/#3033. **Todo re-write subsequente do `plan.json` desta rodada usa o MESMO `path` resolvido aqui** — não re-resolver a cada write. **A checagem de colisão roda tanto aqui quanto no passo 0 (#6328)** — não é exclusiva da 1ª escrita: o passo 0 já resolveu o mesmo `path` antes de decidir se é resume; esta chamada aqui é a confirmação final antes do write em si (idempotente, mesmo resultado se nada mudou entre os dois passos).
376:8. Confirmar o plano com o editor (a tabela completa já foi impressa no passo 4.5; os lotes e a aprovação do agrupamento já foram incluídos no briefing do passo 5; aqui confirmar o estado final antes de entrar na Fase 1). **Se 0 elegíveis, dizer isso agora e encerrar aqui** — é a última chance do editor destravar algo respondendo mais perguntas; não rodar uma noite vazia. Com `--dry-run`, parar aqui (sem comentários postados). A aprovação do agrupamento já foi resolvida no passo 5 (via AskUserQuestion do briefing, ou via fallback `default_proposed`) — **nunca** repetir a pergunta aqui (Regra 1); usar o que está registrado em `batch_approval` do plan.json. **Armar o guard mecânico da Regra 1 imediatamente antes de entrar na Fase 1 (#4450):** (não aplicável a `--dry-run`, que já parou no passo anterior) rodar `npx tsx scripts/overnight-session-marker.ts --phase autonomous` — atualiza `data/overnight/.active-session-{hostname}.json` (gravado com `phase: "briefing"` no passo 1) para `phase: "autonomous"`, o gatilho que arma `.claude/hooks/block-askuserquestion-overnight-autonomous.mjs` (`PreToolUse` sobre `AskUserQuestion`) a partir deste ponto — qualquer `AskUserQuestion` na Fase 1 em diante (incluindo re-scans, mini-rodadas e Fase 1.5) é negado mecanicamente, mesmo que a chamada seja anômala e não passe por nenhum raciocínio que o reforço de prompt da Regra 1 poderia interceptar (ver Regras abaixo). Este é literalmente o último passo da Fase 0 — nada mais roda depois dele antes da Fase 1.
382:1. **Re-checar a fila**: `gh issue list --state open --limit 200 --json number,title,labels,state,createdAt` (sem `body` — corpo só de issues novas, via `gh issue view {N} --json body`; o coordenador precisa ficar enxuto). Issue fechada externamente → marcar `pulada` no plano e não trabalhar. **Issues novas criadas durante a rodada** (qualquer prioridade, qualquer autor — humano ou automação) com direção clara no corpo entram como `elegivel` com `source: "mid-round"`, com as seguintes exceções: (a) issue ambígua → **não** fazer pergunta ao editor; postar comentário explicando o que falta na direção + status `pulada` motivo `ambigua` (a questão vira pergunta do briefing da próxima rodada — Regra 1); (a.1) **com `--bugs` (#3375) ativo**: issue nova sem label `bug` → não entra (mesmo critério do passo 3 — fora de escopo, não é skip por bloqueio); (a.2) **com `--priority` (#3499) ativo**: issue nova cujo label de prioridade ∉ conjunto passado → não entra (mesmo critério do passo 3 — fora de escopo, não é skip por bloqueio); (b) issue com label `external-blocker` ou `not-this-week` → `pulada` com motivo correspondente, **comentar na issue confirmando que esta rodada a viu e a manteve fora** (dedup contra comentário overnight equivalente — mesma lógica do motivo `ambigua` acima, generalizada em #5777: toda `pulada` mid-round comenta, não só `ambigua`); (c) issue com label `session-finding` **e `created_at >= started_at` do `plan.json`** (= gerada pela própria rodada) → **não entra agora** (fluxo da Fase 1.5 cuida da re-entrada controlada com depth limit); issue com label `session-finding` mas `created_at < started_at` (= finding de rodada anterior, pendente) → trata como `mid-round` normal. **Re-varredura de convergência — SEM CAP (#5272; escopo: fila principal apenas, mini-rodadas da Fase 1.5 não incrementam):** cada vez que a fila elegível da fila principal é esgotada (sem itens pendentes), fazer um re-scan para capturar issues novas **e** rodar `npx tsx scripts/check-block-staleness.ts --plan data/overnight/{AAMMDD}/plan.json` (#6259 — passo 0.9 abaixo é o gate final antes do relatório, mas rodar aqui também evita declarar a fila esgotada com bloqueio já caduco: PR em voo mergeado/fechado, claim de sessão vizinha liberado, ou label `bloqueio-execucao` removida — nenhuma dessas transições cria issue nova, então "0 issues novas" sozinho **não** é convergência real). **Gate MECÂNICO obrigatório antes de declarar convergência (#6435).** "Fazer um re-scan" deixou de ser só prosa — rodar `npx tsx scripts/check-overnight-fila-convergence-gate.ts --plan data/overnight/{AAMMDD}/plan.json` **sempre** que a fila parecer esgotada, ANTES de seguir pra Fase 1.5. Achado ao vivo que motivou (rodada 260827b): o coordenador declarou "fila esgotada" e seguiu direto pra Fase 1.5 sem re-scan nenhum — só pegou a issue nova (#6431) porque o editor perguntou diretamente; sem essa pergunta, a issue nunca teria entrado no relatório. `exit 0` = convergência CONFIRMADA (`gh issue list` fresco comparado contra `goal.target_set`/`tiers`/`issues[]` não achou nada fora do conhecido) → seguro declarar a fila esgotada e avançar. `exit 1` = issue(s) nova(s) listada(s) → classificar cada uma (`elegivel` com `source: "mid-round"`, ou `pulada` com motivo do vocabulário fechado de `scripts/lib/overnight-plan-motivo.ts`) e rodar o gate de novo — **não avançar pra Fase 1.5 enquanto o gate não sair `exit 0`**, mesma disciplina dos gates 0.5-0.9 da Fase 2 (este é o gate irmão deles, só que no MEIO do loop em vez do fim — reusa a mesma lógica pura de `scripts/lib/state-changed-tracker.ts`, não duplica). `gh` indisponível → fail-soft (#738): o gate sai `exit 0` mas com aviso explícito "não verificado" — tratar como convergência NÃO confirmada (nunca como "nenhuma issue nova" de verdade), e repetir a tentativa no próximo ponto natural do loop. **Aceitar todas as novatas com direção clara** e trabalhá-las; ao esgotar de novo, re-scan de novo — **repetir enquanto cada re-scan devolver issue nova sem status terminal** — bloqueio caducado conta como issue nova para este fim (#6259). `rescans_done` continua sendo incrementado a cada re-scan, mas como **contador puro de observabilidade** (reportado na Fase 2): nenhuma decisão de parada lê esse valor, e um `plan.json` retomado com `rescans_done >= 2` **não** encerra a rodada por causa disso. **O status `pulada` motivo `rescan-limit` deixou de existir** — não gravar esse motivo em nenhuma circunstância. **O cap K=2 foi eliminado (#5272)** porque matava uma noite produtiva por aritmética: a 3ª fila esgotada encerrava a rodada mesmo com issue nova e trabalhável esperando — e fila que volta a encher é justamente o sinal de que há trabalho real, não de que a rodada deveria parar. **O que garante terminação no lugar dele** (dois mecanismos, nenhum deles um contador): (i) **anti-livelock**, logo abaixo — issue com status terminal nunca é re-escolhida, então um re-scan só devolve trabalho se houver issue genuinamente nova, e a rodada converge sozinha no primeiro re-scan sem novidade (caso comum); (ii) **guard de colisão com a manhã**, também abaixo — a pipeline editorial preempta a rodada. **NÃO existe teto de relógio (decisão do editor, 260814):** um teto de 09:00 BRT chegou a entrar junto com o #5272 e foi retirado no mesmo dia — a rodada não tem deadline de tempo (#2039) e não deve ganhar um pela porta dos fundos. **Consequência aceita, registrada aqui de propósito:** em dia sem edição — quando o guard (ii) nunca aciona — uma rodada que siga recebendo issues novas continua trabalhando sem nenhum limite de horário. É o comportamento pedido: o freio é a fila secar. **Não reintroduzir teto nem contador sem decisão explícita do editor** (esta é a segunda vez que um limite mecânico é retirado desta seção; a próxima sessão que achar que isso é um bug está lendo a decisão, não um esquecimento). **Alinhado com o `/diaria-develop`** (#4319 já havia eliminado o mesmo cap de lá): a diferença que resta é quem segura a sessão — lá o editor presente, aqui os dois mecanismos acima. **O cap de `findings_depth` (2) NÃO muda** — continua valendo, ver Fase 1.5. **Só issues sem status terminal no `plan.json` são candidatas** — uma issue que já virou `draft-ci-vermelho` ou `pulada` nunca é re-escolhida na mesma rodada (anti-livelock). **Guard de colisão com a manhã**: se uma edição diária estiver em curso (`npx tsx scripts/lib/find-current-edition.ts` retorna candidato ou `data/editions/` de hoje/amanhã ganhou arquivos novos), encerrar a Fase 1 após a unidade corrente e ir pra 1.5/relatório — a pipeline editorial tem precedência. Quando este guard acionar, gravar `preempted_by: "edicao_editorial"`, `preempted_at: now()` e `preempted_pending: [lista de issues sem status terminal]` em `plan.json`, e emitir no run-log:
388:2. **Antes do dispatch, checar claim de outra sessão (#5156 item 3):** `npx tsx scripts/lib/session-registry.ts is-claimed --issue {N}` (`--session-id` auto-injetado, exclui a própria sessão da checagem) pra CADA issue da unidade — se `claimed: true`, outra sessão ativa (overnight de outra máquina, ou `/diaria-develop` supervisionado) já está trabalhando essa issue AGORA; pular esta unidade (status `pulada`, motivo `claimed-por-outra-sessao`, sem comentário na issue — não é bloqueio, é corrida evitada) e seguir pra próxima da fila. Registro vazio (nenhuma outra sessão adotou o registro ainda) nunca bloqueia — `claimed` sempre `false`. Após confirmar que nenhuma issue da unidade está claimed, reivindicar todas de uma vez: `npx tsx scripts/lib/session-registry.ts claim-issue --kind overnight --issue {N}` (uma chamada por issue do lote) — **antes** do dispatch abaixo, não depois. **Chamar sempre como comando standalone, nunca dentro de `&&`/`;`/pipe/heredoc** (`context/overnight-dispatch-rules.md` item 18, #5751) — comando encadeado quebra a injeção automática de `--session-id`, e o erro resultante precisa ser tratado como falha real, nunca ignorado. **Consultar `conflicts` antes de abrir o worktree (#6168 Parte C, ponto 1):** `npx tsx scripts/lib/session-registry.ts conflicts --paths {arquivos da unidade, separados por vírgula} --branch {branch atual}` — standalone, mesma disciplina de injeção do `is-claimed` acima. `exit 1` = sobreposição real com peer VIVO. O `is-claimed` responde por ISSUE; este responde por ARQUIVO, que é o buraco 2 da #6168: duas sessões em issues DIFERENTES — sem colisão de claim nenhuma — editando o mesmo arquivo. É **consulta**, nunca aquisição: não cria arquivo nem reserva nada. Sobreposição com peer alcançável → avisar via `SendMessage` antes de dispatchar; peer inalcançável → registrar em `plan.json` e seguir (silêncio nunca trava a rodada). **Check de SAÍDA (ponto 3)**: na Fase 2, antes do `end`, conferir `git status --porcelain` limpo e `conflicts --branch {atual}` sem `branch-drift` — as duas evidências mais caras da #6168 são de saída (registro não encerrado segurando claims de trabalho já mergeado; e trabalho não commitado deixado em `master` num checkout compartilhado, com a rodada reportando "concluído").
426:3. **Agente fixer + revisar, esperar CI e mergear** (coordenador, nunca o subagente implementador):
547:4. **Manter #636 no que ele de fato garante (#6299).** O invariante real é **"nunca 2 PRs que COLIDEM abertos ao mesmo tempo"**, e desde o #6299 ele é satisfeito por CONSTRUÇÃO (análise de cluster + fusão de colidentes), não por serialização — mesma leitura que o `/diaria-develop` já aplicava desde o #4319. Dois PRs de unidades disjuntas abertos simultaneamente **não** violam o #636. O que continua estritamente serial é o **MERGE**: `master` recebe um squash por vez, sempre sob `merge-lock-acquire`, e o próximo merge só começa depois do `git pull` do anterior. Drafts de CI-vermelho ficam abertos para triage do editor — são exceção consciente, sinalizados no relatório.
548:5. **Atualizar `plan.json`** com o status terminal **e o número do PR** (campo `pr`) da unidade. Os eventos de run-log (merged/draft/fix_iteration) são emitidos em tempo real dentro do passo 3 acima; aqui apenas confirmar que `plan.json` foi persistido. Para unidades **puladas** (bloqueio externo, sem-resposta, ambígua), gravar `timeline.pulada = now()` em `plan.json` e emitir:
563:6. `git pull` em master após cada merge, antes da próxima issue. **Liberar o merge lock logo em seguida (#5156 item 4):** `npx tsx scripts/lib/session-registry.ts merge-lock-release --pr {N}` (`--session-id` auto-injetado; mesmo `--pr {N}` do acquire, #7169/#7223) — libera a janela pra próxima sessão que esteja esperando. A janela protegida pelo lock é exatamente `gh pr merge` (passo 3) até este `git pull` (passo 6) — nunca mais que isso, pra não segurar outra sessão além do necessário.
629:0. **Encerrar sessão overnight ativa — adiado pro fim da fase (#6758).** O encerramento (`overnight-session-marker.ts --end` + `session-registry.ts end --kind overnight`) NÃO roda aqui — ver step 7 (o último desta fase, depois do cleanup de worktrees). Rodar aqui era o bug original: os passos 0.5–6 abaixo ainda fazem ~7 chamadas de ferramenta, e cada uma dispara `session-beacon.mjs` (hook `PreToolUse`), que — não achando mais o registro `overnight-*` já removido por um `end` precoce — cria um registro NOVO `interactive-{tag}-{sessionId}.json` do zero, fazendo a sessão overnight "ressuscitar" como `interactive` viva em `list-active` mesmo após a rodada já ter terminado de verdade.
630:0.5. **Gate de re-triagem pendente + convergência (#5476, fundido em #5706) — bloqueia a compilação do relatório, não só um lembrete.** Rodar `npx tsx scripts/check-state-changed-pending.ts --plan data/overnight/{AAMMDD}/plan.json`. Desde #5706 o mesmo comando também faz o `gh issue list` completo e compara contra o `issues[]` já conhecido pelo plano (overnight não grava `goal.target_set`/`goal.tiers` — esses campos são exclusivos do `plan.json` do develop; `issues[]` é a ÚNICA fonte de "já conhecido" aqui), reportando issue aberta não-triangulada (mesmo mecanismo do develop, escopo adaptado ao schema do overnight) — reforça, não substitui, a re-varredura de convergência já descrita na Fase 1. Cada varredura bem-sucedida grava `goal.last_convergence_scan: {at, novas_encontradas}` no `plan.json` — como `goal` **não existe hoje** no schema do overnight (ver exemplo no passo 7 abaixo), a PRIMEIRA varredura da rodada CRIA esse objeto só com esse campo; não é um schema pré-existente sendo populado. `exit 0` ("ok — nenhuma pendência de re-triagem, nenhuma issue nova fora da varredura") → seguir pro passo 1. `exit 1` (lista de issues pendentes e/ou novas) → **voltar pra Fase 1** e reavaliar dispatch pra cada issue listada antes de continuar (ver regra correspondente na seção "Regras" acima) — o relatório só é compilado depois que a lista voltar a `[]`. Sem `gh`/rede, o comando degrada sozinho pra só a pendência explícita (fail-soft #738) — e o stdout de sucesso NESSE caso é explícito que a re-varredura NÃO rodou, nunca afirma "nenhuma issue nova fora da varredura" sem ter checado; `--skip-convergence` faz o mesmo degradê proativamente.
631:0.6. **Gate de cobertura de comentário (#5816) — bloqueia a compilação do relatório, mesma filosofia do passo 0.5.** Rodar `npx tsx scripts/check-overnight-comment-coverage.ts --plan data/overnight/{AAMMDD}/plan.json`. Confere que toda issue `pulada` (regra #5777) e toda issue `mergeada` cujo PR usou `REFS #N, NÃO CLOSES` em vez de `Closes` (regra #5010 — "PR mergeado com REFS" ≠ "issue fechada") de fato recebeu um comentário overnight-visível na issue (via `gh issue view N --json comments`). `exit 0` → seguir pro passo 1. `exit 1` (lista de issues sem comentário) → comentar em cada issue listada com o que foi feito/o que falta antes de continuar — só então o relatório é compilado. Sem `gh`/rede, o comando degrada sozinho pra warning + `exit 0` (fail-soft #738).
632:0.7. **Gate de estado terminal de PR (#5831) — bloqueia a compilação do relatório, mesma filosofia dos passos 0.5/0.6.** Rodar `npx tsx scripts/check-pr-terminal-state.ts --plan data/overnight/{AAMMDD}/plan.json` (achado ao vivo que motivou: `docs/orchestrator-stage-narrative-history.md#overnight-5831-pr-terminal-260820`). O comando cruza `gh pr list --state open` contra `plan.json`: (1) todo PR registrado (`issues[].pr`) que segue aberto no GitHub precisa de status `mergeada`/`draft-ci-vermelho`/`fechada-sem-merge` na issue correspondente — sem isso, é uma divergência confirmada (o coordenador esqueceu de fechar o loop); (2) PR aberto em branch `develop/`/`overnight/`/`fix/` que não aparece em nenhum `issues[].pr` vira candidato pra revisão humana (autoria não confirmada só pelo nome do branch — outra sessão pode estar rodando em paralelo, #5156), listado separado, nunca tratado com a mesma certeza do cenário 1. `exit 0` → seguir pro passo 1. `exit 1` → checar cada PR listado (`gh pr view N`), mergear/fechar/atualizar `plan.json` com o status correto antes de continuar — só então o relatório é compilado. Sem `gh`/rede, o comando degrada sozinho pra warning + `exit 0` (fail-soft #738).
633:0.8. **Gate de drift de label de decisão (#5892) — bloqueia a compilação do relatório, mesma filosofia dos passos 0.5/0.6/0.7.** Rodar `npx tsx scripts/check-decision-label-drift-gate.ts --plan data/overnight/{AAMMDD}/plan.json`. Detecta drift entre a prosa da rodada e as labels estruturais atuais (racional completo, incluindo as duas fontes de prosa do #5955 e o procedimento de resolução: `context/overnight-dispatch-rules.md` item 25). `exit 0` (nenhum drift) → seguir pro passo 1. `exit 1` (drift detectado) → aplicar as labels estruturais faltantes reportadas (procedimento no item 25 citado acima) antes de continuar — o relatório NÃO compila enquanto houver achados não resolvidos.
634:0.85. **Gate de vocabulário fechado do `motivo` de issues `pulada` (#6438) — bloqueia a compilação do relatório, mesma filosofia dos passos 0.5-0.8.** Rodar `npx tsx scripts/check-overnight-plan-motivo.ts --plan data/overnight/{AAMMDD}/plan.json`. Espelha `validate-develop-plan-motivo.ts` do `/diaria-develop` (#5708): confere que TODA issue `status: "pulada"` tem `motivo` dentro de `OVERNIGHT_PULADA_MOTIVOS` (`scripts/lib/overnight-plan-motivo.ts`) — motivo inventado ad-hoc ("gated no D0", "timing do editor") escapava de qualquer verificação antes deste gate, deixando a issue "Overnight sem sinal" pra sempre (achado ao vivo rodada 260827b, #6425/#6427/#6259/#5653). `exit 0` → seguir pro passo 1. `exit 1` (lista de issues com motivo fora do vocabulário) → reclassificar cada uma num motivo real do vocabulário (ou, se o motivo descreve algo genuinamente novo, abrir issue pra fechar o vocabulário, mesmo padrão que originou este gate) antes de continuar.
635:0.86. **Gate de formato do campo raiz `briefing` (#7497) — bloqueia a compilação do relatório, mesma filosofia dos passos 0.5-0.85.** Rodar `npx tsx scripts/check-overnight-plan-briefing.ts --plan data/overnight/{AAMMDD}/plan.json`. Confere que o campo `briefing` gravado no passo 5 da Fase 0 (ver schema lá) está estruturalmente correto — `reason` dentro de `OVERNIGHT_BRIEFING_REASONS` (`scripts/lib/overnight-plan-briefing.ts`) e consistente com `asked` (nunca `asked: true` com `reason` ≠ `"asked"`, nem o inverso). `exit 0` → seguir pro passo 1; se o script imprimir o aviso "ok (com aviso)" (`briefing.asked: false` + `reason: "desconhecido"` — nenhuma causa legítima conhecida pra ausência da pergunta), **copiar essa linha pro relatório da Fase 2** (passo 1 abaixo, na mesma área que hospeda os "Achados de auditoria" do passo 0.95 — não há bullet dedicado a "Briefing" na lista do passo 1, então este aviso entra ali) em vez de descartá-la — é o sinal que a #7497 pediu tornar visível: distinguir "rodou sem editor presente" de "havia editor e a pergunta não foi feita". `exit 1` (campo malformado) → backfill mecânico (reclassificar `reason`/corrigir `asked` conforme o que de fato ocorreu na rodada) antes de continuar, mesma disciplina do gate 0.85. Campo `briefing` ausente (plano anterior a este PR) → `exit 0` silencioso, plano legado.
636:0.9. **Gate de bloqueio caducado (#6259) — bloqueia a compilação do relatório, mesma filosofia dos passos 0.5-0.8.** Rodar `npx tsx scripts/check-block-staleness.ts --plan data/overnight/{AAMMDD}/plan.json`. Cobre a lacuna que o passo 0.5 e a re-varredura de convergência da Fase 1 passo 1 não fecham: os dois só perguntam "apareceu issue NOVA?" — uma issue JÁ CONHECIDA cujo motivo `pulada` era `pr-em-voo`/`claimed-por-outra-sessao`/`bloqueio-execucao` fica igualmente disponível quando o PR mergeia, a sessão dona do claim encerra, ou a label de bloqueio é removida, sem que nenhuma dessas transições crie issue nova (achado ao vivo que motivou: `docs/orchestrator-stage-narrative-history.md#overnight-6259-bloqueio-caducado-260826`). `exit 0` ("ok — nenhum bloqueio... caducado") → seguir pro passo 1. `exit 1` (lista de issues com bloqueio caducado) → **voltar pra Fase 1** e reavaliar dispatch pra cada uma antes de continuar — só então o relatório é compilado. Motivos fora desse conjunto fechado (`bloqueio-externo`, `requer-sessao-local`, `ambigua`, `trade-off-real`) nunca são reabertos por este gate — ver `scripts/lib/block-staleness.ts` pro racional. Sem `gh`/`session-registry`, o comando degrada sozinho pra fail-soft (#738) — nunca reporta caducidade sem ter conseguido confirmar. **O mesmo comando também checa (#6436) claims envelhecidas sem PR aberto** — varre `data/sessions/` inteiro (não só `plan.json`) por issues reivindicadas há mais de `CLAIM_STALE_AGE_MS` (~6h, `scripts/lib/claim-staleness.ts`) sem nenhum PR aberto citando o número; cobre o caso da sessão `continuo` (cron horário do Hermes) que re-reivindica sem nunca soltar, ficando `claimed-por-outra-sessao` pra sempre sem trabalho visível — `exit 1` lista as claims envelhecidas; adicionar cada issue listada como pendência de re-triagem (`npx tsx scripts/check-state-changed-pending.ts --add-pending {N} --plan {path}`) antes de continuar. A Triagem do Studio (`/triagem`) também passou a mostrar essas claims como "em andamento — {kind} {machine}" em vez de "Overnight sem sinal" (`attachClaims`, `scripts/studio-ui/studio-issues.ts`).
637:0.95. **Lint de prosa de dependência sem marcador (#7137 item 4) — AUDITORIA, nunca bloqueia (diferente dos passos 0.5-0.9 acima).** Rodar `npx tsx scripts/check-dependency-prose-lint.ts`. Reporta issues abertas que citam prosa de dependência ("pré-requisito", "depende do #N", "depois que #N", "só depois") sem carregar o marcador `<!-- depends-on: #N -->` (item 24, `reconcile-issue-dependencies.ts`) — mesmo erro que abriu a #7137 (17 issues-filhas do #7112 escritas com dependência só em prosa). Correção exige julgamento (é dependência genuína ou frase comum sem relação?), por isso **sempre `exit 0`**: os achados entram como uma linha no relatório da rodada (passo 1 abaixo, seção "Achados de auditoria" se houver algum) — nunca voltam a Fase 1, nunca travam a compilação. Sem `gh`/rede, degrada sozinho pra warning + `exit 0` (fail-soft #738). Ver `scripts/lib/dependency-prose-lint.ts` pro racional completo de por que isto é auditoria e não gate.
638:1. Compilar o digest da noite a partir de `plan.json` + run-log (filtrado por `agent: "overnight"` + AAMMDD da rodada), **agrupado por PR** (lotes listam suas issues juntas):
663:2. Salvar em `data/overnight/{AAMMDD}/report.md` (AAMMDD do `plan.json`, não recomputado).
664:3. **Registrar o relatório na superfície de Relatórios do Studio (#3714, decisão do editor 260720 — substitui o antigo draft de Gmail, não soma a ele):**
671:4. **Fail-soft**: falha do `register-report.ts` (rara — a gravação em disco em si) → NÃO travar. Mesma exceção consciente e local ao #738 já documentada aqui: avisar no terminal que o relatório ficou só local (`report.md`) e encerrar normalmente. Não citar esta exceção como precedente fora do relatório overnight.
672:5. Imprimir o resumo no terminal — é a primeira coisa que o editor vê ao voltar. Incluir a linha `Relatório: {URL do Studio}` (capturada no passo 3); se o registro falhou (passo 4), `Relatório: só local (data/overnight/{AAMMDD}/report.md) — registro no Studio falhou, ver warn acima`.
673:6. **Limpeza de worktrees mergeados (#4335, follow-up do #4326):** rodar `npx tsx scripts/cleanup-merged-worktrees.ts` — varre `.claude/worktrees/`, confirma via `gh pr list --head {branch} --state merged` quais branches já foram mergeadas e remove (`git worktree remove --force`) só essas. **FAIL-SOFT**: o script já é fail-soft internamente (nunca lança, nunca sai não-zero por falha de `gh`/permissão), mas mesmo assim tratar qualquer stderr/falha de invocação como warning — **nunca travar o encerramento da sessão** por causa desta limpeza. Roda por último, depois do relatório já estar salvo/registrado. **Guard de sessão compartilhada embutido no próprio script (#5156 item 9):** ele já consulta `session-registry.ts` (`listActiveSessions`) e PULA a varredura inteira, sozinho, se detectar qualquer outra sessão ativa nesta máquina — nada extra a fazer aqui além de rodar o comando normalmente; se o script imprimir esse aviso (por exemplo, uma sessão `/diaria-develop` supervisionada ainda ativa em paralelo), não insistir com `--confirm-shared` sem ter certeza de que os worktrees dela não colidem — a omissão é o comportamento seguro por padrão.
674:7. **Encerrar sessão overnight ativa (#3322, movido pro FIM da fase em #6758)** — último passo da Fase 2, depois de esgotadas todas as chamadas de ferramenta que a fase precisa fazer (passos 0.5-6 acima): rodar `npx tsx scripts/overnight-session-marker.ts --end` — remove `data/overnight/.active-session-{hostname}.json` gravado na Fase 0 passo 1, pra `pr-create-review.mjs` parar de dar `low` effort em PRs abertos nesta máquina depois que a rodada de fato terminou (manuais, `/diaria-develop`, etc). Idempotente — no-op se o marker já não existir. Rodar mesmo nos caminhos de encerramento antecipado (guard de colisão editorial, erro irrecuperável) — sempre que esta fase é alcançada, a rodada está terminando. **Junto, encerrar também o registro no `session-registry.ts` (#5156)**: `npx tsx scripts/lib/session-registry.ts end --kind overnight` — sem isso, o registro fica visível (até expirar por staleness, 24h) pro guard de cleanup de worktree (item 9 da seção "Paralelismo entre sessões") e pro claim de issue (item 3) de outra sessão, mesmo com esta rodada já encerrada. **Por que aqui e não no passo 0:** rodar cedo demais faz cada chamada de ferramenta seguinte (`check-*.ts`, `Write` do report, `register-report.ts`, `cleanup-merged-worktrees.ts`) disparar `session-beacon.mjs` (hook `PreToolUse`) sem achar mais o registro `overnight-*` — o beacon cria, por desenho (#6326), um registro NOVO `interactive-{tag}-{sessionId}.json`, fazendo a sessão overnight "ressuscitar" como `interactive` viva em `list-active` mesmo já tendo terminado (achado ao vivo, rodada 260829).
691:1. **`AskUserQuestion` do develop negado por um overnight ativo na mesma máquina** — MECANIZADO. `.claude/hooks/block-askuserquestion-overnight-autonomous.mjs` só bloqueia quando a chamada pertence à MESMA sessão que gravou o marker (`session_id` comparado, injetado automaticamente por `.claude/hooks/inject-session-id.mjs` — nenhuma ação da skill). Marker sem `session_id` (formato antigo) preserva o comportamento pré-#5156. Nada a fazer nos passos acima além de continuar chamando `overnight-session-marker.ts --start`/`--phase` normalmente.
692:2. **Effort do review por PR cai pra `low` incorretamente com overnight ativo** — MECANIZADO, mesmo mecanismo do item 1 (`.claude/hooks/pr-create-review.mjs`, `isOvernightRoundActive` session-aware).
693:3. **Claim de issue entre sessões** — MECANIZADO via `session-registry.ts` (`is-claimed`/`claim-issue`), ver Fase 1 passo 2 acima.
694:4. **Serialização de merge cross-sessão** — MECANIZADO via `session-registry.ts merge-lock-acquire`/`merge-lock-release`, ver Fase 1 passos 3/6 acima. **Nota (#6182): entre máquinas via OneDrive, o lock é advisory — `O_CREAT|O_EXCL` vê inodes diferentes, não garante exclusão real.**
695:5. **Checkout principal compartilhado** — SEM fix de código razoável além do merge lock do item 4 (que já mitiga a janela crítica de merge+pull). `git checkout master && git pull` na Fase 0 passo 1 e após cada merge continuam sem lock próprio — se outra sessão estiver escrevendo no checkout no MESMO instante, é uma corrida rara e de baixo blast-radius (o pior caso é um `git pull` pegando um estado intermediário, resolvido no próximo pull). Documentado, não mecanizado.
696:6. **Teto de concorrência por sessão** — MECANIZADO DESTE LADO desde o #6299; **assimétrico, e é isso que importa saber:** o read-path sempre foi real (`list-active` soma `active_worktrees` das sessões ativas nesta máquina), e o write-path (`heartbeat --active-worktrees N`) agora tem call site REAL no overnight — dois, obrigatórios: ao dispatchar a onda (Fase 1 passo 2) e após o cleanup de cada worktree (Fase 1 passo 3). Antes disso nenhuma skill chamava o comando e `active_worktrees` ficava sempre ausente, tornando a soma inútil (#5161 fleet review item 7). **O `/diaria-develop` ainda NÃO publica o dele** — logo, quando as duas rodam juntas, o overnight enxerga os worktrees do develop como zero e pode abrir seus 3 por cima dos 6 dele. A assimetria é conservadora na direção certa (quem publica é quem tem o teto menor), mas não é simetria: fechar o lado develop é trabalho pendente, não pressuposto desta seção. Teto de 6 do develop: ver seção equivalente em `diaria-develop/SKILL.md`.
697:7. **Herança de plano em voo** — não se aplica ao overnight (é quem GERA o `plan.json` que o develop herda, não o contrário). Ver `diaria-develop/SKILL.md` pro tratamento do lado que consome.
698:8. **Fase 1.5 revisa diff de sessão alheia** — já é o comportamento esperado, agora formalizado: com paralelismo, o diff acumulado da Fase 1.5 PODE incluir merges de uma sessão `/diaria-develop` concorrente. **Findings sobre esse código viram issue, nunca hotfix** (mesma regra de sempre) — com paralelismo isso é o CASO COMUM, não uma exceção rara.
699:9. **Cleanup de worktree global** — MECANIZADO, embutido no próprio `cleanup-merged-worktrees.ts` (ver Fase 2 passo 6 acima).
