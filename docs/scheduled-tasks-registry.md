# Registro operacional de tasks agendadas

Extraído do `CLAUDE.md` (#5344 Parte B2, 15/08/2026) — `CLAUDE.md` é o único
arquivo de prompt carregado incondicionalmente em toda sessão **e em todo
dispatch de subagente** (#4814); este bloco de registro operacional (task X
armada/agendada, horários, histórico de cutover Windows) não é relevante pra
um subagente implementando uma issue qualquer, então paga o custo de token
sem retorno em praticamente todo dispatch. `context/` não tem esse problema —
é lido sob demanda arquivo por arquivo, não incondicionalmente — daí este
arquivo viver em `docs/` e não em `context/`.

Conteúdo preservado integralmente (só realocação, não resumo). Para
armar/verificar qualquer task, seguir o doc de setup específico citado em
cada parágrafo.

---

**Watchdog de stall overnight (#2688):** task `Diaria-Overnight-Watchdog`, a cada 10 min, janela 18:00→09:00 BRT — fora do registry declarativo por schema (a janela cruza a meia-noite, #4857). Armado e validado ao vivo em `helios` (260810). **Nenhuma tarefa `Diaria-*` roda no Windows** (política confirmada 260811, decisão do editor) — o par Windows deste watchdog foi desligado de propósito por redundância; auditoria de fechamento em #5074/#5091. Arme e detalhes: `docs/overnight-watchdog-setup.md`.

**Sync incremental diário do store Clarice (#2932):** task `Diaria-Clarice-Sync`, diária às 08:30. Confirmado ativo em `helios` (260812). Arme e detalhes (dois passos store+KV, catch-up de opens #4688): `docs/clarice-sync-daily-setup.md`.

**Alarme de guardrail furado do ramp Clarice (#4064, agendamento #4131 finding 1):** task `Diaria-Clarice-Guardrail-Alarm`, a cada 4h. Confirmado ativo em `helios` (260812). Arme e detalhes: `docs/clarice-guardrail-alarm-setup.md`.

**Sync automático do spamRate do Google Postmaster Tools (#4154):** task `Diaria-Postmaster-Spam-Sync`, diária às 12:30 (mudou de "a cada 12h" — decisão do editor 260810, o dado fonte só muda em granularidade diária). Confirmado ativo em `helios` (260812). Arme e detalhes: `docs/postmaster-spam-sync-setup.md`.

**Alarme do sinal de spam do Postmaster (#5399, #5446):** task `Diaria-Postmaster-Spam-Alarm`, diária às 12:45 (15min depois do `Diaria-Postmaster-Spam-Sync` acima, pra garantir que o sync já gravou no KV antes do alarme ler). Roda `scripts/clarice-postmaster-alarm.ts`, que checa 2 sinais INDEPENDENTES na mesma execução: (1) o agregado de domínio degradou pra `indeterminate` por N checagens consecutivas (`CONSECUTIVE_STALE_THRESHOLD`, #5399); (2) `worstCampaignSpamRatePct`/`campaignSpam` ficaram ausentes do KV por `CAMPAIGN_SPAM_MISSING_THRESHOLD_DAYS` (14) dias corridos consecutivos mesmo com o agregado de domínio saudável (#5446 item 2 — sinaliza o breaker rodando no fallback de média de domínio sem sinal por-campanha). Cada achado vira issue própria via o mecanismo do #5339 (dedup/reconciliação por fingerprint). Registrada no #5446 e **ARMADA em 17/08/2026** na `helios` (próximo disparo 18/08 12:45 BRT) (`scripts/clarice-postmaster-alarm.ts` existia desde o #5399 mas nunca tinha sido registrada no `SCHEDULED_TASKS`, então nunca rodava sozinha).

**Envio automático aos cadastros novos da Clarice (#4941, automatiza a skill `/diaria-clarice-novos` do #4347):** task `Diaria-Clarice-Novos`, diária às 09:00 (mudou de 11:00 — decisão do editor 260816, #5447, a partir da análise do #5445: latência média cai de 9,2h pra 6,7h; sucede o #5140/260812, que tinha mudado de 17:00 pra 11:00). **Gated por kill switch dedicado, default PAUSADO** — `data/clarice-novos-enabled.json` ausente/`{enabled:false}` faz a task sair limpo sem tocar Stripe/MV/Brevo; armar nunca liga o disparo sozinho, requer `npx tsx scripts/lib/clarice-novos-enabled.ts --set enabled` explícito. **Armar só numa máquina** (`data/` é junction OneDrive — duas armadas podem disparar 2×). Confirmado ativo em `helios` (260812); re-arme pro novo horário PENDENTE (#5447, ver `docs/clarice-novos-daily-setup.md`). Arme e detalhes: `docs/clarice-novos-daily-setup.md`.

**2a captura diária, `Diaria-Clarice-Novos-Tarde` às 18:00 (#5185, 260814; mudou de 15:00 no #5447, 260816):** mesmo script/kill switch/guards de `Diaria-Clarice-Novos` — 2ª rodada diária fecha boa parte da janela em que um cadastro da tarde/noite só seria pego pelo `novos` do dia seguinte. Decisão do editor: manter os dois orquestradores separados (opção "cadastro direto no pool `ramp-warm`" descartada no briefing 260814). **Risco residual do #5185 fechado pelo #5410** (16/08/2026: `isNovos`/`isRampWarm` passaram a PARTICIONAR a fila de 1º envio, não mais um subconjunto do outro) — é esse fechamento estrutural que libera as 18:00 sem reabrir risco de duplicata. **ARMADA** na `helios` (confirmado ao vivo em 17/08/2026: `systemctl --user is-enabled` = `enabled`, próximo disparo 18/08 18:00 BRT). Arme e detalhes: `docs/clarice-novos-daily-setup.md`.

**Alarme de abort recorrente do `novos` (#5405) — aposentado pelo #5660:** o guard D4 foi removido do caminho `clarice-novos`, então `semaphore-red` deixou de ser produzido. `Diaria-Clarice-Novos-Abort-Alarm` foi removida do registro; o script permanece como no-op dormente para não alarmar sobre status histórico.

**Sync do KV de assinantes do worker `cursos` (#4320, #4052):** task `Diaria-Cursos-Kv-Sync` (diária, 09:15). Confirmada ativa em `helios` (260812). Arme e detalhes: `docs/cursos-worker-alarm-setup.md`.

**Alarme de erro do worker `cursos` — CORTADO (#4320 criou, redesign #4382, #6798 removeu, 01/09/2026).** Existiu como task `Diaria-Cursos-Error-Alarm`, a cada 2h — lia 4 contadores cumulativos do KV `CURSOS_SUBSCRIBERS` e alarmava por erro fatal ou taxa alta de `?email=` não confirmado. Removido por decisão do editor na auditoria do #6798: 279 execuções, 0 disparos, 24 dias — maior volume do projeto com o menor sinal medido, e nenhum achado nunca chegou a virar issue. `Diaria-Cursos-Kv-Sync` (item acima) **NÃO faz parte deste corte** — é sync, não alarme, e continua ativo. **Ação manual pendente do editor:** desarmar a task no `helios` — `systemctl --user disable --now diaria-cursos-error-alarm.timer` (remover `.service`/`.timer` de `~/.config/systemd/user/` é opcional).

**Alarme diário de diff pendente do sync de apoio (#4485 item 2):** task `Diaria-Apoios-Diff-Alarm`, diária às 09:45. Confirmado ativo em `helios` (260812). Arme e detalhes: `docs/apoios-diff-alarm-setup.md`.

**Evaluate diário do canal `brevo_diaria` (#4534, fecha o checkbox aberto da #4476):** task `Diaria-Brevo-Diaria-Evaluate`, diária às 05:30 BRT (antes do envio canônico das 06:00). Confirmado ativo em `helios` (260812). Arme e detalhes: `docs/evaluate-brevo-diaria-setup.md`.

**Circuit breaker de campanha do canal `brevo_diaria` (#4476 item 9):** task `Diaria-Brevo-Diaria-Guardrail`, a cada 4h. Confirmado ativo em `helios` (260812). Arme e detalhes: `docs/brevo-diaria-guardrail-setup.md`.

**Alarme de drift entre o código publicado e o master de cada Worker (#4723):** task `Diaria-Worker-Drift-Check`, a cada 6h. Confirmado ativo em `helios` (260812). **Wired ao `alarm-issues` (#5339, 15/08/2026, encerrado)** — cada worker defasado ganha issue própria (criada/reusada/comentada/fechada automaticamente, mesmo padrão de `home-meta-check.ts`/#5112), citada no e-mail; `worker-drift-check.ts` foi o 1º dos 11 alarmes sem wiring a receber (priorizado por ter gerado o achado #5337 manualmente). **Lote 1/3** wireou `hub-drift-check.ts`/`robots-txt-drift-check.ts` (1 issue por hub/host) e `apoios-diff-alarm.ts` (1 issue por RODADA de diff, não por assinante — repo PÚBLICO, e-mails mascarados na issue via `maskEmailForIssue`, o e-mail privado ao editor continua completo). **Lote 2/3** wireou a família Clarice inteira: `clarice-envio-alarm.ts`/`clarice-envio-guard-alarm.ts` por `aammdd` (achado = dia, fecha sozinha no dia seguinte limpo); `clarice-guardrail-alarm.ts` por `campaign-{id}` (achado = campanha com breach, avaliada 1x pra sempre); `clarice-opens-catchup-alarm.ts` com fingerprint FIXO (`streak-failing`, mesma issue reusada enquanto o streak de falhas persistir). Nenhum dos 4 tem PII no achado (só `aammdd`/verdict/reportId, nome/id de campanha, ou contagem de streak). **Lote 3/3 (final)** wireou os 3 últimos: `cursos-error-alarm.ts` (1 issue por padrão fatal + 1 pra taxa de "?email= não confirmado", achados só com contadores agregados do KV, sem PII), `geo-citation-staleness-alarm.ts` (2 checks independentes — staleness por painel e provider ausente da última rodada #5316 —, sem PII), `linkedin-weekly-staleness-alarm.ts` (1 issue por ciclo `{YY}w{WW}` sem artefato produzido, sem PII). **Os 11 alarmes enumerados originalmente na issue #5339 estão todos wired.** Os healthchecks citados à parte na issue (`clarice-healthcheck.ts`, `poll-worker-healthcheck.ts`, `hub-staleness-check.ts`, `seo-index-check.ts`) seguem fora de escopo, "a decidir caso a caso" por texto explícito da própria issue — não fazem parte do fechamento do #5339. Arme e detalhes: `docs/worker-drift-check-setup.md`.

**Alarme de falha sustentada do catch-up de opens da Clarice (#4740, item 4 do #4722):** task `Diaria-Clarice-Opens-Catchup-Alarm`, diária às 09:00 (depois do sync das 08:30). Confirmado ativo em `helios` (260812). Arme e detalhes: `docs/clarice-opens-catchup-alarm-setup.md`.

**Alarme de rodada falha do `Diaria-Clarice-Envio` (#5058):** task `Diaria-Clarice-Envio-Alarm`, diária às 20:30 BRT. Lê o relatório mais recente do dia e alarma se a onda de amanhã não foi agendada por motivo que não é pausa esperada, ou se nenhum relatório existir. Armado e confirmado ativo em `helios` (260812, #5091) — não confundir com `diaria-clarice-envio-guard.timer` (guard matinal 05:00, tarefa DIFERENTE). Arme e detalhes: `docs/clarice-envio-alarm-setup.md`.

**Retry+fallback do `Diaria-Clarice-Envio-Guard` + alarme próprio (#5220, 13/08/2026):** o guard das 05:00 tinha um gap — falha transitória (503/rate-limit) abortava a rodada ANTES de reavaliar o freio, e a onda já agendada disparava às 06:00 sem checagem. Fix: retry com backoff + fallback fail-closed lendo o último freio conhecido (nunca reconsultando a Brevo). Task nova `Diaria-Clarice-Envio-Guard-Alarm`, diária às 06:15 BRT, isolada do alarme do run das 19:00 (Gap 2 da mesma issue). **ARMADO** na `helios` (confirmado ao vivo em 17/08/2026: `enabled`, último disparo 17/08 06:15 BRT); o texto anterior dizia que o arme estava pendente. Registro histórico: o gerador de unit resolveria path do worktree (apagado no cleanup), por isso o arme foi depois do merge. Arme e detalhes: `docs/clarice-envio-daily-setup.md` (seção "Retry e fallback do guard") e `docs/clarice-envio-guard-alarm-setup.md`.

**Escalada deliberada vs. falha real no exit code do guard (#6221, 26/08/2026):** achado ao vivo — escalada deliberada (pré-requisito falhou, freio HOLD com override do editor vigente sobre ele, #6134) saía `exit 1`, indistinguível de erro real pro `Diaria-Systemd-Failed-Units-Alarm` (#5942); já produziu leitura invertida por um coordenador de overnight (#6215). Fix: a escalada agora sai `exit 3`, e a task declara `successExitCodes: [3]` (`SuccessExitStatus=3` no `.service` gerado) — a unit termina limpa nesse caminho específico, e a escalada segue visível só pelo `Diaria-Clarice-Envio-Guard-Alarm` (que lê o `reportId`, não o exit code) — que TAMBÉM ganhou vocabulário próprio (verdict `alarm-escalated`, título "escalou" em vez de "falhou", corpo declarando que nenhuma ação automática foi tomada e que isso é o comportamento desejado); antes desse ajuste no alarme, o mesmo evento ainda gerava um e-mail/issue dizendo "falhou" apesar do exit code já corrigido — foi exatamente esse vocabulário que enganou o coordenador no #6215. Todo outro erro duro (lock detido, exceção inesperada, cancelamento incompleto) continua `exit 1`/`2` e pondo a unit em `failed`, sem mudança. Mesma issue também encolheu o orçamento de retry dos pré-requisitos (`GUARD_TRANSIENT_RETRY_BUDGET` em `scripts/clarice-envio-guard.ts`, 3 tentativas/10min de teto → 2 tentativas/2min) — pior caso de espera caiu de ~20min para ~4min dentro da janela útil de 60min (05:00 guard → 06:00 disparo).

**Guard por hash de escrita do `dash:lastgood:campaigns` (#5216, 13/08/2026):** o write-through do dashboard `clarice-dashboard` (`workers/brevo-dashboard`) gravava esse KV INCONDICIONALMENTE a cada `GET /` bem-sucedido fora de `?fresh=1` — sem TTL de escrita, sem hash, sem intervalo mínimo. Free tier da Cloudflare tem teto de ~1.000 escritas de KV/dia POR CONTA, compartilhado com o Worker `poll` (já quase estourou uma vez, #2282). Fix: reintroduz o gate por hash djb2 que existia pré-#2739 pro HTML lastgood (removido quando o modelo mudou pra campaigns-lastgood) — `dash:lastgood:campaigns:hash` guarda o hash do conteúdo ESTÁVEL (`campaigns`+`scheduled`+`campaignsLimit`, nunca `generatedAt`) e só reescreve o payload quando o hash muda. `LASTGOOD_CAMPAIGNS_HASH_KEY`/`djb2Hash` em `workers/brevo-dashboard/src/brevo-api.ts`.

**Crawl periódico de coortes de engajamento via v2 (#4451, kill switch acima não se aplica aqui):** task `Diaria-Clarice-Cohorts-Crawl`, diária às 21:00 BRT — roda `clarice-engagement-cohorts-v2.ts --push`, atualizando o KV `cohorts:engagement` (snapshot "Coortes de engajamento" do dashboard clarice-dashboard, guard anti-clobber #5015) além do artefato local `--out`. Registro do zero, sem contraparte na task Windows legada `DiariaCohortsCrawl` (v1). Confirmado ativo em `helios` (260812). Arme e detalhes: `docs/cohorts-schedule.md`.

**Precompute horário do dashboard clarice-dashboard (#5217, 13/08/2026):** `dash:lastgood:campaigns` ficou órfão desde que o Cron Trigger interno do Worker foi removido (#3553/#3639). Task nova `Diaria-Clarice-Dashboard-Precompute`, HORÁRIA — bate `GET /` autenticado via Bearer, reusando o MESMO `AUTH_TOKEN` do login humano por cookie (trade-off aceito: rotacionar o token desloga humanos E quebra este job ao mesmo tempo). **ARMADO** na `helios` (confirmado ao vivo em 17/08/2026: `enabled`, roda de hora em hora, último disparo hoje); o texto anterior dizia que o arme estava pendente pelo mesmo motivo do `Diaria-Clarice-Envio-Guard-Alarm` acima — feito depois do merge. Arme e detalhes: `docs/clarice-dashboard-precompute-setup.md`.

**Monitor semanal de citação por assistente de IA (#4558 Parte C):** task `Diaria-Geo-Citation-Monitor`, domingos 07:00 (mudou de segundas 10:30 — decisão do editor 260810, semanais consolidadas na manhã de domingo). Confirmado ativo em `helios` (260812). Arme e detalhes (baseline, exit codes `--strict` #4754): `docs/geo-citation-monitor-setup.md`.

**Alarme de staleness do monitor de citação GEO (#4755):** task `Diaria-Geo-Citation-Staleness-Alarm`, domingos 10:30 (mudou de segundas 14:00 — decisão do editor 260810; segue depois da task do monitor acima). Confirmado ativo em `helios` (260812). Arme e detalhes: `docs/geo-citation-staleness-alarm-setup.md`.

**Alarme de staleness da newsletter semanal do LinkedIn (#5111):** task `Diaria-LinkedIn-Weekly-Staleness-Alarm`, domingos 22:00 BRT — checa se `data/weekly/{cycle}/ln-{cycle}.json` foi produzido pra semana de conteúdo que acabou de terminar. Achado ao vivo 260812: o ciclo `26w32` não rodou e nada avisou. **Escopo reduzido de propósito** — não agenda a produção nem trata "semana perdida" (follow-up). **ARMADO** em `helios` (confirmado ao vivo em 17/08/2026: `enabled`, último disparo dom 16/08 22:00 BRT). Detalhes: `docs/linkedin-weekly-staleness-alarm-setup.md`.

**Smoke-test de drift entre HUB_META e o Worker `arquivo` publicado (#4750, follow-up do fleet review da PR #4749):** task `Diaria-Hub-Drift-Check`, diária às 10:00 (mudou de "a cada 6h" — decisão do editor 260812, #5113: o conserto é ação manual do editor de manhã). Confirmado ativo em `helios` (260812). **Auditoria de retorno (#6798 item 5, 02/09/2026): 38 execuções, 0 disparos — amostra curta demais pra concluir; decisão do editor foi MANTER sem mudança.** Não reabrir a mesma pergunta sem novo dado. Arme e detalhes: `docs/hub-drift-check-setup.md`.

**Regeneração automática das páginas de entidade (`especial.diar.ia.br/entidades/`) (#5125, condição inegociável do editor 14/08/2026 pra publicar a Apple):** task `Diaria-Entity-Pages-Regen`, diária às 09:40 — regen mecânica do HTML (no-op quando nada mudou), `--skip-alarm` (#7147, 02/09/2026: o alarme de menção não capturada saiu desta task pra rodar separado). **ARMADA em 17/08/2026** na checkout compartilhada (`helios`, Linux/systemd) — `diaria-entity-pages-regen.timer` ativo; validada ao vivo em 17/08 (`--dry-run`: `nada divergiu (no-op)`, `0 entrada(s) stale`). Detalhes: `docs/entity-pages-regen-setup.md`.

**Alarme de staleness das páginas de entidade (#7147, 02/09/2026, split de `Diaria-Entity-Pages-Regen` acima):** task `Diaria-Entity-Pages-Staleness-Alarm`, domingos às 09:43 — mesmo mecanismo de aging do `Diaria-Hub-Staleness-Check`/#5123 ("só alarmar, nunca auto-escrever prosa"), agora `--alarm-only` (regen já rodou na task irmã diária) + `--threshold-days 1` (baixado de 3 — a cadência semanal já é o amortecedor; sem baixar o limiar, "3+ dias" na prática viraria "8-13 dias"). Motivo da troca: o achado de staleness muda na cadência de PUBLICAÇÃO (1 edição/dia), não na de checagem — a task diária original virava e-mail quase todo dia pro mesmo tipo de pendência. **Ainda NÃO armada nesta unidade** (worktree isolado) — arme real via `scripts/setup-systemd-timers.ts` da checkout compartilhada é ação posterior do editor. Sem doc de setup próprio — ver `docs/entity-pages-regen-setup.md`.

**Detecção de edições fora do dataset commitado dos hubs temáticos (#5123, cadência revisada #7147 02/09/2026):** task `Diaria-Hub-Staleness-Check`, domingos às 09:33 (mudou de diária — mesmo racional do split acima: o achado muda na cadência de publicação, e a versão diária gerava issue quase todo dia por hub, evidência: #7101/#7102/#7103 numa única manhã). `--threshold-days 1` (baixado de 3, mesmo motivo do `Diaria-Entity-Pages-Staleness-Alarm`). Detalhes: docstring de `scripts/hub-staleness-check.ts`.

**Smoke-test do `robots.txt` SERVIDO pelos Workers de curadoria (#4910 item 3):** task `Diaria-Robots-Txt-Drift-Check`, diária às 10:15 — audita o arquivo que de fato chega no crawler (não só o que `renderCuradoriaRobotsTxt` produz), alarmando se o bloco gerenciado da Cloudflare reaparecer bloqueando algum bot fora do esperado (ver princípio "Crawlers de IA ficam liberados" no `CLAUDE.md`). Armado e confirmado ativo em `helios` (260812, #5091). **Auditoria de retorno (#6798 item 5, 02/09/2026): 21 execuções, 0 disparos — silêncio aqui é política de pé** (guarda uma decisão explícita do CLAUDE.md sobre o bloco gerenciado da Cloudflare, "Crawlers de IA ficam liberados"), não um alarme sem uso; decisão do editor foi MANTER sem mudança. Não reabrir a mesma pergunta sem novo dado. Arme e detalhes: `docs/robots-txt-drift-check-setup.md`.

**Drift-check do system prompt do plugin de review (#5311, 14/08/2026):** task `Diaria-Plugin-Review-Drift-Check`, diária às 10:20 (logo depois do robots-txt-drift-check acima) — o system prompt de `pr-review-toolkit:code-reviewer` (e dos outros 4 agentes que `DEFAULT_EFFORT=max` dispara: `silent-failure-hunter`, `pr-test-analyzer`, `comment-analyzer`, `type-design-analyzer`) vem do marketplace, fora deste repo, e pode mudar sem aviso — o #5304 descobriu que `code-reviewer.md` tem uma diretiva de limiar de confiança ("Only report issues with confidence ≥ 80") que a instrução do hook sobrepõe por especificidade, mas isso nunca foi verificado como garantido. O check extrai só o SINAL relevante (vocabulário de filtro de confiança/severidade), não um hash do arquivo inteiro — edição cosmética do marketplace não alarma; mudança na linguagem de filtro alarma. Plugin ausente (sessão cloud, clone fresco) = skip, nunca falha. **ARMADA em 17/08/2026** na `helios` (próximo disparo 18/08 10:20 BRT). **Auditoria de retorno (#6798 item 5, 02/09/2026): 13 execuções, 0 disparos — amostra curta demais pra concluir; decisão do editor foi MANTER sem mudança.** Não reabrir a mesma pergunta sem novo dado. Arme e detalhes: `docs/plugin-review-drift-check-setup.md`.

**Smoke-test do destino do redirect `/subscribe` (perfil hospedado Kit) + `/` e `/p/{slug}` do Worker `diaria-site` (#6365, achado do fleet review da PR #6363):** task `Diaria-Subscribe-Redirect-Drift-Check`, diária às 10:30 (logo depois do plugin-review-drift-check acima, fechando o cluster matinal 10:00-10:30). O destino Kit (`https://diar-ia-br.kit.com/`) foi verificado ao vivo só UMA vez, em 26/08/2026, quando `_redirects` foi escrito — nada re-verificava depois, e depois do cutover do apex (#467) `/subscribe` é a única porta de cadastro pelo site, com o destino inteiramente fora do nosso controle (conta Kit de terceiro). `test/site-worker-routes-6359.test.ts` só garante que a REGRA existe no código committed; este alarme pergunta se o destino publicado hoje ainda serve o que promete — status 200 sozinho não basta (página de erro do Kit tipicamente vem 200), então cada alvo exige a presença dos marcadores esperados no corpo (campo de e-mail + botão "Subscribe" pro alvo Kit; marcador da home/canonical pros 2 alvos do Worker `diaria-site` — extensão do item 4 do checklist da issue, reusa `WORKER_DEV_HOST`/`EXPECTED_ROOT_MARKER`/`SAMPLE_ARCHIVE_SLUG` de `scripts/lib/apex-cutover.ts`). Achado nasce `P1` (não `P2` como os drift-checks vizinhos) — sem workaround pro leitor externo enquanto `/subscribe` estiver quebrado pós-cutover. **DECLARADA, NÃO ARMADA nesta unidade** (worktree isolado, mesma disciplina do #4750/#4910/#5311 acima) — armar via `scripts/setup-systemd-timers.ts` na checkout compartilhada (`helios`) é ação POSTERIOR do editor. Arme e detalhes: `docs/subscribe-redirect-drift-check-setup.md`.

**Guard de órfão do double opt-in do Kit (#6810, 01/09/2026):** task `Diaria-Kit-Doi-Orphan-Guard`, diária às 10:35 (logo depois do subscribe-redirect-drift-check acima, fechando o cluster matinal 10:00-10:35). Incidente de origem: em 28/08/2026, o worker `poll` já criava subscriber `inactive` (double opt-in, #6340) numa janela de ~19h em que `KIT_DOI_FORM_ID` ainda não estava deployado — `vincularKitDoiForm` virou no-op silencioso, o e-mail de confirmação nunca saiu, e 4 cadastros reais (3 vindos da Clarice) ficaram presos em `inactive` para sempre (nada no Kit promove `inactive→active` sozinho). Nenhuma camada reportava erro. O guard (`scripts/kit-doi-orphan-guard.ts`, miolo puro em `scripts/lib/kit-doi-orphan-guard.ts`) cobre só a DETECÇÃO (Ação 2 da issue): `GET /v4/subscribers?status=inactive` menos `GET /v4/forms/{kit.doiFormId}/subscribers` (`platform.config.json`) = órfãos — `inactive` há mais de 48h e nunca vinculado ao form. Alarma por e-mail + issue agregada (`scripts/lib/alarm-issues.ts`, mesmo mecanismo dos vizinhos). O resgate em si (Ação 1 — `POST /forms/{form}/subscribers/{id}` disparando a confirmação com atraso, e-mail real a pessoas reais) fica de fora de propósito — decisão do editor, fora do alcance de uma sessão autônoma. **DECLARADA, NÃO ARMADA nesta unidade** (worktree isolado, mesma disciplina dos vizinhos acima) — armar via `scripts/setup-systemd-timers.ts` na checkout compartilhada (`helios`) é ação POSTERIOR do editor. Sem doc de setup dedicado — mesmo padrão do resto do cluster matinal (só `KIT_API_KEY`, já configurado nos demais scripts REST Kit deste repo).

**Alarme de teto de assinantes do plano Kit (#7362, 03/09/2026; limiar convertido pra percentual no mesmo dia após reabertura):** task `Diaria-Kit-Subscriber-Limit-Alarm`, interval de 4h (mesma cadência de `Diaria-Clarice-Guardrail-Alarm`/`Diaria-Brevo-Diaria-Guardrail`, NÃO um horário fixo diário como os vizinhos do cluster matinal Kit — a issue mede o risco em horas, não em dias). Achado na auditoria de pré-voo de mídia paga: o plano do Kit tem teto de assinantes (`subscriber_limit`, hoje 1000 no plano "creator", `GET /v4/account`) e nenhum script do repo lia esse campo — o teto era invisível pra toda a maquinaria de guard/alarme. Medido ao vivo em 03/09/2026: `subscriber_limit 1000`, `active 629`; pela aritmética do protocolo de mídia paga em curso (~750 cadastros esperados na janela do teste), o teto seria cruzado por volta da metade da janela. Decisão original do editor (03/09/2026, manhã): "armar alarme em 900 e decidir na hora" — implementada como limiar ABSOLUTO (#7368). **Essa decisão mudou no mesmo dia**: o editor trocou o plano pra "free" (`subscriber_limit` 10000, efetivo 07/09/2026) e um limiar absoluto de 900 vira 9% de ocupação no plano novo — a issue reabriu com o pedido explícito de um limiar PERCENTUAL, aplicado depois. O guard (`scripts/kit-subscriber-limit-alarm.ts`, miolo puro em `scripts/lib/kit-subscriber-limit-alarm.ts`) lê `subscriber_limit` (`getKitAccount`, `scripts/lib/kit-client.ts`) × contagem de assinantes ATIVOS (`listAllKitSubscribers(config, {status: "active"})`) e alarma por e-mail + issue (`scripts/lib/alarm-issues.ts`) quando a OCUPAÇÃO (`ativos / subscriber_limit`) cruza `DEFAULT_KIT_SUBSCRIBER_ALARM_THRESHOLD_PCT` (85%, constante — recalibrar é trocar essa constante, não reescrever o mecanismo). Latch: arma na transição pra acima do threshold, não repete e-mail a cada execução enquanto permanece acima, re-arma se a ocupação cair de volta abaixo (inclusive por VIRADA DE PLANO — mesma contagem absoluta, teto maior). **DECLARADA, NÃO ARMADA nesta unidade** (worktree isolado, mesma disciplina dos vizinhos acima) — armar via `scripts/setup-systemd-timers.ts` na checkout compartilhada (`helios`) é ação POSTERIOR do editor. Sem doc de setup dedicado — mesmo padrão do resto do cluster Kit (só `KIT_API_KEY`, já configurado nos demais scripts REST Kit deste repo).

**Alarme das contas OpenAI Codex (#7250, registro #7316, 03/09/2026):** task `Diaria-Codex-Credential-Alarm`, diária às 10:50 BRT — fecha o cluster matinal de alarmes, depois de `Diaria-Branch-Cleanup` (10:45). Avisa quando resta **UMA** conta viva no pool do Hermes (`~/.hermes/auth.json` → `credential_pool["openai-codex"]`). Estado medido em 03/09/2026: 2 de 3 esgotadas (`usage_limit_reached`, HTTP 429), com retorno em 29/09 e 02/10 — datas que a própria OpenAI devolve no 429 (`resets_at`), não estimativa nossa.

*Por que diária, e não mais frequente.* A cadência é limitada pelo desenho do sinal, nas duas pontas. **A montante:** as contas são OAuth, não há endpoint de saldo — `last_status` só muda quando algo de fato TENTA usar a conta e leva 429, então checar de hora em hora relê o mesmo arquivo congelado. O gerador de uso é o tick do contínuo (`hermes cron list --all`, job `5d791ef6fc2c`), que em 03/09/2026 estava **`[paused]`**: com ele parado, nenhuma cadência de checagem descobre nada. **A jusante:** o horizonte de resposta é de semanas (as datas de retorno acima), e latência de detecção abaixo da latência de resposta é desperdiçada — mesmo raciocínio dos vizinhos deste cluster, com margem de ordens de grandeza. Não é menos que diária porque o evento que importa — a última conta esgotar, parando a delegação — é abrupto e silencioso. Repetição não preocupa: o script deduplica por fingerprint do estado do pool e só envia quando o estado MUDA.

*Limitação conhecida, não corrigida aqui (#7320).* `classifyCodexCredential` nunca compara `resets_at` com o agora: uma conta esgotada continua contando como esgotada mesmo depois da data de retorno, até que algo a use de novo e reescreva `last_status`. Com o contínuo pausado, isso significa que a partir de 29/09 o alarme tende a reportar menos contas vivas do que existem. Ver a issue para o veredito.

**DECLARADA E ARMADA em `helios`** — o arme depende do merge (o gerador escreve `WorkingDirectory` a partir do checkout onde roda; gerar de dentro de uma worktree produziria um unit apontando para um caminho que some no merge). Ver o PR para a saída do `--dry-run` e do `start` manual. Sem doc de setup dedicado: o único requisito é `~/.hermes/auth.json` existir, e noutra máquina o script sai com exit 1 dizendo que não mediu, de propósito.

**Alarme semanal de vencimento das issues `on-hold` (#5317, 14/08/2026):** task `Diaria-On-Hold-Vencimento-Alarm`, domingos 11:00 BRT — lê a linha `Vencimento: AAAA-MM-DD` do CORPO da issue (nunca o título) e alarma (digest semanal, nunca suprime reenvio) quando vencida ou sem data declarada; mesma classe de silêncio do #5111. **Nunca remove a label `on-hold` sozinho** — decisão explícita do editor. **Armado e confirmado ativo em `helios` (260817)** — próxima corrida domingo 11:00 BRT; `--dry-run` de verificação leu as 4 `on-hold` abertas e produziu 1 achado (#4549, sem data declarada), com a #4556 corretamente fora por ter `Vencimento: 2026-09-15` ainda no futuro. O carimbo (`~/.local/share/systemd/timers/stamp-*.timer`) foi tocado ANTES do `enable --now` pra não disparar o catch-up de `Persistent=true` na hora do arme (saída 3 do aviso impresso por `setup-systemd-timers.ts`) — o digest do #4549 sai na primeira corrida agendada, não no arme. Arme e detalhes: `docs/on-hold-vencimento-alarm-setup.md`.

**Smoke-test dos eixos de drift da home pública (#4557, registro #5005; renomeada de `Diaria-Beehiiv-Home-Meta-Check`, #6498, 28/08/2026 — o apex não é mais um tema Beehiiv desde o cutover #467):** task `Diaria-Home-Meta-Check`, diária às 09:35 — audita `og:title`, self-links `http://`, rótulos residuais em inglês (eixo genérico desde #6498, não mais amarrado ao vocabulário do tema Beehiiv), links pra host legado (#5099), hub sem link na home (#5257) e porta explícita na URL (#5106) no HTML servido por `https://diar.ia.br/`. Armado e confirmado ativo em `helios` (260812, #5091) sob o nome antigo — **o rename da task/units systemd (`diaria-beehiiv-home-meta-check.{service,timer}` → `diaria-home-meta-check.{service,timer}`) fica pendente de ação manual do editor em `helios`** (#6498; não feito nesta unidade — ver PR). Arme e detalhes: `docs/home-meta-check-setup.md`.

**Loop de SEO semanal — cobertura de indexação + Search Analytics (#4105, #1896, #1989, #4909):** task `Diaria-SEO-Weekly`, domingos 04:10 BRT (mudou de segunda, mesmo horário — decisão do editor 260810) — 3 passos: `seo-index-check.ts` (site principal, `--limit 2000`), `seo-index-check.ts --sitemap https://arquivo.diar.ia.br/sitemap.xml` (hub `arquivo`, `--out-suffix arquivo`) e `seo-pull.ts --days 28` (Search Analytics). Confirmado ativo em `helios` (260812). Arme e detalhes: `docs/seo-notes.md`.

**Alarme semanal de saúde de aquisição por canal (#5249, sobrevivência/CTR/canal novo ou parado):** task `Diaria-Acquisition-Health-Alarm`, domingos 03:30 BRT — 30min depois do `Diaria-Beehiiv-Backup` (03:00), que gera o snapshot semanal que este alarme lê. Confirmado ativo em `helios` (260812). **#5494 (16/08/2026):** `canal_parou` comparava contagem bruta de cadastros entre janelas de largura DIFERENTE (ex.: 2 dias vs 58 dias, quando um domingo é pulado) — gerou 7 falsos positivos na 1ª rodada real. `detectAcquisitionHealthFindings` agora recebe `windowDays` por janela e SUPRIME o finding quando a razão de largura excede `canalParouMaxWindowRatio` (default 1.5×), registrando a supressão (`suppressedFindings`, logada e citada no e-mail — nunca silenciada). Arme e detalhes: `docs/acquisition-health-alarm-setup.md`.

**Pull mensal de demanda (Bing Keyword Research) + autoridade (Bing backlinks) (#5128, #5130):** task `Diaria-Bing-Seo-Monthly-Pull`, dia 1 de cada mês, 09:00 BRT — 2 passos no mesmo script (`bing-pull.ts --mode keywords` e `--mode links`), sem contraparte Windows/.ps1 (registrada depois do cutover systemd). Sem doc de setup dedicado — ver docstring de `scripts/bing-pull.ts`.

**Snapshot semanal da publicação Beehiiv (#5229, `expand[]=stats` + agendamento #5230):** task `Diaria-Beehiiv-Backup`, domingos 03:00 BRT — o timer mais cedo do dia, janela reservada pro snapshot mais pesado da semana. `expand[]=stats` preserva origem (`utm_source`) e engajamento por assinante antes que `promoteBeehiivSubscription` (DELETE+CREATE diário às 05:30) sobrescreva. **Cadência semanal não fecha o problema, só reduz** — não cobre a via de CLIQUE (`workers/reativar/`), follow-up registrado na própria #5229. Armado e confirmado ativo em `helios` (260814). Arme e detalhes: `docs/beehiiv-backup-setup.md`.

**Alarme de staleness do snapshot semanal do `Diaria-Beehiiv-Backup` (#5494):** task `Diaria-Beehiiv-Backup-Staleness-Alarm`, domingos 04:00 BRT — depois do `Diaria-Beehiiv-Backup` (03:00) e do `Diaria-Acquisition-Health-Alarm` (03:30, acima). Alarma se o snapshot mais recente em `data/beehiiv-backup/` tiver mais de 7 dias OU se `isSubscribersSnapshotUsable` reprovar o mais recente (manifest com erro/skip, ou `subscribers.jsonl` ausente/vazio/corrompido). Fecha o buraco descrito na issue #5494 item 3: sem este alarme, um domingo pulado é indistinguível de "já avaliado nesta semana" no journal do `check-acquisition-health.ts`, que nunca dispara e-mail sozinho pra esse caso — e a perda é irrecuperável (`promoteBeehiivSubscription` sobrescreve a origem diariamente). **ARMADA em 17/08/2026** na `helios` (próximo disparo dom 23/08 04:00 BRT). Sem doc de setup dedicado — ver docstring de `scripts/beehiiv-backup-staleness-alarm.ts`.

**Edição diária agendada (#2068, reativada #4998, volta pro Windows #5611):** task `Diaria-Edicao-Diaria`, dom-qui 16:00 BRT — roda `/diaria-edicao {D+1} --skip newsletter,linkedin,facebook` (Stages 0-4 + pré-render, sem publicar), com guard de idempotência (pula se `data/editions/{AAMMDD}/` já existe). Fora do registry declarativo (invoca `claude -p` direto, não `npx tsx <script>.ts`). **VIA MUDOU em 17/08/2026 (#5611):** decisão do editor — a task depende de acesso ao navegador (Claude in Chrome), então roda no Windows; o timer systemd em `helios` (Linux) foi **desabilitado** (`systemctl --user disable --now diaria-edicao-diaria.timer`) pra evitar duas máquinas armadas na mesma janela. O mecanismo Windows (wrapper `.ps1` fino invocando o mesmo runner TS do Linux, `run-scheduled-edicao.ts`) está pronto no repo desde #5611, mas o **arme real da task no Task Scheduler ainda não foi confirmado** — registrar de dentro de um worktree temporário embutiria um path que será deletado (mesma ressalva do `.ps1` original), então esse passo fica para o editor rodar no clone permanente. Arme e detalhes: `docs/scheduled-edicao-setup.md`.

**Alarme de sync do OneDrive parado (#5548, 17/08/2026):** task `Diaria-OneDrive-Sync-Alarm`, a cada 4h — dois sinais independentes (`systemctl --user is-active onedrive`, só leitura; e um canário de frescor em `data/.onedrive-sync-canary.json`). Achado ao vivo: `onedrive.service` morreu com exit 0 (systemd não reinicia unit que sai "com sucesso") e ficou 17h parado sem que ninguém percebesse — causou o skip silencioso da #5526. **ARMADA em 17/08/2026** na `helios` (próximo disparo 17/08 20:00 BRT). Arme e detalhes (incluindo `skip_file`/`Restart=always` recomendados, itens 2 e 4 da issue, também não aplicados ao vivo nesta unidade): `docs/onedrive-sync-setup.md`.

**Sweep genérico de units systemd falhas (#5563, 17/08/2026):** task `Diaria-Systemd-Failed-Units-Alarm`, a cada 2h — varre `systemctl --user list-units 'diaria-*.service' --state=failed` (só leitura) e alarma qualquer unit `diaria-*.service` neste estado, cobrindo de graça as ~34 tasks deste registro em vez de um alarme artesanal por task. Achado ao vivo que motivou a issue: `diaria-edicao-diaria.service` falhou 4x em silêncio (`spawnSync claude ENOENT`, corrigido no PR #5562) porque `systemctl --user list-timers` continua mostrando o timer como saudável mesmo com o service morrendo em ~1s — só `--state=failed` revela. **NÃO cobre** unit que nunca chegou a ser disparada pelo timer (nada em `failed` pra um service nunca invocado) — esse caso é coberto pela task específica abaixo. **ARMADA em 17/08/2026** na `helios` (próximo disparo 17/08 20:00 BRT). Sem doc de setup dedicado — ver docstring de `scripts/systemd-failed-units-alarm.ts`.

**Alarme de TAXA de falha das últimas execuções — CORTADO (#5765 criou, #6798 removeu, 01/09/2026).** Existiu como task `Diaria-Systemd-Unit-Rate-Alarm`, a cada 4h — `scripts/systemd-unit-rate-alarm.ts` percorria TODO o registro e lia `journalctl --user -u <unit>` por unit, alarmando quando ≥2 das últimas 5 execuções falhavam. Removido pela auditoria do #6798: as 4 units que ele efetivamente viu (`clarice-novos`, `clarice-novos-tarde`, `clarice-envio`, `clarice-guardrail-alarm`) são subconjunto das 11 cobertas pelo `Diaria-Systemd-Failed-Units-Alarm` (sweep de ESTADO, item acima), e os dois chegaram a abrir issues paralelas pro mesmo incidente (#6455–6458 vs #5615/#5826) — nenhuma unit apareceu SÓ na taxa. Medição: 4 issues, 0 correções, 2 dias de vida. **`Diaria-Studio-Liveness-Alarm` (item abaixo) NÃO faz parte deste corte** — vigia HTTP, não estado systemd, e nunca teve sobreposição com o sweep de estado. **Ação manual pendente do editor:** desarmar a task no `helios` — `systemctl --user disable --now diaria-systemd-unit-rate-alarm.timer` (remover os arquivos `.service`/`.timer` de `~/.config/systemd/user/` é opcional, `disable --now` já basta pra parar de disparar).

**Alarme de staleness específico do `Diaria-Edicao-Diaria` (#5563, 17/08/2026):** task `Diaria-Edicao-Diaria-Staleness-Alarm`, diária 18:20 BRT — ~2h20 depois do disparo do `diaria-edicao-diaria.timer` (dom-qui 16:00 BRT). Complementar ao sweep genérico acima: distingue "nunca disparou" (nenhuma linha em `data/overnight-schedule.log` pra edição de amanhã) de "disparou e falhou" (última linha `FAIL`, ou `START` travado além de 3h) — "disparou e pulou por idempotência" (edição já iniciada à mão pelo editor, `data/editions/{AAMMDD}/` existe) é tratado como caso legítimo e nunca alarma. Fecha sozinho no dia seguinte (fingerprint por `AAMMDD`, mesmo padrão do `Diaria-LinkedIn-Weekly-Staleness-Alarm`). **DESABILITADA em 17/08/2026, mesma sessão do #5611** — o alarme não checa se algum timer/task está de fato armado, só se o log tem entrada pra edição de amanhã; com o timer Linux desligado e a task Windows sem arme confirmado (ver item acima), ele alarmaria `alarm-never-fired` todo dia sobre um estado hoje intencional. **Situação atualizada em 19/08/2026:** o editor informou que **cancelou o agendamento da edição diária de propósito** — não é uma pane nem um arme pendente. Não há task/timer de edição armado em máquina nenhuma (Windows: só `Diaria-Overnight-Watchdog`, `Disabled` — **confirmado ao vivo em 19/08/2026** via `Get-ScheduledTask`; Linux: timer desligado no #5611, **estado do `helios` NÃO reconsultado nesta sessão** — sem SSH configurado na máquina do editor, a afirmação vem do registro de 17/08. Para certeza: `systemctl --user list-timers --all | grep diaria` em `helios`), e a última linha de `data/overnight-schedule.log` é de 17/08. A edição diária passou a ser rodada À MÃO pelo editor. Portanto este alarme continua desarmado **porque não há nada para vigiar**, não porque falta confirmar um arme — a redação anterior ("reativação fica para depois que o editor confirmar o arme real da task Windows") descrevia uma pendência que não existe. Reativar o alarme só faz sentido se o agendamento voltar, e isso é decisão nova do editor, não tarefa em aberto. Ver `docs/scheduled-edicao-setup.md` §"Alarme de staleness". Sem doc de setup dedicado — ver docstring de `scripts/edicao-diaria-staleness-alarm.ts`.

**Detector de task nunca armada (#5607, 17/08/2026), ARMADA em `helios` (28/08/2026).** Task `Diaria-Task-Never-Armed-Alarm`, diária 18:30 BRT — cruza o registro declarativo (`scheduled-tasks.ts --json`) contra `systemctl --user list-timers --all` nas duas direções: task no registro sem timer armado (alarme forte, P1) e timer `diaria-*` armado sem task correspondente no registro (alarme mais fraco, P3 — allowlist explícita cobre `diaria-edicao-diaria`/`diaria-overnight-watchdog`, fora do schema por desenho). Achado ao vivo que motivou a issue: 6 tasks do registro (incluindo o próprio `Diaria-Systemd-Failed-Units-Alarm`) nunca tinham timer armado na `helios`, e nenhum alarme existente pegava esse caso — só o sweep `--state=failed` existia, que não cobre unit nunca disparada. **ARMADA em `helios` (28/08/2026, rodada overnight 260827b)** — units `diaria-task-never-armed-alarm.{service,timer}` presentes em `~/.config/systemd/user/`, timer `enabled`+`active`, carimbo pré-tocado pra evitar catch-up-fire (#5140). Sem doc de setup dedicado — ver docstring de `scripts/task-never-armed-alarm.ts`.

**Ingestão diária de gasto de aquisição — histórico da proposta original (#5502 Parte C, 17/08/2026), superada em parte pelo #5704.** A proposta original era uma task ÚNICA rodando os dois canais em sequência (`google-ads-ingest-spend.ts && microsoft-ads-ingest-spend.ts`), decisão de task agendada dedicada em vez de chamada fail-soft embutida no início de `cac-report.ts` — motivo: `cac-report.ts`/`/api/ads` do Studio rodam SOB DEMANDA (o dado de gasto muda ~1×/trimestre por decisão explícita da issue #5236 — ver docstring de `scripts/studio-ui/studio-ads.ts`), então acoplar a ingestão à leitura do relatório faria toda visualização pagar a latência de 2 chamadas de rede mesmo quando ninguém mudou o CSV. **O Google Ads Basic Access saiu da fila em 19/08/2026 (#5704)** — as credenciais `GOOGLE_ADS_*` já estão presentes e funcionais (confirmado ao vivo via consulta GAQL real, 200 com dados). A Microsoft segue sem credencial (`docs/microsoft-ads-api-setup.md`), então o #5704 registrou só o lado Google, com nome/task PRÓPRIOS (não o `Diaria-Ads-Spend-Ingest` combinado que a proposta original citava) — ver entrada abaixo. Quando a credencial Microsoft existir, decidir então se ela ganha task própria ou se as duas passam a compartilhar uma (mudança de escopo, não deste registro).

**Ingestão diária de gasto do Google Ads (#5704, 19/08/2026), ARMADA em `helios` (28/08/2026).** Task `Diaria-Google-Ads-Spend-Ingest`, diária às 09:50 BRT — roda `scripts/google-ads-ingest-spend.ts` (item 4 da issue #5704; itens 1-3, credenciais `GOOGLE_ADS_*`, já confirmados funcionais), que consulta GAQL por dia num range explícito, agrega por mês e atualiza as linhas do canal "Google Ads" em `data/aquisicao/spend.csv`, preservando LinkedIn/Beehiiv Boosts e qualquer mês fora do range consultado. Fail-soft por design (#5237): sem credencial ou com a API indisponível, o script sai limpo com exit 0 sem tocar o CSV, então a task nunca falha "de verdade". Horário 09:50 fica dentro do cluster matinal de checks/syncs do registro (09:00-09:45 já ocupados por outras tasks), sem colisão de horário — a janela também é a mesma que a proposta original do #5502 tinha reservado, agora ocupada só pelo lado Google. **ARMADA em `helios` (28/08/2026, rodada overnight 260827b)** — units `diaria-google-ads-spend-ingest.{service,timer}` presentes em `~/.config/systemd/user/`, timer `enabled`+`active`, carimbo pré-tocado pra evitar catch-up-fire (#5140); nenhuma máquina Windows roda mais tasks `Diaria-*` (#5074).\n\n**Captura de motivos editoriais do Microsoft Advertising (#5878, 22/08/2026), DECLARADA — implementada nesta sessão, ainda NÃO armada.** Task `Diaria-Microsoft-Ads-Editorial-Reasons`, diária às 10:00 BRT (10min após o `Diaria-Google-Ads-Spend-Ingest` 09:50, sem colisão) — roda `scripts/microsoft-ads-editorial-reasons.ts`, que capta `GetAssetGroupsEditorialReasons` via Campaign Management API v13 SOAP (endpoint distinto do Reporting API, chamada síncrona — não follow submit→poll→download). Output: `data/microsoft-ads/editorial-reasons-{YYYY-MM-DD}.json`. Fail-soft por design: sem as 6 env vars `MICROSOFT_ADS_*` → exit 0, nada escrito (mesmo padrão do #5704). Reusa credencial já no Doppler (#5928, 21/08). **Ainda NÃO armada nesta unidade** (arme real via `scripts/setup-systemd-timers.ts` da checkout compartilhada `helios` é ação POSTERIOR do editor).

**Alarme de liveness do origem do Studio (#5759, 20/08/2026), ARMADA em `helios` (28/08/2026).** Task `Diaria-Studio-Liveness-Alarm`, a cada 1h — `GET http://127.0.0.1:4174/` (`scripts/studio-liveness-alarm.ts`) com timeout de 5s, alarma só após **2 falhas consecutivas** (nunca no restart normal do serviço, ~7s: `RestartSec=5` + boot do `tsx`). Achado ao vivo que motivou a issue: `diaria-studio-server.service` ficou ~15min `active/running` sem escutar a porta 4174 (zumbi de restart travado — `server.close()` pendurado numa conexão SSE aberta, `/api/events`) e o único sweep genérico de serviço deste registro (`Diaria-Systemd-Failed-Units-Alarm`, `--state=failed`) é **cego por construção** a esse modo de falha — uma unit zumbi nunca entra em `failed`. A mecânica do zumbi em si já foi corrigida (#5737/#5760, `scripts/lib/shutdown-with-timeout.ts`); esta task é só o sensor de detecção que faltava. Fail-soft honesto: só roda o check HTTP quando `systemctl --user is-active diaria-studio-server.service` reconhece a unit (`active`/`inactive`/`failed` — máquina que roda o serviço); `unknown` (sessão cloud, clone fresco) é "não detectável aqui", nunca alarme falso — mesmo padrão de `onedrive-sync-alarm.ts`. **Cadência 1h, não os 10min sugeridos pela issue** — `ScheduledTaskSchedule.interval` só suporta granularidade de hora inteira (ver docstring da entry em `scripts/lib/scheduled-tasks.ts`); estender o schema pra granularidade de minuto tocaria `scheduled-task-status.ts` (staleness/overdue usado por várias tasks) de forma mais ampla que o escopo desta issue, então a decisão foi usar o interval mais fino já suportado (1h, precedente `Diaria-Clarice-Dashboard-Precompute`) em vez de expandir o schema — 2 falhas consecutivas cobrem até ~2h de indisponibilidade não detectada, mais frouxo que os 10min sugeridos mas uma melhora categórica sobre "o editor é o monitor" (estado anterior ao vivo em 20/08/2026). **O alarme NUNCA reinicia o serviço** — só reporta (e-mail + issue via `alarm-issues.ts`); religar é sempre ação manual do editor, de propósito (evita mascarar o defeito). **ARMADA em `helios` (28/08/2026, rodada overnight 260827b)** — units `diaria-studio-liveness-alarm.{service,timer}` presentes em `~/.config/systemd/user/`, timer `enabled`+`active`, carimbo pré-tocado pra evitar catch-up-fire (#5140). Sem doc de setup dedicado — ver docstring de `scripts/studio-liveness-alarm.ts`.

**Build semanal dos hubs temáticos (#5754, 20/08/2026), DECLARADA — `enabled: false` DE PROPÓSITO desde 26/08/2026 (#6267), não "ainda não armada".** **NÃO ARMAR.** Como declarada, a task não pode ter sucesso: `--check-facts` sem `--skip-fact-check` exige `data/hub-fact-check/{slug}-report.json`, arquivo que nunca existiu pra nenhum hub (o dispatch do agente `fact-checker mode:hub` nunca foi ligado — ver `.claude/agents/fact-checker.md` §"Modo hub"), então ela abortaria `exit 2` no 1º hub todo domingo. Um 2º bloqueador independente mata a variante "só regenerar as fontes": `UPDATED_DATE` é hand-written por hub e `validateHubContent` exige `updatedDate >= sourceEditions[0].date` (guard do #5124), então fonte regenerada sempre avança à frente dele — com as fontes regeneradas, 5 testes de render quebram, e a checkout compartilhada ficaria suja toda semana. Decisão do editor (#6267): o regen fica **manual**, barato e seguro desde o #6270 (`--all` + preservação de `manual: true`), rastreado por issue quando a defasagem acumula (#6274). Descrição original abaixo. Task `Diaria-Hub-Pages-Build`, domingos às 08:05 BRT (5min depois dos 08:00 sugeridos pela issue — `minute:0` bateria em toda batida de interval de 2h/4h já registrada, mesmo raciocínio do `Diaria-Acquisition-Health-Alarm`/#5249) — roda `scripts/build-hub-page.ts --all --check-facts`, antes da janela 09:30-10:20 dos checks de drift/staleness matinais (`Diaria-Hub-Staleness-Check`/`Diaria-Hub-Drift-Check`/`Diaria-Robots-Txt-Drift-Check`/`Diaria-Plugin-Review-Drift-Check`), pra que eles já vejam o resultado atualizado. Reusa `scripts/build-hub-page.ts` direto como step (mesmo padrão dos vizinhos `Diaria-Hub-Drift-Check`/`Diaria-Hub-Staleness-Check` — sem wrapper dedicado), com `--check-facts` explícito porque `--all` sozinho NÃO aciona esse gate (é opt-in por flag) — sem a flag, o build semanal contornaria a verificação de conteúdo silenciosamente. **A task só ESCREVE `{slug}.generated.ts` local — nunca commita nem deploya o Worker `arquivo` sozinha** (decisão explícita da issue: gerar e reportar diff é o escopo, deploy automático de Worker é blast radius fora do pedido); cabe ao editor (ou a uma rodada overnight/develop que veja o diff) revisar e deployar manualmente para o resultado ter efeito em produção. Sem `--skip-fact-check`: um hub sem relatório de fact-check fresco aborta a run inteira (`process.exit(2)`, comportamento já existente do script) — falhar alto numa run desassistida semanal é preferível a publicar conteúdo não verificado. **Ainda NÃO armada nesta unidade** (worktree isolado, mesma disciplina do #5220/#5217/#5311/#5494/#5607/#5704) — arme real via `scripts/setup-systemd-timers.ts` da checkout compartilhada (`helios`) é ação POSTERIOR do editor. Sem doc de setup dedicado — ver docstring de `scripts/build-hub-page.ts`.

**Rodada semanal contínua do sunset de assinantes mortos (#5807, 21/08/2026, reescopo do comentário do editor 20/08 20:56), DECLARADA — `enabled: false` DE PROPÓSITO, não "ainda não armada".** Task `Diaria-Sunset-Weekly`, domingos às 09:20 BRT (depois de `Diaria-Beehiiv-Backup` 03:00 + `Diaria-Beehiiv-Backup-Staleness-Alarm` 04:00, antes de `Diaria-Geo-Citation-Staleness-Alarm` 10:30 e `Diaria-On-Hold-Vencimento-Alarm` 11:00 — sem colisão com nenhuma weekly/daily já registrada, checado via `scheduled-tasks.ts --list` antes de escolher o horário). Roda `scripts/sunset-dead-subscribers.ts --push`, que desde este reescopo É a rodada semanal completa (não existe mais um modo separado): (a) garante snapshot fresco de `data/beehiiv-backup/` via `ensureFreshSnapshot` — dispara `backup-beehiiv.ts` sozinho se o mais recente passar de 7 dias (mesmo limiar de `Diaria-Beehiiv-Backup-Staleness-Alarm`), só quando `--snapshot` não foi passado manualmente; (b) avalia TODOS os ativos maduros (`status=active` AND `total_received>=20`) contra o critério `cadastro>=90 dias E abertura<=10% E zero cliques na vida inteira` (piso de idade combinado, #5849, sessão `/diaria-develop` 260821 — sem ele, `receivedMin` sozinho misturava assinantes mortos de verdade com recém-chegados dentro da janela de medição de campanha; guard de clique herdado do script original, #5807 análise 260820: abertura baixa sozinha nunca decide — 101 dos candidatos da medição de origem tinham pixel de abertura bloqueado mas cliques reais); (c) exclui quem já se descadastrou organicamente (`status!=active` nunca entra) e quem já passou pelo store `brevo_diaria` (`excludeAlreadyInStore`, dedup por e-mail normalizado — mesmo padrão de `computeContactsToIngest` em `sync-pending-to-brevo.ts`); (d) aplica o guard de blast radius (recusa o `--push` inteiro se passar de 20% dos ativos maduros CONSIDERADOS nesta rodada, `--force-blast-radius` como escape hatch) e o guard de folga da fila Brevo compartilhada (`brevo_diaria.daily_send_cap`, mesma fila de `sync-pending-to-brevo.ts`); (e) grava 1 linha append-only em `data/brevo-diaria/sunset-rounds.jsonl` por rodada (avaliados/elegíveis/já-no-store/considerados/blast-radius/fila/aplicados/falhados/avisados/pulados+motivo — distinto de `data/brevo-diaria/sunset-log.jsonl`, que é 1 linha por CONTATO movido, não por rodada). **A task nasce `enabled: false` e o interruptor fica assim até segunda ordem** — a #5849 achou que `receivedMin=20` sozinho não separava "morto de verdade" de "recém-chegado dentro da janela de medição do teste pago 2608" (23 dos 88 candidatos de uma rodada real tinham só ~4 semanas de vida, dentro da janela de campanhas em medição, #5734/#4556). **JÁ RESOLVIDA** (sessão `/diaria-develop` 260821): o critério agora combina `receivedMin` E idade mínima de cadastro (90 dias), ver item (b) acima e `SunsetThresholds` em `scripts/sunset-dead-subscribers.ts`. O interruptor segue `false` porque ligar a execução automática é uma decisão SEPARADA (ainda não tomada), não mais um bloqueio de "qual critério usar". O mecanismo inteiro (script + testes + registro) está pronto e testado; armar é trocar `enabled: false` → `true` (uma linha) e rodar `scripts/setup-systemd-timers.ts` da checkout compartilhada — nenhuma outra mudança de código esperada. `guard.requiredFile: brevo-diaria/contacts.json` (mesmo sinal de "junction `data/` ainda não montada" de `Diaria-Brevo-Diaria-Evaluate`, que compartilha o mesmo store). Sem doc de setup dedicado — ver docstring de `scripts/sunset-dead-subscribers.ts`.

**Ciclo de vida do teste de 3 canais pagos (#5845, 21/08/2026), ARMADA em `helios` (28/08/2026).** Task `Diaria-Ads-Test-Watch`, diária 06:30 BRT — `scripts/ads-test-watch.ts` cobra sozinha os marcos do teste de 3 canais pagos (#5524, R$ 4.500) que hoje dependiam 100% da memória do editor (`data/aquisicao/campanhas-260816/00-PROTOCOLO.md` §7.1/§7.2/§8.3). Lê `data/aquisicao/teste-2608/run-state.json` (pré-registro imutável, gravado uma vez por `scripts/ads-test-d0.ts --d0 AAAA-MM-DD`) e decide, por dia, até 5 ações independentes: (1) alarma se a data de D0 recomendada já passou sem pré-registro; (2) dentro da janela de 15 dias, alarma se faltar linha de ONTEM em `data/aquisicao/clicks-2608.csv` para algum dos 3 braços (§8.3); (3) dentro da janela, alarma se a condição de morte "gasto acumulado > 2× o planejado" (§3.2 item 3) disparar; (4) em D+21, comenta em #5838 confirmando que o religamento de `Diaria-Brevo-Diaria-Evaluate` é devido; (5) na data de apuração pré-registrada (1º domingo ≥ D+42), roda `build-origem-map.ts` imediatamente antes de `cac-report.ts --snapshot {data}` (nunca separados, §7.2) e registra o relatório — mas só se `isSubscribersSnapshotUsable` (`scripts/lib/beehiiv-backup-snapshots.ts`, #5281) aprovar o snapshot; snapshot inutilizável alarma e NÃO grava relatório. Idempotência assimétrica: religamento e apuração são eventos de UMA VEZ (`data/aquisicao/teste-2608/watch-state.json`); os 3 primeiros alarmes REPETEM todo dia enquanto a condição continuar verdadeira — dinheiro real em jogo, cursor de idempotência silenciaria um problema recorrente. Guard duplo de `data/` ausente: `guard.requiredFile: aquisicao/spend.csv` no registro (task-runner aborta antes de rodar) + checagem interna via `detectExecMode` (`scripts/lib/exec-mode.ts`) no próprio script, defesa em profundidade. **Nunca chama API paga (Google/Meta/Microsoft Ads) ao vivo** — só lê `clicks-2608.csv` (reconciliado manualmente pelo editor) e o snapshot Beehiiv já em disco. Lógica pura em `scripts/lib/ads-test-schedule.ts` (derivação de datas, testada contra "1º domingo ≥ D+42" e virada de mês/ano) e `scripts/lib/ads-test-watch.ts` (plano diário + parsing do CSV + condição de morte + e-mails). **ARMADA em `helios` (28/08/2026, rodada overnight 260827b)** — units `diaria-ads-test-watch.{service,timer}` presentes em `~/.config/systemd/user/`, timer `enabled`+`active`, carimbo pré-tocado pra evitar catch-up-fire (#5140). Sem doc de setup dedicado — ver docstring de `scripts/ads-test-watch.ts`/`scripts/ads-test-d0.ts`.

**Sequência diária de onboarding via Brevo transacional (#5908, 22-23/08/2026), ARMADA em `helios`.** Task `Diaria-Onboarding-Welcome-Run`, diária 09:05 BRT (logo depois de `Diaria-Clarice-Novos`, 09:00, mesmo cluster matinal de detecção de gente nova; antes do resto do cluster 09:15-09:50) — `scripts/onboarding-welcome-run.ts --send` detecta assinantes novos na Beehiiv (API pública, `created_at__gte` sobre um cursor persistido em `data/onboarding/store.json`) e dispara e-mail 1 (transacional, imediato), e-mail 2 (transacional, D+3) e e-mail 3 (CAMPANHA Brevo — marketing, não transacional — D+10 condicional a zero aberturas/cliques) pela conta `brevo_diaria`. Nasceu de #5808 (sequência escrita/humanizada numa automação Beehiiv em Draft) esbarrando em #5641-like plan-gate: publicar QUALQUER automação, mesmo 1 e-mail sem condição, exige o plano Scale — decisão do editor (22/08) foi não fazer upgrade e buscar outro caminho, escopado nesta issue. **Guard duro de conteúdo**: enquanto `data/snippets/onboarding-{1,2,3}.md` carregasse o marcador `ONBOARDING-CORPO-PENDENTE`, o script pulava o envio (skip `corpo_pendente`) — nenhum risco de sair copy vazia. Sessão `/diaria-develop` 260823 exportou os 3 corpos definitivos da automação `Onboarding — Boas-vindas` (`aut_48bcae89-e812-4711-a6ea-1f0729e7e6d8`, workspace staging via `get_automation_email_content`, já que a automação nunca foi publicada) para os 3 snippets, removendo o marcador, e rodou o bootstrap (`--send` com `store.last_detection_cursor` ainda nulo): cursor marcado em `now`, `detected_new: 0` — a base existente de assinantes fica de fora por desenho (onboarding não é retroativo). Sender `oi@diar.ia.br` já verificado na conta `brevo_diaria` (confirmado ao vivo 22/08). `guard.requiredFile: onboarding/store.json` (achado do review de fleet do PR #5956) — sem ele, uma junction `data/` caída momentaneamente em `helios` faria o store voltar vazio silenciosamente e a semântica de bootstrap resetaria o tracking de quem já está no meio da sequência (ex: aguardando e-mail 2 em D+3), sem aviso; mesmo padrão de guard das tasks-irmãs `Diaria-Clarice-Novos`/`-Tarde`, `Diaria-Clarice-Envio`, `Diaria-Brevo-Diaria-Evaluate`, `Diaria-Ads-Test-Watch`. **ARMADA em `helios`** — units `diaria-onboarding-welcome-run.{service,timer}` presentes em `~/.config/systemd/user/`, timer `enabled` + `active`, com execução real registrada em `data/onboarding/.welcome-run.log` (centenas de linhas `[onboarding] email1 → ...` com message-ids `smtp-relay.mailin.fr` em 24/08/2026). **Esta entrada dizia "ainda NÃO armada nesta unidade" até 25/08/2026** — texto correto quando redigido (sessão develop no Windows do editor), nunca atualizado depois do arme real em `helios`. Drift corrigido nesta linha (PR #6106); o guard mecânico contra reincidência é rastreado em #6105. Ver também o incidente #6043 (`created_at__gte` é no-op na API v2 pública da Beehiiv → 585 boas-vindas indevidas em 24/08), corrigido em `df8690cf` (#6054). Sem doc de setup dedicado — ver docstring de `scripts/onboarding-welcome-run.ts`.

**GC de registros encerrados de `data/sessions/` (#6130, 25/08/2026), ARMADA em `helios` (28/08/2026).** Task `Diaria-Session-Registry-Gc`, diária às 09:55 BRT (fim do cluster matinal de checks/alarmes 09:00-10:20, depois de `Diaria-Onboarding-Welcome-Run` 09:05, sem colisão) — `scripts/session-registry-gc.ts` remove registro de sessão `/diaria-overnight`/`/diaria-develop`/`/diaria-continuo` ENCERRADA de `data/sessions/*.json` (arquivo real + suas cópias de conflito do OneDrive, e backup ÓRFÃO sem arquivo real correspondente — o achado ao vivo de "11 arquivos, o mais antigo com 10 dias"). Toda a decisão vive em `planSessionGc`/`garbageCollectSessions`/`decideSessionGc` (`scripts/lib/session-registry.ts`) e **nunca remove por staleness de heartbeat sozinha** — a ressalva da própria issue: um registro `stale: true` correspondeu, na investigação que motivou o #6130, a uma sessão VIVA que só tinha parado de bater heartbeat (processo `claude` ainda rodando no `helios`). Árvore de decisão: (1) heartbeat mais recente do grupo dentro de `SOFT_STALE_MS` (90min) → mantém; (2) além disso, se algum registro do grupo foi escrito pela MÁQUINA LOCAL e carrega `pid` CONFIRMADO VIVO (`process.kill(pid, 0)`) → mantém INDEPENDENTE da idade do heartbeat (**#6294: pid "morto" deixou de remover na hora** — a fonte do pid, `process.ppid` gravado por `inject-session-id.mjs`/`session-beacon.mjs`, foi medida ao vivo apontando pra um processo efêmero em vez do processo real da sessão; um pid "morto" cai pro branch (3) em vez de remover sozinho); (3) sem sinal de processo VIVO verificável (máquina diferente do host que roda o GC, nenhum `pid` gravado, ou `pid` reportado morto) → só remove além de `GC_CONSERVATIVE_MAX_AGE_MS` (7 dias default, override via `--max-age-days`) — janela deliberadamente bem maior que qualquer heartbeat esperado, pra nunca remover uma sessão que ainda pode estar viva noutra máquina. `--dry-run` avalia e imprime sem tocar disco. **ARMADA em `helios` (28/08/2026, rodada overnight 260827b)** — units `diaria-session-registry-gc.{service,timer}` presentes em `~/.config/systemd/user/`, timer `enabled`+`active`, carimbo pré-tocado pra evitar catch-up-fire (#5140). Sem doc de setup dedicado — ver docstring de `scripts/session-registry-gc.ts`.

**CORTADA (#6798 item 5, 02/09/2026).** Task `Diaria-Session-Registry-SafeBackup-Alarm` (alarme de cópia de conflito do OneDrive em `data/sessions/`, #6130) foi removida do registro declarativo e do repo: 46 issues abertas (#6518-#6523, #6576-#6580, #6685-#6689, #6789 + #6573), 0 correções — 39% de todas as issues de alarme do projeto na auditoria original. O problema de fundo que ela sinalizava (cópias de conflito do OneDrive em `data/sessions/`) segue rastreado em #6573 e #7043; o cleanup de backups já reconciliados mergeou no #7077 (`session-registry-cleanup-safebackups.ts`, #6970 — mecanismo INDEPENDENTE deste alarme, mantido). **Ação manual pendente no `helios`** (nunca de sessão desassistida): `systemctl --user disable --now diaria-session-registry-safebackup-alarm.timer && rm ~/.config/systemd/user/diaria-session-registry-safebackup-alarm.{service,timer} && systemctl --user daemon-reload` — sem isso o timer segue armado apontando pra um script que não existe mais e falha duro no `run-task.ts`, alimentando o `Diaria-Systemd-Failed-Units-Alarm` com ruído (mesmo efeito de 2ª ordem já visto no corte do `systemd-unit-rate-alarm` e do `cursos-error-alarm`, ambos nesta mesma issue).

`hub-staleness-check` (fingerprint por hub, #6254/#6295) e `robots-txt-drift-check`/`hub-drift-check`/`plugin-review-drift-check` (0 disparos, amostra curta demais pra concluir — decisão do editor: MANTER sem mudança) foram os outros itens desta rodada de auditoria; ver #6798 pro plano completo.

**Reconciliação diária do backlog aberto (#6198, 26/08/2026), ARMADA em `helios` (28/08/2026).** Task `Diaria-Backlog-Reconcile`, diária às 10:10 BRT (fim do cluster matinal de checks/alarmes 09:00-10:20 — o slot 10:05 que a precedia ficou livre desde o corte de `Diaria-Session-Registry-SafeBackup-Alarm`, #6798 item 5). `scripts/backlog-reconcile.ts` (lógica pura em `scripts/lib/backlog-reconcile.ts`) percorre TODAS as issues abertas via `gh issue list` e trata 4 padrões de drift entre labels × marcador `aguardando-ate:` × estado, sem ler prosa — nascida da auditoria de 26/08 (#6191) que achou 7 issues mal classificadas em 40, três dias depois da #5969 ter achado 2 em 21: o drift acumula sozinho porque nada varria o backlog fora de uma rodada overnight/develop. **Padrões 1 e 2 (marcador × label de deferimento) são correção SEGURA** — contradição pura entre dois sinais, o marcador (mais específico) vence: quando o ÚNICO sinal roteável em conflito é uma das labels de deferimento (`not-this-week`/`next-month`/`on-hold`/`wontfix`), a task remove a(s) label(s) e mantém o marcador, roteando via `routeIssue` (`scripts/route-issue.ts` — nunca `gh issue edit` direto, é o único caminho que valida o resultado pós-escrita) para `agendada` (marcador ainda futuro) ou `overnight` (marcador já expirado, nada mais a sincronizar). Se QUALQUER outra label roteável coexistir (`windows`, `trade-off-real`, etc.) a task **nunca adivinha** — vira alarme, nunca correção, respeitando a restrição inegociável da issue: nunca remover um deferimento a partir de um sinal isolado. **Padrões 3 e 4 são só ALARME, nunca correção** — exigem contexto que o módulo não lê: padrão 3 é uma label de bloqueio (`external-blocker`/`kit-migration`/`beehiiv`/`bloqueio-execucao`) compartilhada entre uma issue-filha (referenciada via "Fatia de #N"/"Desdobrada d[ae] #N" no corpo) e sua mãe — herança que pode ou não fazer sentido pro escopo específico da fatia (fixture real: #6187, cache híbrido Beehiiv/Kit herdando `kit-migration` da épica #463 mesmo sendo 100% código local); padrão 4 é um checkbox (`- [ ] `) ainda aberto no corpo de uma issue já classificada `fora-de-rodada` — nada revisita issues terminais, então um item pode ter sido resolvido alhures sem que ninguém marque aqui (fixture real: #6047, gate da migração Kit com 4 itens já resolvidos em #6050/#6181/#6111/comentário, mas a issue "Fora de rodada" nunca foi revisitada). O relatório final (stdout, capturado no log da task) sempre separa CORRIGIDOS de SÓ ALARMADOS em seções distintas. Idempotente por construção — os 4 detectores são puros/determinísticos e `routeIssue` releva o estado da issue antes de escrever (não decide a partir do snapshot em lote deste script), então uma corrida entre esta task e outra sessão editando a mesma issue nunca aplica correção sobre estado obsoleto (achado ao vivo durante a própria auditoria #6191: outra sessão editou uma issue 3 segundos depois de outra ação). Testes com as 7 issues-fixture reais citadas acima em `test/backlog-reconcile.test.ts` (lógica pura) e `test/backlog-reconcile-cli.test.ts` (camada de I/O, `gh` fake em memória) — inclui casos de idempotência (rodar 2× seguidas não re-corrige nem duplica comentário) e o caso ambíguo (marcador + deferimento + `windows` coexistindo → alarme, nunca fix). **Fora do escopo desta unidade, deliberadamente**: os dois bullets adicionais que a issue também menciona na seção "Escopo" — "issue CLOSED ainda com label de fila" (`routeIssue` não suporta editar labels de uma issue fechada sem efeito colateral, a validação pós-escrita dele sempre resolve `fora-de-rodada` pra CLOSED) e "`trade-off-real` em issue com decisão registrada posterior" (julgar se a decisão FECHOU o trade-off ou só resolveu parte dele exige ler a prosa da decisão — o próprio módulo `issue-exec-track.ts` documenta o contra-exemplo #4555, onde a coexistência das duas labels é correta). **ARMADA em `helios` (28/08/2026, rodada overnight 260827b)** — units `diaria-backlog-reconcile.{service,timer}` presentes em `~/.config/systemd/user/`, timer `enabled`+`active`, carimbo pré-tocado pra evitar catch-up-fire (#5140). Sem doc de setup dedicado — ver docstring de `scripts/backlog-reconcile.ts`/`scripts/lib/backlog-reconcile.ts`.

**Sync recorrente de assinantes ativos Beehiiv -> Kit (#6093, 25/08/2026), DECLARADA — ainda NÃO armada.** Task `Diaria-Kit-Subscriber-Sync`, diária às 09:25 BRT (janela livre entre `Diaria-Cursos-Kv-Sync` 09:15 e o cluster de checks matinais 09:30 em diante — não 09:20 como a issue sugeriu, porque esse minuto já pertence a `Diaria-Sunset-Weekly`, domingos; dailies rodam todo dia da semana, então colidiriam nos domingos) — roda `scripts/sync-beehiiv-subscribers-kit.ts --push` (#6091/#6092, mergeado 25/08/2026), que mantém o Kit convergente com os assinantes `active` da Beehiiv (cria/reativa quem falta via `POST /v4/subscribers`; NUNCA remove/desativa ninguém — escopo aditivo de propósito). Pré-requisito de QUALQUER switchover real do épico #461 (#464 newsletter, #6048 signup, #466 survey) — sem recorrência, o Kit volta a congelar no próximo assinante novo, exatamente o comportamento observado antes do #6091 (Kit travado em 585, todos `created_at: 2026-08-24`, desde a importação pontual do #6047). Sem `guard.requiredFile` — o próprio script já embute um guard de blast radius (lista do Kit devolvida com menos de 50% do que tinha na última run bem-sucedida, persistido em `data/kit-subscriber-sync-state.json`, bloqueia TODA a rodada inclusive dry-run — provável falha de auth/paginação silenciosa, não "todo mundo cancelou"). **Ainda NÃO armada nesta unidade** (worktree isolado, mesma disciplina das outras entradas desta lista) — arme real via `scripts/setup-systemd-timers.ts` da checkout compartilhada (`helios`) é ação POSTERIOR do editor. Sem doc de setup dedicado — ver docstring de `scripts/sync-beehiiv-subscribers-kit.ts`.

**Drenagem/agregação dos relatórios DMARC de `news.diar.ia.br` (#6189, extraída da #6111, 26/08/2026), ARMADA em `helios` (28/08/2026, issue fechada).** Task `Diaria-Dmarc-Drain`, diária às 10:25 BRT (logo depois de `Diaria-Hub-Staleness-Check` 10:20, fechando o cluster matinal de checks/alarmes — nasceu pedindo 10:10, mesmo slot que `Diaria-Backlog-Reconcile` (#6198) escolheu no mesmo dia em worktree isolado; realocada no merge da rodada overnight 260826) — `scripts/dmarc-drain.ts` busca no Gmail (mesmo conector OAuth/`gFetch` de `inbox-drain.ts`) os anexos de relatório agregado DMARC (`rua=mailto:pixel@diar.ia.br`, `_dmarc.news.diar.ia.br` — ver estado do DNS medido 26/08 na issue), desempacota base64→ZIP/gzip→XML (`scripts/lib/dmarc-report.ts`, sem dependência externa de ZIP — parser à mão pro caso de 1-entry-por-relatório) e agrega por domínio: volume total, % SPF/DKIM brutos, % ALINHADO (o que decide se o DMARC passa — nunca confundir com "passou SPF bruto") e os IPs de origem que falharam alinhamento. Resumo salvo em `data/dmarc-reports/{YYYYMMDD}-summary.json` (histórico append-only, reconstitui a janela de 2-4 semanas que a decisão de enforcement da #6111 precisa). **Por que esta task existe separada da #6111 (gated até 2026-09-15):** a #6111 só espera o RESULTADO acumulado — subir `p=none` pra `quarantine`/`reject`; este script é o que PRODUZ esse dado, e sem ele o `rua=` configurado é indistinguível de não ter `rua=` nenhum. Alarme: se qualquer domínio tiver volume NÃO-alinhado > 0, cria/reusa issue via `scripts/lib/alarm-issues.ts` (mesmo padrão dos demais alarmes do repo, `family: "estado"`, auto-fecha após 2 execuções limpas) + e-mail best-effort ao editor. **Escopo da BUSCA ampliado pro apex (rodada overnight 260826):** a query original só aceitava assunto de `news.diar.ia.br` e procurava o remetente `dmarc-noreply@google.com` — endereço errado; o real é `noreply-dmarc-support@google.com`. Com os dois defeitos, a busca devolvia 0 relatórios contra a caixa real. Corrigidos e validados ao vivo (`--dry-run`): 1 relatório parseado, `diar.ia.br`, volume 249, SPF 99,6%, DKIM 100%, alinhado 100%. **Achado que a correção revelou (#6229, P1):** de `news.diar.ia.br` não chega relatório NENHUM — 0 em 120 dias, medido por busca direta na caixa —, enquanto o do apex chega normalmente, o que prova que o roteamento da caixa funciona. Ou seja, o prazo `aguardando-ate: 2026-09-15` da #6111 está contando sobre dado que não existe. **`reativa.` segue fora** — `_dmarc.reativa.diar.ia.br` aponta `rua=` pra `rua@dmarc.brevo.com` (relatórios desse domínio vão pra Brevo, não pra nós); entrar com `reativa.` no agregador exigiria primeiro incluir nosso endereço no `rua=` dele, decisão fora do escopo desta unidade. Busca no Gmail é I/O real (não testável offline sem credencial) — miolo puro (parser + agregador) tem cobertura completa em `test/dmarc-report.test.ts`/`test/dmarc-drain.test.ts` contra fixtures XML reais + ZIP/gzip construídos em memória. **ARMADA em `helios` (28/08/2026, rodada overnight 260827b, issue #6189 fechada)** — units `diaria-dmarc-drain.{service,timer}` presentes em `~/.config/systemd/user/`, timer `enabled`+`active`, carimbo pré-tocado pra evitar catch-up-fire (#5140); próxima execução real 10:25 BRT. Sem doc de setup dedicado — ver docstring de `scripts/dmarc-drain.ts`.

**Alarme sem política de drift de versão em sessão Claude Code de vida longa (#6927, 01/09/2026), ARMADA em 01/09/2026.** Task `Diaria-Claude-Session-Version-Drift-Alarm`, `interval` a cada 6h — `scripts/claude-session-version-drift-alarm.ts` (lógica pura em `scripts/lib/claude-session-version-drift-alarm.ts`) fecha a Parte C do #6875/#6891: opção 3 da issue ("alarme, sem política") pra sessão `claude ... --remote-control` de vida longa cujo binário em memória diverge do que já está em disco — o mecanismo que realimentava reinstalações em ciclo (~30min, ~214MB/ciclo, medido ao vivo com sessões de 31h/36h, 4 quebras de cron em ~15h antes do fix mecânico do #6891 Partes A/B, que já fechou o dano operacional). Detecção: `readlink /proc/<pid>/exe` de processo com mais de N horas (default 24h) contendo o marcador `(deleted)` — o kernel mantém o link apontando pro staging temporário do npm que o disco já removeu numa reinstalação, sinal mais confiável que `claude --version` (que sempre lê o disco, nunca o que o processo tem carregado). Só Linux (`process.platform !== "linux"` sai 0 sem checar nada — achado é específico do `helios`). Idempotência por fingerprint do CONJUNTO de pids pendentes em `data/claude-session-version-drift-alarm/state.json` — mesmo conjunto não repete e-mail, pid novo em drift ou conjunto mudando dispara de novo. **Sem issue automática** (diferente de `Diaria-Node-Modules-Loop-Alarm`) — decisão explícita: o achado é recorrente por natureza (reaparece toda vez que uma sessão fica velha o bastante), issue reaberta a cada ciclo seria ruído, não sinal; só e-mail via Gmail. `ps` falhando PROPAGA o erro (não vira "zero sessões encontradas") — achado de review #6953: numa run Linux saudável `ps` sempre existe, uma falha ali é anomalia real do host, e tratar como lista vazia resetaria o fingerprint de idempotência e causaria realarme duplicado na run seguinte. **ARMADA e validada ao vivo em `helios` (01/09/2026, 20:07Z)** — `systemctl --user is-enabled` = `enabled`, primeiro disparo agendado 01/09 18:00 BRT, e um `start` manual da `.service` fechou `status=0/SUCCESS` com a checagem completa no `.alarm.log`. Nenhum e-mail de catch-up saiu no arme: o `Persistent=true` não tinha ocorrência devida (unit nova, sem carimbo) e, mesmo se tivesse, não havia achado pendente. Setup/uso: `docs/claude-session-version-drift-alarm-setup.md`.

**Duas limitações medidas NO arme, que o alarme não cobre — nenhuma é motivo pra desarmar, mas nenhuma deve ser esquecida (#6927):**

1. **Ele não mede defasagem, e é defasagem que passou a importar.** Com a decisão do editor de 01/09 (`DISABLE_AUTOUPDATER=1` no ambiente da máquina), o papel esperado do alarme inverteu — de detectar a quebra pra medir há quantos dias a máquina não atualiza. O sinal deste script é `/proc/<pid>/exe` terminando em `(deleted)`, ou seja **uma reinstalação ACONTECEU**; com o updater desligado essa reinstalação deixa de acontecer e o alarme fica quieto por construção. A leitura invertida exige uma comparação diferente — versão em DISCO contra a do UPSTREAM npm, por N dias —, que é o item 2 em aberto no comentário do editor na #6927 e é trabalho separado. Este alarme armado não é o contrapeso do "desligado virar esquecido"; ainda falta escrevê-lo.
2. **O limiar de 24h deixa passar a quebra que de fato aconteceu hoje.** Às 20:05Z de 01/09 o binário quebrou pela 5ª vez no dia e as 5 sessões `--remote-control` vivas tinham `(deleted)` no `exe` — e o `--dry-run` do alarme, rodado no mesmo minuto, reportou `nenhum e-mail necessário`, porque todas estavam abaixo do limiar (duas com 0,1h de vida, três com 7,0h). O limiar de 24h veio da hipótese do LOOP do #6875 (sessões de 31h/36h realimentando reinstalações), e é adequado pra ela; o modo de falha que mordeu hoje é a **corrida do install**, que atinge sessão de qualquer idade — inclusive uma de 3 minutos, que foi o caso medido. Baixar o limiar sem mais nada trocaria o silêncio por ruído (toda sessão nova durante uma reinstalação legítima ficaria drifted por segundos); o que separa os dois casos é a idade da sessão contra o instante da reinstalação, não a idade sozinha. **Rastreado no comentário de arme da #6927** (mesmo lugar do item 1) — nenhuma das duas limitações tem issue própria, de propósito: as duas dependem da decisão sobre o auto-updater seguir de pé, e é ali que ela vive.

**Defasagem versão-em-disco × npm upstream (#6960, 02/09/2026) — o contrapeso que o item 1 acima aponta como faltante.** Task `Diaria-Npm-Version-Drift-Alarm`, diária às 10:40 BRT — `scripts/npm-version-drift-alarm.ts` (lógica pura em `scripts/lib/npm-version-drift-alarm.ts`) compara `npm root -g`/@anthropic-ai/claude-code/package.json` (disco) contra `npm view @anthropic-ai/claude-code version` (upstream) e alarma quando a divergência persiste por ≥N dias (default 7 — o Claude Code publica quase todo dia, então 1-2 dias de defasagem é cadência normal de release, não sinal). Mede algo DIFERENTE do `Diaria-Claude-Session-Version-Drift-Alarm` (#6927, acima): aquele detecta reinstalação RECENTE (`/proc/<pid>/exe` apontando pra staging removido) e fica mudo por construção quando o updater está desligado (reinstalações param, o marcador nunca aparece); este mede a defasagem ACUMULADA entre disco e upstream, que continua existindo mesmo sem nenhuma reinstalação acontecer — é o sinal que sobra quando o outro silencia. Cursor `driftSince` persistido em `data/npm-version-drift-alarm/state.json` — mantém a data do 1º dia em que a divergência apareceu (não reinicia a cada execução), reseta pra `null` assim que disco volta a bater com upstream (atualização manual). Idempotência por fingerprint do par `disco->upstream` — mesmo par não repete e-mail, upstream avançando de novo dispara outro. **Restrição de desenho central da issue: nunca falhar em silêncio** — `npm root -g`, a leitura do `package.json`, e `npm view` PROPAGAM qualquer erro (rede fora, path mudou, JSON corrompido/sem campo `version`) em vez de virar "sem defasagem" por omissão; `main()` sai com `exitCode = 1` sem persistir estado, mesma disciplina do `ps` propagando em `claude-session-version-drift-alarm.ts` (#6953). Sem issue automática (mesma decisão do alarme irmão) — só e-mail via Gmail. **DECLARADA, NÃO ARMADA nesta unidade** (worktree isolado) — armar via `scripts/setup-systemd-timers.ts` na checkout compartilhada (`helios`) é ação POSTERIOR do editor.

**Alarme diário de modelo pago não pedido faturado no gateway OpenRouter (#6985, wiring do #6716 escopo 3/#6983).** Task `Diaria-Openrouter-Billing-Leak-Alarm`, diária às 21:45 BRT (00:45 UTC — depois da virada de dia UTC que o `/api/v1/activity` fecha, com ~45min de folga sobre o mínimo de ~30min que a doc da OpenRouter recomenda; ver docblock de `scripts/lib/openrouter-billing-leak.ts` pro porquê deste guard ser estruturalmente D-1). Roda `scripts/openrouter-billing-leak-check.ts` (guard já implementado e testado no #6983 — só o wiring faltava, achado do code-reviewer daquela PR: zero ocorrências em cron/registro). Requer `OPENROUTER_MANAGEMENT_KEY` (management key, não key de inferência comum — devolve 403 caso contrário); sem ela o script sai indeterminado (exit 1), nunca "limpo". `successExitCodes: [3]` — exit 3 (`LEAK_FOUND_EXIT_CODE`) é "achou vazamento E JÁ ALARMOU por e-mail", trabalho concluído, não falha da unit. **Exit 1 (indeterminado — sem key, HTTP não-ok, janela vazia, leitura parcial) fica DE FORA de propósito**: é falha real e deve continuar reprovando a unit / contando pra `Diaria-Systemd-Unit-Rate-Alarm`, senão o silêncio do #6716 original (US$ 2,55 sem alarme em 29-31/08) reaparece pela porta do runner em vez da porta do cron ausente. **DECLARADA, NÃO ARMADA nesta unidade** (worktree isolado, mesma disciplina do #4750/#4910/#5311/#6189 acima) — armar requer, na `helios`: (1) confirmar `OPENROUTER_MANAGEMENT_KEY` presente no `.env` da máquina — **cuidado com a armadilha de parsing medida ao vivo em 01/09/2026**: `sync-env` grava o valor ENTRE ASPAS, e um `grep KEY .env | cut -d= -f2` ingênuo repassa as aspas, produzindo `401 Missing Authentication header` que PARECE key inválida e é bug de parsing — o script em si já escapa disso (lê via `loadProjectEnv`), a armadilha é só pra quem for inspecionar a env manualmente; (2) `npx tsx scripts/setup-systemd-timers.ts --task Diaria-Openrouter-Billing-Leak-Alarm`; (3) copiar `.service`/`.timer` gerados pra `~/.config/systemd/user/`; (4) `systemctl --user daemon-reload && systemctl --user enable --now diaria-openrouter-billing-leak-alarm.timer`. Sem doc de setup dedicado — ver docstring de `scripts/openrouter-billing-leak-check.ts`.

**GC diário de branches locais + worktrees encerrados (#6802).** Task `Diaria-Branch-Cleanup`, diária às 10:45 BRT — cauda do cluster matinal de checks/alarmes (09:00-10:40), logo após `Diaria-Npm-Version-Drift-Alarm` (10:40) e antes do próximo horário ocupado (`Diaria-Postmaster-Spam-Sync`, 12:30); fora das janelas quentes (envio `Diaria-Clarice-Envio` 19:00, janela do `Diaria-Overnight-Watchdog` 18:00→09:00, e a extinta edição de madrugada). Roda `scripts/branch-cleanup.ts --push` — o critério de segurança (estado da PR via `gh`, não `merge-base --is-ancestor` sozinho, que classifica errado neste repo por causa do squash-merge) e o guard de worktree travado/sujo já vêm do script (#6852, ver docstring de `scripts/branch-cleanup.ts`); `--push` é necessário porque dry-run (só relatório, nada apagado) é o default do próprio script — sem a flag a task não teria efeito nenhum. **Correção (#7044, P0 do review da PR #7044): a afirmação original desta entrada, de que "nenhuma flag de segurança adicional foi necessária", estava ERRADA** — o script rodava numa cronjob diária desassistida (checkout compartilhado por overnight/develop/contínuo simultâneos) sem NUNCA consultar `session-registry.ts`, apesar do script irmão `scripts/cleanup-merged-worktrees.ts` já resolver exatamente esse problema desde o #5156 item 9; o guard `locked` sozinho não cobria isso porque nenhum call site deste repo chama `git worktree lock`. O #7044 portou o guard (`shouldSkipForSharedSession`/`activeSessionWorktreePaths`, generalizado em `scripts/lib/shared-session-guard.ts`, reusado pelos dois scripts) — `--push` agora pula a varredura inteira quando há sessão coordenadora ativa não-stale (salvo `--confirm-shared`), e nunca remove um worktree registrado por qualquer sessão viva mesmo com árvore limpa. Também corrigido no mesmo PR: falha real de `git worktree remove`/`git branch -D` agora seta `process.exitCode = 1` (antes só logava e a unit sempre saía 0, deixando `Diaria-Systemd-Failed-Units-Alarm` mudo). **Task NOVA e ISOLADA de propósito, deliberadamente NÃO pendurada na `Diaria-Session-Registry-Gc`** (decisão do editor registrada no #6802: falha de um GC não pode contaminar o outro). Motivação/urgência: medição ao vivo de 01/09/2026 — 745 branches locais viraram 32 após limpeza manual em 31/08, e voltaram a **1.424 branches + 47 worktrees em ~36h**, confirmando que sem GC recorrente o número cresce de novo em dias, não semanas. **DECLARADA, NÃO ARMADA nesta unidade** (worktree isolado, mesma disciplina do #4750/#4910/#5311/#6189/#6985 acima) — armar requer, na `helios`, após o merge: `npx tsx scripts/setup-systemd-timers.ts --task Diaria-Branch-Cleanup` + `arm-systemd-timers.ts --task Diaria-Branch-Cleanup` (ou o par manual `.service`/`.timer` + `daemon-reload` + `enable --now`, mesmo padrão das entradas acima). Sem doc de setup dedicado — ver docstring de `scripts/branch-cleanup.ts`.

**Ingestão diária GA4 — topo de funil (#7184, fatia 12 do épico #7172).** Task `Diaria-GA4-Sync`, diária às 10:50 BRT — livre entre `Diaria-Npm-Version-Drift-Alarm`/`Diaria-Branch-Cleanup` (10:40/10:45) e o próximo horário ocupado (`Diaria-Postmaster-Spam-Sync`, 12:30). Roda `scripts/ga4-sync.ts --days 30`, que agora puxa 4 relatórios (antes 2): `overview` e `top-pages` (herdados do #5248) mais `channel` (fino — `date`/`sessionSource`/`sessionMedium`/`sessionCampaignName`/`hostName`, o topo de funil que estava sendo descartado na coleta) e `channel-group` (coluna de conferência via `sessionDefaultChannelGroup`, nunca a fonte da classificação). A allowlist de `hostName` que restringe o denominador só ao que serve o cadastro da diária (`diar.ia.br` + `diaria.beehiiv.com`, excluindo os demais Workers do Vigil.ia.br na mesma propriedade GA4 516813959) é aplicada NA LEITURA por `scripts/lib/metrics/ga4-channel.ts`, não nesta task de coleta. Sem `guard` — `ga4-sync.ts` escreve snapshot novo (`{AAAA-MM-DD}.json` + `latest.json`), nunca sobrescreve store acumulado. Sem `successExitCodes` de propósito: o script sai 2 quando falta `GA4_PROPERTY_ID`/a auth falha, e 1 em falha de ingestão — credencial faltando é falha real, não resultado ambíguo normal, e deve aparecer em `systemctl --user list-units --state=failed` para `Diaria-Systemd-Failed-Units-Alarm` avisar. **DECLARADA, NÃO ARMADA nesta unidade** (worktree isolado, mesma disciplina das entradas acima) — armar requer, na `helios`, após o merge: `npx tsx scripts/setup-systemd-timers.ts --task Diaria-GA4-Sync` + confirmar em `systemctl --user list-timers`. Sem doc de setup dedicado — ver docstring de `scripts/ga4-sync.ts` e `docs/ga4-data-api-setup.md`.

**`Diaria-Clarice-Envio` moveu de 19:00 pra 19:10 BRT (#5826, 02/09/2026) — colisão de minuto com task `interval`.** `Diaria-Clarice-Dashboard-Precompute` (`interval hours:1`, acima) dispara SEMPRE no minuto `:00` de toda hora por construção (`scheduleToOnCalendar` traduz qualquer `interval` pra `OnCalendar=*-*-* 0/N:00:00`) — com `Diaria-Clarice-Envio` também em `:00`, as duas batiam no MESMO minuto todo dia (confirmado ao vivo: journal mostra as duas arrancando `19:00:08` em 01/09/2026), contribuindo pra estourar o teto de 100 req/hora POR CONTA da Brevo (`docs/brevo-rate-limits.md`) — a mesma causa raiz por trás de #6458/#6831/#7007. `:10` elimina a colisão determinística com qualquer task `interval` do registro sem remanejar mais nada (nenhuma outra `daily` ocupava a hora 19). Comportamento/guard/`successExitCodes: [4]` inalterados — só o minuto mudou. `Diaria-Clarice-Envio-Alarm` (irmão, abaixo) também moveu, por consequência do #6831.

**`Diaria-Clarice-Envio-Alarm` moveu de 20:30 pra 22:45 BRT (#6831, 02/09/2026) — orçamento de retry do irmão cresceu.** `TRANSIENT_RETRY_CAP_MS` (`clarice-envio-run.ts`) subiu de 35min pra 70min por tentativa (cobre `Retry-After` reais observados até 3570s/~59,5min contra o rate limit HORÁRIO da Brevo — 35min nunca cobriu isso, só coincidia por sorte com os 2 valores pequenos medidos até 05/08). Pior caso de retry só do sinal 429/503 do dashboard: 2 esperas × 70min ≈ 2h20; somado à retentativa NOVA e dedicada da consulta de campanhas comprometidas (`retryProposalOnCommittedRateLimit`, mesmo orçamento) o pior caso plausível cresce mais — 22:45 (19:10 + 3h35) dá folga real antes de alarmar, evitando que o alarme dispare em cima de um retry legítimo ainda em curso (o que a checagem antiga, às 20:30, faria quase toda vez que o orçamento maior fosse de fato usado). `GUARD_TRANSIENT_RETRY_BUDGET` (`clarice-envio-guard.ts`, task `Diaria-Clarice-Envio-Guard` acima, 05:00 BRT) **NÃO mudou** — permanece 120s por desenho já revisado no #6221 (janela de ~1h antes do disparo das 06:00; esperar mais não muda a chance real da janela horária ter virado) com fallback seguro (`handlePrereqFailure`, lê o último freio conhecido) intacto; a nova retentativa do #6831 vive inteiramente em `clarice-envio-run.ts` e nunca faz `clarice-plan-wave.ts` lançar, então o guard nunca vê esse sinal — os dois orçamentos protegem janelas de tempo genuinamente diferentes, não é a mesma decisão desalinhada.

**Estende ao grupo `engajados` (retenção) a orquestração automática que o `ramp-warm` já tem (#6945, 02/09/2026), DECLARADAS mas NÃO ARMADAS.** Tasks `Diaria-Clarice-Envio-Engajados` (diária, 20:15 BRT — depois de `Diaria-Clarice-Envio`, 19:10, cujo assunto do dia já travado esta task REUSA em vez de rodar teste A/B/C próprio) e `Diaria-Clarice-Envio-Engajados-Alarm` (diária, 21:15 BRT — 1h depois, sem orçamento de retry próprio pra esperar). A SELEÇÃO do grupo `engajados` (`send_eligible=1 AND sends_count>0 AND priority_points>0`, ordenado por `priority_points DESC`, exclui quem já recebeu neste MÊS de envio) já estava implementada e correta em `clarice-build-segment.ts --group engajados` — o que faltava era o GATILHO: nenhuma task automatizada chamava esse grupo, e o último envio foi manual em 06/08/2026 (817 contatos represados viraram 37,9k em 26 dias). `scripts/clarice-envio-engajados-run.ts` é DELIBERADAMENTE mais simples que o irmão `clarice-envio-run.ts` — sem freio de risco de ISP replicado (já hardcoded "ok" no motor do ramp-warm desde #6793/#6888), sem teste A/B/C próprio (pula o dia com motivo claro se o assunto ainda não estiver travado), sem estado de frescor `novos`. Volume: `scripts/lib/clarice-envio-engajados-policy.ts` reusa a MESMA taxa de crescimento do ramp-warm (10%/dia, `ENGAJADOS_DAILY_GROWTH_STEP`), bootstrap conservador (`ENGAJADOS_BOOTSTRAP_VOLUME = 1500`, automação nunca rodou desassistida) e um TETO ABSOLUTO (`ENGAJADOS_MAX_DAILY_VOLUME = 8000`) que substitui o gate humano de "volume inesperado" que a automação remove (`clarice-build-segment.ts:405-420` nomeia esse gate). Estado de escalada em `data/clarice-subscribers/engajados-state.json` (`clarice-envio-engajados-state.ts`) — só avança em agendamento CONFIRMADO; um dia pulado (kill switch, ciclo não pronto, assunto ainda não travado, lock detido pelo ramp-warm no mesmo ciclo) não perde progresso, só adia. Lock COMPARTILHADO com o ramp-warm por design (`acquireEnvioLock` chaveia por CICLO, não por grupo — as duas automações escrevem no mesmo `sent-or-queued.json` cycle-wide, #4765) — `LockHeldError` vira exit 4 (mesmo código do ramp-warm), nunca falha genuína. **Kill switch (`clarice-envio-engajados-enabled.ts`) nasce DESLIGADO** — ao contrário do `clarice-envio-enabled.ts` do ramp-warm (que nasceu ligado por substituir trabalho manual já diário), esta é automação NOVA que dispara e-mail real pra até 8.000 contatos/dia sem gate humano no caminho normal; o editor liga explicitamente (`npx tsx scripts/lib/clarice-envio-engajados-enabled.ts --set enabled`) depois de revisar a 1ª rodada (`--dry-run` primeiro). O trade-off editorial (quem recebe, em que ordem, cadência) já foi decidido pelo editor em 01/09/2026 ("sempre pra quem não recebeu ainda aquele mês, score mais alto pro mais baixo") — não redecidido aqui, só implementado. **DECLARADAS, NÃO ARMADAS nesta unidade** (mesma disciplina do #4750/#4910/#5311/#6189/#6985/#6802 acima) — armar requer, na `helios`, após o merge E o editor revisar a 1ª rodada manual/dry-run e ligar o kill switch: `npx tsx scripts/clarice-envio-engajados-run.ts --dry-run` (revisar) → `npx tsx scripts/lib/clarice-envio-engajados-enabled.ts --set enabled` → `npx tsx scripts/setup-systemd-timers.ts --task Diaria-Clarice-Envio-Engajados` (+ `--task Diaria-Clarice-Envio-Engajados-Alarm`) → `arm-systemd-timers.ts` pros dois. Sem doc de setup dedicado — ver docstring de `scripts/clarice-envio-engajados-run.ts`.

**Alarme semanal de marcador de roteamento desatualizado — mesmo defeito de fundo por trás de duas issues (#7270, #7288, 03/09/2026), DECLARADA, NÃO ARMADA.** Task `Diaria-Route-Marker-Staleness-Alarm`, domingos 11:15 BRT — logo após `Diaria-On-Hold-Vencimento-Alarm` (11:00), mesma família de alarme "rótulo que ninguém reavalia vira mentira durável" (#5317). Roda `scripts/route-marker-staleness-alarm.ts` sobre `scripts/lib/route-marker-staleness.ts` (lógica pura), varrendo TODAS as issues abertas e reportando 5 categorias determinísticas: **(1) `bloqueada-sem-marcador`** — label de bloqueio sem `ExecutionBlock` válido (#7270 mediu 9 de 12 issues bloqueadas assim; desde que `scripts/route-issue.ts --track bloqueada` passou a embutir o marcador automaticamente no mesmo comentário do roteamento, este achado só deveria aparecer pra bloqueio aplicado por `gh issue edit` direto, fora do verbo); **(2) `bloqueada-depends-on-fechada`** — `condicao.tipo: "depends_on"` cuja issue-alvo já fechou (backstop — `reconcile-issue-dependencies.ts` já deveria ter removido a label sozinho); **(3) `bloqueada-externa-sem-atualizacao`** — `condicao.tipo: "externo"` (o único tipo sem auto-desarme, por natureza) sem atualização há 30+ dias, o ÚNICO achado desta lista que de fato exige revisão HUMANA; **(4) `agendada-motivo-cita-issue-fechada`** — a razão do `route-issue --track agendada` mais recente cita `#N` já `CLOSED` (#7288 mediu 3 dos 11 casos de "agendada" que eram estacionamento disfarçado); **(5) `agendada-renovada-multiplas-vezes`** — 3+ comentários `route-issue` com `track=agendada` na mesma issue (deferimento repetido). O alarme **reporta, não desbloqueia/reroteia** — decisão explícita do #7270 ("remover label é decisão de quem tem contexto"), mesmo digest sem estado/idempotência persistente de `on-hold-vencimento-alarm.ts` (reenvia todo achado pendente a cada rodada). A escrita que alimenta este alarme mudou junto: `formatExecutionBlockMarker` (`scripts/lib/issue-decisions.ts`) ganhou campo `condicao` OBRIGATÓRIO (union `depends_on`/`externo` — deliberadamente SEM uma 3ª variante `aguardando_ate`, porque bloqueio com data conhecida é `--track agendada`, não `bloqueada` com data embutida, ver docstring do módulo), e `route-issue.ts` passou a exigir `--reason` não-vazio pra `--track bloqueada`/`--track agendada`, recusar razão de `--track agendada` que descreve dependência/gatilho-condicional/escopo (não uma data — 3 dos 4 padrões medidos na auditoria do #7288, `scripts/lib/route-reason-guard.ts`, escape hatch `--force`), e embutir o `ExecutionBlock` automaticamente no comentário de `--track bloqueada` (`--depends-on N` grava `condicao: depends_on` + label `dependencia-aberta`, exige que o marcador `depends-on:` #7137 já esteja no corpo; sem `--depends-on`, condição `externo` com a descrição = `--reason`). `guard`: nenhum modelado — `listOpenIssuesForStaleness` já é fail-soft (falha do `gh` devolve `null`, o script aborta sem alarmar). **DECLARADA, NÃO ARMADA nesta unidade** (worktree isolado, mesma disciplina do resto do registro acima) — armar requer, na `helios`, após o merge: `npx tsx scripts/setup-systemd-timers.ts --task Diaria-Route-Marker-Staleness-Alarm` + `arm-systemd-timers.ts --task Diaria-Route-Marker-Staleness-Alarm`. Sem doc de setup dedicado — ver docstring de `scripts/lib/route-marker-staleness.ts`.

**Captura diária do roster completo do Kit — dimensão `subscription` do store do #6464, populada pela 1ª vez (#7174, F2 do épico #7172, 02/09/2026).** Task `Diaria-Kit-Roster-Ingest`, diária às 04:25 BRT — depois da fronteira do dia (00:00 BRT) e antes do cluster matinal (`Diaria-Clarice-Envio-Guard`, 05:00), fora do bloco de domingo (`Diaria-Beehiiv-Backup` 03:00, `Diaria-Acquisition-Health-Alarm` 03:30, `Diaria-Beehiiv-Backup-Staleness-Alarm` 04:00, `Diaria-SEO-Weekly` 04:10). Roda `scripts/diaria-subscribers-ingest-kit.ts --write`, que agora tem 2 passos: **Passo 1 (novo)** lista `/v4/subscribers` inteiro com `status: "all"` (nunca omitir — a API devolve só `active` em silêncio sem essa opção) e grava 1 `subscriber` + 1 `subscription` + eventos `subscribe`/`unsub` por assinante via `ingestKitRoster` (`scripts/lib/kit-subscribers-ingest.ts`) — fecha o buraco medido na issue: `subscription` tinha UM único caller no repo inteiro antes desta fatia (`brevo-subscribers-ingest.ts`), então nenhuma leitura de classe de aquisição (#7173) tinha de onde tirar a dimensão de assinatura do Kit. **Passo 2 (pré-existente, #6586)** segue ingerindo audiência por broadcast, inalterado. UTM/atribuição vem de `fields` (custom fields), nunca do bloco `attribution` (que vem sempre mas com UTM nulo pra quem foi criado via API — achado #7174, corrigido na docstring de `scripts/lib/kit-subscribers.ts`). `--write` é exigido explicitamente — execução manual sem a flag é dry-run (lista o roster, não grava nada); só esta task agendada passa `--write` por padrão, escritor único da dimensão `subscription` (guard de cópia de conflito do OneDrive em `data/metrics/captura-log.jsonl` roda dentro do próprio script antes de qualquer escrita). Log de execução em `data/metrics/captura-log.jsonl` (1 linha por RODADA, nunca sobrescrita — `hasCaptureOnDay`/`scripts/lib/metrics/captura-log.ts`), o que separa "dia com 0 cadastros novos" de "dia sem coleta" pra F5 (metas/streak) do mesmo épico. `guard.requiredFile: diaria-subscribers/diaria-subscribers.db` — mesmo sinal de "junction `data/` ainda não montada" das tasks irmãs que dependem do store do #6464. **DECLARADA, NÃO ARMADA nesta unidade** (worktree isolado, mesma disciplina do #4750/#4910/#5311/#6189/#6985/#6802/#6945 acima) — armar requer, na `helios`, após o merge: `npx tsx scripts/setup-systemd-timers.ts --task Diaria-Kit-Roster-Ingest` + `arm-systemd-timers.ts --task Diaria-Kit-Roster-Ingest`. Sem doc de setup dedicado — ver docstring de `scripts/diaria-subscribers-ingest-kit.ts` e `scripts/lib/kit-subscribers-ingest.ts::ingestKitRoster`.

**Alarme de saúde do registry de métricas de negócio (#7172, F8) — UM alarme, N sinais, DECLARADA, NÃO ARMADA.** Task `Diaria-Metrics-Health-Alarm`, diária 04:40 BRT — depois de `Diaria-Kit-Roster-Ingest` (04:25, acima), cuja captura do dia (`data/metrics/captura-log.jsonl`, F2) este alarme lê para o sinal de frescor. Roda `scripts/check-metrics-health.ts` sobre `scripts/lib/metrics/health.ts` (lógica pura), avaliando as métricas do registry (F3/F5) contra 5 sinais: **frescor** (o principal — nenhum alarme cobria isso antes, #7180), **queda**, **meta não-atingida**, **indeterminado alto** e **registry mudo**. `guard`: nenhum modelado — degrada por camada quando faltam insumos locais (store/captura-log/metas ausentes), nunca lança. **DECLARADA, NÃO ARMADA nesta unidade** (worktree isolado, mesma disciplina do resto do registro acima) — armar requer, na `helios`, após o merge: `npx tsx scripts/setup-systemd-timers.ts` + `systemctl --user enable --now diaria-metrics-health-alarm.timer`. Sem doc de setup dedicado — ver docstring de `scripts/lib/metrics/health.ts`.

**Guard "quem recebe × quem recebe" nas 3 plataformas de envio da diária (#7385, 03/09/2026) — DECLARADA, NÃO ARMADA.** Task `Diaria-Reconcile-Send-Audiences`, diária 04:45 BRT — depois de `Diaria-Metrics-Health-Alarm` (04:40, acima) e antes do cluster `Diaria-Clarice-Envio-Guard` (05:00). Corrige `scripts/reconcile-beehiiv-kit.ts` (#6269), que comparava BASE DE ATIVOS (Kit inteiro × Beehiiv ativos) — comparação enganosa que escondeu 7 dias de gap na migração (achado #7357: Kit tinha 629 ativos mas só 280 recebiam de fato, via a tag `rampa-kit`; comparar contra os 317 ativos da Beehiiv sugeria migração concluída, quando a comparação real — 280×314 — mostrava os 349 órfãos). `scripts/reconcile-send-audiences.ts` mede **audiência de ENVIO**, não presença na base, nas 3 plataformas: Kit = membros da tag `kit_diaria.audience_tag` (default `rampa-kit`, via `listAllTagSubscriberEmails`); Beehiiv = assinantes `active` (ainda é audiência de envio real do post principal); Brevo = contatos da lista `brevo_diaria.list_id` (via `brevoListContacts`, novo em `scripts/lib/brevo-client.ts`). Miolo puro em `scripts/lib/beehiiv-kit-reconcile.ts`: `reconcileSendAudiences` (sobreposição entre as 3 audiências — hoje deveria ser sempre 0, disjuntas por construção) e `findOrphans` (ativo em alguma plataforma, fora de toda audiência de envio — o achado #7357 vira guard mecânico). Também mede, informativo, os destinatários reais do último envio de cada plataforma, com as 2 armadilhas de medição do corpo da issue embutidas: `checkBeehiivDeliveryGap` tolera o gap constante da Beehiiv (medido em ~3 abaixo do total ativo nos últimos 4 envios: 485/488, 463/466, 415/418, 314/317 — piso de tolerância `max(6, 1% dos ativos)`, nunca alarma por esse padrão normal); `resolveBrevoCampaignRecipients`/`looksLikeMissingGlobalStatsParam` detectam quando `GET /v3/emailCampaigns/{id}` voltou sem `?statistics=globalStats` (já documentado em `workers/brevo-dashboard/src/brevo-api.ts:2298-2302`) e recusam ler `sent` como zero real nesse caso — `brevoGetCampaignGlobalStats` (novo em `brevo-client.ts`) evita a armadilha mecanicamente, hardcoding o parâmetro na URL em vez de depender do caller lembrar. Exit codes: 0 = sem órfão/sobreposição; 1 = DIVERGE (bloqueante); 2 = falha de config/rede (não foi possível medir a audiência). `scripts/reconcile-beehiiv-kit.ts` (#6269) continua intocado — segue sendo o precondition do switchover #6114 ("só na Beehiiv: 0" contra bases de ativos), pergunta DIFERENTE da deste guard. **DECLARADA, NÃO ARMADA nesta unidade** (worktree isolado, mesma disciplina do resto do registro acima) — armar requer, na `helios`, após o merge: `npx tsx scripts/setup-systemd-timers.ts --task Diaria-Reconcile-Send-Audiences` + `systemctl --user enable --now diaria-reconcile-send-audiences.timer`. Sem doc de setup dedicado — ver docstring de `scripts/reconcile-send-audiences.ts`.
