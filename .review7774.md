Review automatizado (1 agente, effort low): guard #7763 nunca é chamado por nenhum caminho real de `npm ci` — fica órfão, só exercitado pelo próprio teste.

**Achado 1 — confiança alta, P1 (correctness/completude):** `scripts/lib/worktree-node-modules-guard.ts` exporta `checkNodeModulesSymlink`/`guardBeforeNpmInstall`, mas nenhum script, hook ou skill deste repo importa/chama essas funções (`grep -rn "worktree-node-modules-guard|guardBeforeNpmInstall|checkNodeModulesSymlink"` só acha o próprio arquivo e seu teste). Todos os call sites de `npm ci` no repo são prosa dentro de SKILL.md (diaria-overnight, diaria-develop, diaria-continuo) — lidos por um agente, não executados por script. Ou seja, o "Guard mecânico" citado em `context/overnight-dispatch-rules.md` não guarda nada em runtime: se um agente rodar `npm ci` direto via Bash (como o incidente #7763 descreve), a função nunca é invocada e o `node_modules` do principal pode ser esvaziado de novo. O PR resolve a documentação/prosa e adiciona testes unitários da função isolada, mas não fecha o buraco mecânico que o título promete.
  - Sugestão: ou (a) wrap `npm ci` num script TS (`scripts/safe-npm-ci.ts`) que chama `guardBeforeNpmInstall` antes de invocar o processo, e trocar as instruções de SKILL.md para usar esse wrapper em vez de `npm ci` cru; ou (b) adicionar um hook Bash pre-tool-use que intercepta comandos `npm ci`/`npm install` e roda o guard antes.

**Achado 2 — confiança baixa, P3:** o teste 3 (`lança no guardBeforeNpmInstall...`) symlinka para `/tmp/fake-principal`, um path que não existe — o guard usa `readlinkSync` + comparação de string, então funciona independentemente de o alvo existir, mas deixa a asserção um pouco menos representativa do cenário real (alvo é sempre um checkout git existente). Não bloqueia, só nota de qualidade do teste.

Nenhum achado de bug de lógica na função de detecção em si (self-referente vs externo, ENOENT, etc. parecem corretos); o problema é de integração/wiring, não da implementação do guard.

<!-- continuo-review: run=1788945846-2707897-31916 at=2026-09-09T09:24:06Z verdict=reject head=8f26311e43366ecc2c3a1ff2f4dabe8e206c3450 -->
