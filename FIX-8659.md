# Fix #8659 — rejeição do gate de merge sem dono (regressão #7704)

## Diagnóstico (body + comentários da issue #8659)
- `watch-continuo-health.sh` detectou fila de PRs travada (3 abertas; #8641 mais velha, 13.8h, `verdict=reject`; #8644 CI falho `test`; #8651 verde).
- PR #8641 recebeu `verdict=reject` mas label `continuo-rejeitado` nunca aplicado.
- Mesma classe #7567 / #7446 item 2.

## Correção de produção (já no repo, verificado live)
`scripts/check-continuo-reject-label.ts` (linha 95): `labelApplied = alreadyRejected ? true : applyLabel(...)`
Corrige #7704 (label presente → `labelApplied=false` a cada tick).
Verificado: `npx tsx scripts/check-continuo-reject-label.ts --pr 8641` → `labelApplied=true`.

## Regressão neste PR
`test/regression-8659/reject-label-already-applied.test.ts` (node:test).

## Não tocado
watch-continuo-health.sh, publicação/agendamento/close-poll.
