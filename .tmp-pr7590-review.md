Review automatizado (1 agente, effort low): 1 achado real (meta robots noindex contradiz o propósito de página indexável), mas sem impacto ao vivo nesta PR (nada consome a key ainda) — approve.

## Achados

**1. `wrapTeaserWebShell` seta `<meta name="robots" content="noindex,nofollow">`, contradizendo o propósito declarado da própria função — P2, confiança alta**
- `scripts/lib/mensal/monthly-render.ts` linha ~1365 (dentro de `wrapTeaserWebShell`).
- O comentário do bloco (linhas 1341-1349) descreve explicitamente que esse shell é *"uma página pública indexável pelo crawler"*, e por isso declara og:title/og:description/canonical — exatamente os sinais que servem pra indexação. Mas a tag `robots` emitida é `noindex,nofollow`, que instrui qualquer crawler a NÃO indexar a página e NÃO seguir os links nela (inclusive o próprio link `canonical` e o CTA de apoio). Isso também diverge da política do projeto de manter crawlers de IA liberados nas superfícies públicas (CLAUDE.md, "Crawlers de IA ficam liberados").
- Cenário concreto: quando a Parte 2 (worker) passar a servir essa key `article:{cycle}:teaser` pro tráfego real, o Google/Bing/GPTBot vai receber og:*+canonical convidando a indexar, mas o próprio `<meta robots>` vai bloquear isso — o teaser nunca aparece em busca, indo contra o objetivo do #7580 (atrair leitor via SEO pro trecho público).
- Sem impacto AO VIVO nesta PR — o worker (`workers/artigo-mensal/src/index.ts`) não foi tocado aqui e não consome essa key ainda (confirmado via grep, zero menções a "teaser" no worker), então é dead code até a Parte 2. Mas vale corrigir antes de wire-up, senão a Parte 2 herda o bug em silêncio.

**2. Nenhum teste cobre o novo modo teaser — P2, confiança média**
- `scripts/lib/mensal/build-article-page.ts` (`buildArticleTeaser`) e a nova ramificação de `draftToEmail`/`wrapTeaserWebShell` em `monthly-render.ts` não têm teste novo — `git diff --stat` não toca nenhum arquivo em `test/`.
- O título da PR já disclosa isso ("testes pendentes"), então não é uma violação silenciosa do #633 (que é especificamente pra bugfix) — mas registrando como gap de cobertura antes do merge da Parte 2, já que a lógica de corte por bloco (`teaserCutDone`) e o novo shell HTML não têm nenhuma verificação automatizada hoje.

**3. Mensagem de log cita `--push-teaser` como se fosse uma flag própria, mas o código só reconhece `--teaser` + `--push` combinados — P3, confiança baixa**
- `scripts/build-article-page.ts`: a mensagem de dry-run diz "Use --push (artigo completo) ou --push-teaser (trecho) para gravar", mas `hasFlag(argv, "push-teaser")` não existe em lugar nenhum do código — o mecanismo real é `--teaser --push` juntos. Só confunde quem for rodar o comando a partir da mensagem impressa; não quebra nada.

<!-- continuo-review: run=1788816876-1239934-12561 at=2026-09-07T21:34:36Z verdict=approve head=087a640f9b4e1197cbdb15e7d7e63b42497f9d34 -->
