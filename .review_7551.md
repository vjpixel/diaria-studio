Review automatizado (1 agente, effort low): feature bem testada e sem bugs de correção; 2 achados de baixa severidade, nenhum bloqueante — approve.

**Nota metodológica:** o diff pedido (`4f6e99d..49725b07`) não é o diff real da PR — o branch `continuo/fix-7528-truncation-detection` bifurcou de `c6d7c818` (1 commit atrás de `4f6e99d` em master), então aquele range mostra como "removida" a doc do #7538 (Modo Desenvolvedor/symlink) que na verdade não é tocada por este PR. Revisei o diff real (`c6d7c818..49725b07`, 4 arquivos: `detect-context-truncation.py`, `.test.py`, `watch-continuo-health.sh`, `README.md`), que só adiciona o detector. Merge não corre risco de reverter a doc do #7538 (regiões disjuntas).

**Achados:**

1. **[baixa confiança, P3] Fallback do nome do modelo Ollama é hardcoded, indo contra o próprio princípio do código ("leia da config, nao hardcode").** `hermes/scripts/detect-context-truncation.py:461` — `--ollama-model` default `"qwen-64k:latest"` é usado em `_read_num_ctx_from_ollama` quando `config.yaml` não expõe `context_length`, mas o nome do modelo em si nunca é lido de `config.yaml` (`model.default`), só o `context_length` é. Se o modelo padrão mudar no futuro e o `context_length` sondado ficar ausente da config, o fallback consulta o Ollama pelo nome errado (`qwen-64k:latest` hardcoded) e recebe erro/404 → `None` → INDETERMINADO. Path só é exercido quando a fonte primária (config) já falhou, então o dano é modesto (fail-closed já cobre o caso), mas fica inconsistente com a docstring do próprio módulo.

2. **[baixa confiança, P3] Log do shell para status `edge` reaproveita a mesma linha genérica de `ok`.** `hermes/scripts/watch-continuo-health.sh` — o `case`/`if` só ramifica em `__ERR__`, `indeterminado` e `truncating`; o status `edge` (NA BORDA, que o header do arquivo descreve como "log apenas") cai no `else` e imprime `"sem truncagem ativa"`, sem listar as sessões perto do teto (diferente do bloco `truncating`, que lista `TRUNC_SESSIONS` em detalhe). Está dentro do espírito "aviso, não alarme", mas quem lesse só esse log não saberia qual sessão está na borda sem rodar o script a mão.

Nenhum dos dois achados é P0/P1 nem afeta o comportamento core (14 asserções da suíte cobrem os casos centrais: cumulativo/first_seen/ceiling-from-config/2 patamares/fail-closed/tolerância/exclusão de modelos pagos). Validação ao vivo contra o state.db real (343MB) reportada na PR corrobora a lógica.

<!-- continuo-review: run=1788735162-944933-30355 at=2026-09-06T22:52:42Z verdict=approve head=49725b07971c3e5c96aec265e0d2ae2247ffef82 -->
