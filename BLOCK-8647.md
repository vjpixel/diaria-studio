#8647 bloqueio — causa duplicação não confirmada (ver comentários #2-3 na issue)
- #8646 (dedup event_id + claim KV) já cobre reenvio.
- Remedição ads_get_dataset_stats / cruzamento KV NÃO executada.
- Fix seguro independente: eventSourceUrl opcional (instrumentação).
