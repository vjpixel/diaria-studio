# Design Reference — diar.ia.br Home Redesign (#6375)

Source: repo `vjpixel/diaria-design`, file `v1-daily.jsx` (Direção A · Edição diária).

Components to port to `workers/site/` for `/`:
- V1Nav → nav bar
- V1Masthead → hero (H1 + lede + inline form)
- V1Feature → destaque do dia
- V1Specials → livros/cursos
- V1Archive → edições anteriores
- V1Faqs → FAQs
- V1Footer → form de inscrição (tema escuro)
- EmailSignup (core) — pill wrapper + input + botão inline

Status: analysis/reference only — design system not cloned locally; requires `git clone https://github.com/vjpixel/diaria-design.git` before implementation.
