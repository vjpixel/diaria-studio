#!/bin/sh
if git diff --cached --name-only | grep -q "seed/sources.csv"; then
  echo "seed/sources.csv staged — regenerando context/sources.md..."
  npm run sync-sources
  git add context/sources.md
fi
