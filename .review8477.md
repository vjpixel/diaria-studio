Review automatizado (1 agente, effort low): 2 checks obrigatórios de CI falham por omissão real no PR (faltam `@one-off-validity` e `removal-declaration:`) — reject até corrigir.

**Achados**

1. **[P1, confiança alta] CI "One-off script validity (#7114)" falha de verdade.** `scripts/measure-title-picker-8420.ts` e `scripts/measure-social-critic-8420.ts` são scripts NOVOS na raiz de `scripts/` cujo nome bate o padrão `measure-*` (o mesmo padrão que gerou 17 órfãos em 4 meses, #7114) e nenhum dos dois declara `@one-off-validity` (expira=AAAA-MM-DD ou permanente). Confirmado por grep: nenhuma ocorrência da tag nos dois arquivos. Isso não é flake — é o guard funcionando como desenhado. Precisa de uma linha `@one-off-validity: expira=AAAA-MM-DD` (ou `permanente`, com justificativa) no header de cada script antes do merge.

2. **[P1, confiança alta] CI "PR removal declaration (#7115)" falha de verdade.** O PR soma 763 linhas adicionadas (4 arquivos novos, 0 removidos) e o body não tem a linha `removal-declaration: ...` exigida pelo guard acima do limiar — nem "o que remove" nem "por que legitimamente não remove nada". Precisa adicionar essa linha ao corpo da PR (ex.: `removal-declaration: harness de medição, sem remoção — issue #8420 pede script reutilizável, veredito já registrado como comentário na issue, não há código de produção anterior pra remover`).

3. **[P3, confiança baixa] `diff 06041b8a2..03514c2e0` mostrado no pedido de review inclui remoção espúria de `scripts/jev-eval-pool-relevance.ts`/`scripts/lib/jev-questions.ts` e seus testes.** Isso é artefato de branch desatualizada (o branch da PR bifurcou de `#8417`, antes do merge de `#8418`/`06041b8a2` em `master`) — não é uma mudança real deste PR. `git show 03514c2e0 --stat` confirma que o commit único da PR só ADICIONA 4 arquivos; `gh pr view` reporta `mergeable: MERGEABLE`, então o merge (3-way, não um `git diff` linear) não vai de fato apagar esses arquivos de `master`. Reportando por transparência, não bloqueia.

4. **[P3, confiança baixa] Lógica dos scripts em si sem bugs encontrados.** `parseDestaques`/`extractBodyForUrl`/`extractSocialParagraphs` têm cobertura de teste razoável para os casos de borda documentados (bloco já podado, placeholder não resolvido, fronteira `# Social`/`# Curto`, legendas CC BY-SA). `myPicks.length !== sample.length` e o check de que o pick está entre as opções embaralhadas (`item.shuffledOptions.includes(myPick)`) dão fail-fast correto em vez de mascarar erro de rotulagem — bom sinal contra falha silenciosa. Nenhum código de produção é tocado; consistente com o mandato do #633 de justificar `no-regression-test` quando não há como testar sem custo de API real, o que o PR body já faz.

5. **[P4/nit, confiança baixa] Achado editorial dos dois experimentos ("não adotar" em ambos os escopos) é consistente com o padrão já observado nas medições 4/5 do mesmo epic** — nenhuma inconsistência a reportar aí.

**Veredito**: `reject` — os dois achados #1 e #2 são falhas de CI reais e reprodutíveis causadas diretamente pelo conteúdo desta PR, não flakes; corrigir com a tag `@one-off-validity` nos 2 scripts e a linha `removal-declaration:` no corpo da PR resolve ambos.

<!-- continuo-review: run=1789854502-2653065-9842 at=2026-09-19T21:48:22Z verdict=reject head=03514c2e0125db43873c653f1f3efd858ba7faec -->
