Review automatizado (1 agente, effort low): commit contém marcadores de merge conflict (`<<<<<<< Updated upstream`/`=======`/`>>>>>>> Stashed changes`) não resolvidos, comitados dentro do script bash — reject.

## Achados

### 1. [P0, confiança alta] Marcadores de conflito de merge comitados no script — quebra o script em runtime
**Arquivo:** `hermes/scripts/watch-continuo-health.sh` (linhas ~16-22, ~335-341, ~347-376, ~381-409 no HEAD do PR).

O commit `eab682d9` contém literalmente `<<<<<<< Updated upstream`, `=======`, `>>>>>>> Stashed changes` embutidos no meio de comentários E de um bloco Python heredoc (linhas ~381-409, dentro do `GASTO=$(python3 ... | python3 -c "...")`). Confirmado via `git show eab682d9815b09823cecf3857d5e5c869e519e64:hermes/scripts/watch-continuo-health.sh` — os marcadores estão no blob commitado, não é artefato de exibição do diff.

**Cenário de falha concreto:** o job cron do Hermes (`--no-agent`, roda 1x/dia) executa este arquivo como está no HEAD. Há DUAS definições concorrentes do heredoc Python pra `GASTO` dentro dos marcadores de conflito — isso quebra a sintaxe do heredoc/script (parse error) antes de completar a checagem 8, e potencialmente aborta o script inteiro dependendo de onde o shell trava.

Curiosamente, a working tree local (`git status` limpo, conteúdo lido via `Read`) NÃO mostra os marcadores — está resolvida corretamente. Isso indica que o conflito foi resolvido no disco depois do commit, mas o COMMIT em si preserva a versão quebrada. Working tree limpa ≠ commit correto — quem só olhar o disco não vê o problema.

**Por que é P0:** script de observabilidade cuja finalidade declarada é ser "a rede de qualidade dos merges autônomos" do contínuo; se ele quebra no meio da execução, as checagens (review Opus diário, failure_streak, claims presos, vazamento pago) deixam de rodar de forma incerta — silencia justamente o observador que deveria pegar esse tipo de degradação.

### 2. [P3, confiança alta] Título/contexto da PR ("trabalho órfão recuperado de tick interrompido") explica a causa, não justifica pular a resolução
Característico de um commit gerado a partir de um checkout com merge/stash em andamento sem que o passo de resolução de conflito tenha sido re-commitado antes do commit final.

## Recomendação
Não mergear como está — resolver os marcadores (a versão "Stashed changes" é textualmente mais completa/atualizada) e recomitar, ou regenerar o commit a partir da working tree atual (que já está limpa e correta).

<!-- continuo-review: run=1788450406-2167776-15596 at=2026-09-03T15:46:46Z verdict=reject head=eab682d9815b09823cecf3857d5e5c869e519e64 -->
