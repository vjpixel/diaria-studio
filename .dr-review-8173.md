Review automatizado (1 agente, effort low): mecanismo bem construído (CI barato + eval real na esteira, allowlist com teste de drift, fix real do fleet review anterior); nenhum achado de alta/média confiança em P0/P1 — approve.

**Nota sobre o diff informado:** o range `6665602d..4357aa43` inclui ~2 hunks (`scripts/lib/scheduled-tasks.ts`, `docs/scheduled-tasks-registry.md`, remoção de `Diaria-Remediate-Never-Armed-Tasks`) que NÃO pertencem a este PR — `6665602d` não é ancestral do branch da PR (`git merge-base --is-ancestor` confirma), então essa diferença é ruído de comparar contra um commit irmão divergente, não uma mudança real desta PR. Os 3 commits da PR (`6a5184a2`, `8ad17f0c`, `4357aa43`) não tocam esses arquivos — confirmado via `git show --stat` de cada um e via diff contra o merge-base real (`811b1e0b`), que bate igual.

Achados (nenhum bloqueante):

1. **[baixa confiança, P3] Assimetria residual no fix de "infra vs 404".** O fix desta PR (`fetchFileContentAtRef`, usado por `run-agent-eval-for-pr.ts` via API REST) agora distingue 404 genuíno de falha de infra e lança nos outros casos — correto. Mas o companion do lado CI (`check-agent-eval-required.ts`) usa `gitShowFileAtSha` (`scripts/lib/diff-touched-lines.ts`), que continua colapsando QUALQUER falha de `git show` (arquivo ausente, objeto corrompido, shallow clone insuficiente) no mesmo `null`. O workflow roda com `fetch-depth: 0`, o que torna esse cenário raro no gate de CI (git local, não API de rede sujeita a rate-limit/token), mas a mesma classe teórica de bug (infra ≠ ausência real) sobrevive nesse call site — não corrigida pelo fix "#8144 fleet review" que endereçou só o lado API. Não é código novo desta PR (`gitShowFileAtSha` é reuso pré-existente), então é observação, não regressão.

2. **[baixa confiança, P3] `agentDeclaresMcpTools` em `run-agent-eval-for-pr.ts` chama `fetchFileContentAtRef` de novo pro head, apesar de `findTriggeringAgents` já ter buscado o mesmo conteúdo momentos antes** (linha ~1013-1016) — 1 chamada de API extra por agent disparado, puramente por não cachear o resultado já obtido. Efeito prático desprezível (poucos agents, chamadas já raras) — eficiência, não correção.

3. **[baixa confiança, P3] `computeCostDelta`/custo best-effort correto**, mas o comentário da PR nunca reporta explicitamente "custo indisponível" quando `--live` roda sem `cost.json` (ex: eval falhou antes de escrever o artefato) — `costRows` simplesmente fica vazio e a seção de custo é omitida em silêncio pro grader daquela edição. Consistente com o padrão "nunca fabricar dado ausente" do resto do módulo, mas um leitor da PR pode não perceber que o dado de custo está ausente vs. zero.

Pontos positivos que valem registrar: o teste de drift (`test/agent-eval-trigger-allowlist.test.ts`, varre `.claude/agents/*.md` real) implementa de fato o mandato "nunca por silêncio" da #8144; a ordem label-antes-do-comentário (self-review #2038) evita duplicação de comentário em retry corretamente; o warning-only/nasce-pendente está documentado em 3 lugares consistentes (workflow, script, docstring) sem contradição.

<!-- continuo-review: run=1789577226-3663323-31309 at=2026-09-16T16:47:06Z verdict=approve head=4357aa431740188b543e684f8b085f3203f8b300 -->
