---
name: diaria-overnight
description: Assume o turno no fim do dia (#2021) — varre as issues abertas, faz briefing interativo com o editor antes dele sair, e resolve a fila autonomamente até esgotá-la (PR → CI → auto-merge), com code-review consolidado pós-rodada (#2039). Ao final, registra o relatório na superfície de Relatórios do Studio (#3714) + resumo no terminal. Uso — `/diaria-overnight [--dry-run] [--bugs] [--priority P0,P1,P2,P3]`.
disable-model-invocation: true
model: sonnet
effort: medium
---

# /diaria-overnight

O editor invoca esta skill ao encerrar o expediente. Você assume o turno: varre a fila de issues do GitHub, tira todas as dúvidas com o editor **antes** dele sair (briefing único), e depois trabalha a fila de forma 100% autônoma **até esgotá-la** (#2039 — sem deadline de tempo). Ao final, roda um code-review consolidado do diff da noite e compila o relatório (registro na superfície de Relatórios do Studio, #3714, + resumo no terminal).

Escopo = **resolver issues de código/config/docs do repo**. Fora de escopo: executar a pipeline editorial (pesquisa, escrita, publicação de edição) — mudanças em código de publishers/Workers SÃO elegíveis, mas *disparar* publicação não.

Esta skill só roda por invocação explícita do editor (`disable-model-invocation: true`) — o blast radius (merges autônomos em master) exige que a invocação seja o consentimento, mesmo padrão de `/diaria-remover-votos-pixel`.

**Modelo/effort do coordenador (#2941, effort revisado em #3453).** O frontmatter fixa `model: sonnet` + `effort: high` — o loop principal (coordenador) roda em Sonnet 5 com effort `high` sempre que esta skill é invocada, sem exigir `/model`/`/effort` manual antes. Racional: o coordenador é majoritariamente orquestração + decisão estruturada (triage elegível/bloqueado, gate de 2 condições, review consolidado) — os subagentes implementadores já rodam em `sonnet` explícito (#2019); rodar o coordenador em Opus por horas custaria mais sem ganho proporcional de qualidade nessas decisões. **Effort baixado de `xhigh` → `high` (#3453):** o coordenador roda ~8h e é o único ator da rodada que roda o tempo inteiro, então é o maior candidato de custo por token — e a tarefa (triagem estruturada + gate de 2 condições + review consolidado) é do mesmo gênero de julgamento holístico que já roda em `effort: low` no `orchestrator`/`scorer-select`/`analyst-monthly` (#3218). Mudança reversível de 1 linha; monitorar pós-hoc a qualidade das rodadas seguintes ([[overnight-posthoc-quality-monitoring]]) em vez de A/B prévia. A instrumentação de token do coordenador (Fase 0 passo 1 + fim de cada fase) permite medir o efeito real desta troca em vez de "mergear e torcer" (#3453 Rec 1).

**EXPERIMENTO em curso (#5306, 15/08/2026): `effort` baixado de `high` → `medium`.** Hipótese: o guia de migração do Sonnet 5 indica que `medium` no Sonnet 5 é comparável em inteligência ao `high` do Sonnet 4.6 — geração em que o racional acima (#3453) foi calibrado. Se a hipótese se confirmar, o degrau de qualidade que o #3453 comprou ao não descer para `low` está disponível hoje um nível abaixo do configurado. **Esta troca vale só a partir da PRÓXIMA invocação desta skill** — o override de frontmatter escopa ao turno corrente (ver limitação documentada logo abaixo), então uma sessão já em andamento quando este arquivo foi editado continua no `effort` com que começou. Medição pendente: comparar `coordinator_tokens_estimate` (normalizado por unidade trabalhada) de rodadas futuras em `medium` contra as últimas rodadas em `high`, mais avaliação pós-hoc da qualidade da triagem/gate/review consolidado — protocolo completo e resultado a registrar em #5306 antes de tornar a mudança permanente. `.claude/skills/diaria-develop/SKILL.md` foi deixado de fora de propósito (fica em `high` como controle).

**Limitação documentada (confirmada contra a doc oficial do harness, `/en/skills#frontmatter-reference`):** o override de `model`/`effort` via frontmatter de skill vale **"pelo resto do turno atual"** — a sessão volta ao modelo/effort anteriores **"no seu próximo prompt"**. Não há mecanismo de hook (`UserPromptSubmit`/`PreToolUse`/etc.) que force troca de modelo/effort programaticamente; a única forma de trocar modelo é `/model` (manual) ou este campo de frontmatter (automático, mas escopado ao turno). Na prática, isso cobre bem o `/diaria-overnight`: a Fase 0 (briefing via `AskUserQuestion`) e toda a Fase 1 autônoma seguinte rodam dentro do mesmo turno iniciado pela invocação da skill — não exigem que o editor digite uma nova mensagem livre no meio do caminho. **Exceção real:** se o editor intervier por iniciativa própria digitando uma mensagem no terminal mid-rodada (já previsto como exceção na regra "zero perguntas pós-briefing" abaixo), isso conta como "próximo prompt" e a sessão volta ao modelo/effort anteriores a partir daquele ponto — não há como este arquivo evitar isso; se notar o modelo divergindo de Sonnet/`high` após uma intervenção, é esperado, não um bug.

## Argumentos

- `--dry-run` (opcional) — executa só a Fase 0 **sem nenhum side-effect externo** (não comenta em issues, não mexe em PRs) e imprime o plano. Serve de ensaio seguro.
- `--bugs` (opcional, #3375) — restringe a rodada a issues com label `bug`; `enhancement`/`documentation`/cleanup/etc. ficam fora mesmo que elegíveis por todos os outros critérios. Útil pra uma rodada focada em estabilidade (ex: antes de uma janela de maior risco, ou quando o backlog de bugs pede atenção dedicada) sem gastar PRs em melhorias. Aplica-se do início ao fim da rodada — varredura inicial (passo 3), re-scans de convergência e issues `mid-round` (Fase 1 passo 1). Sem a flag, comportamento atual sem mudança (todos os tipos elegíveis).
- `--priority [P0,P1,P2,P3]` (opcional, #3499, aceita lista) — restringe a rodada às issues cujo label de prioridade ∈ conjunto passado (ex: `--priority P2` ou `--priority P0,P1`); as demais ficam fora de escopo mesmo que elegíveis por todos os outros critérios — não são "puladas por bloqueio", simplesmente não entram, mesmo tratamento do `--bugs`. Compõe com `--bugs` (ex: `--bugs --priority P2` = só bugs P2). Aplica-se do início ao fim da rodada — varredura inicial (passo 3), re-scans de convergência e issues `mid-round` (Fase 1 passo 1). Sem a flag, comportamento atual sem mudança (todas as prioridades elegíveis).

## Fase 0 — Varredura + briefing interativo (editor ainda presente)

O objetivo é converter o máximo da fila em trabalho autônomo enquanto o editor ainda está aí pra responder. **Depois desta fase, zero interação.**

> **REGRA INVARIÁVEL — ZERO PERGUNTAS PÓS-BRIEFING:** Toda interação com o editor acontece **exclusivamente** nesta Fase 0. Depois do briefing, nenhum `AskUserQuestion` em nenhuma fase (Fase 1, re-scans, mini-rodadas, reviews). Issue ou finding ambíguo surgido mid-round → status `pulada` + comentário na issue explicando exatamente qual decisão falta (vira pergunta do briefing da **próxima** rodada). Vale mesmo que o editor pareça online: a rodada não pode depender de presença. Única exceção: o editor intervém por iniciativa própria (mensagem no terminal) — responder a ele não é "fazer pergunta", mas a rodada segue sem aguardar follow-up.
>
> **A regra vale também pra decisões de baixo risco disparadas por trabalho ad-hoc do editor mid-round** (ex: o editor pede pra investigar algo fora da fila de issues, e a investigação revela um achado colateral) — não é uma "zona cinzenta" que justifique confirmação só por parecer conversacional. Regra prática: se a ação é reversível e de baixo blast radius (criar uma issue com prioridade justificada, escolher entre duas implementações tecnicamente equivalentes, decidir se um achado merece documentação), a resposta é **sempre agir direto, nunca perguntar** — a pergunta "devo fazer X?" já é a resposta "sim, faça X" (CLAUDE.md, "Sempre indicar prioridade ao criar issues"). Responder a uma mensagem do editor não dá licença pra encaixar uma pergunta própria na resposta (histórico do incidente que motivou a regra: `docs/orchestrator-stage-narrative-history.md#overnight-3037-ctr-anomaly-260706`). Só pare de fato pra perguntar quando a ação é genuinamente irreversível/alto-blast-radius, ou é um trade-off real de produto/editorial afetando usuário final (mesmo critério das issues `precisa-resposta` — ver passo 4).

0. **Resume**: antes de assumir que é retomada, resolver o path pelo mesmo verbo do passo 7 (#6328 — generaliza pro overnight o mecanismo do #6309, que já protegia o `/diaria-develop`; a checagem de colisão precisa valer também nesta porta de entrada, não só na 1ª escrita): rodar `npx tsx scripts/resolve-overnight-plan-path.ts --aammdd {AAMMDD}` (`--session-id` auto-injetado, standalone, nunca em `&&`/`;`/pipe/heredoc — item 18 de `context/overnight-dispatch-rules.md`). `mode: "resume"` → é de fato ESTA sessão continuando (mesmo `session_id`); ler o `plan.json` no `path` devolvido e **pular o briefing** — retomar a partir dos status do próprio `plan.json` usando a tabela de estados abaixo. `mode: "fresh"` ou `"derived-after-collision"` → **NÃO é resume desta sessão**, mesmo que algum `plan.json` já exista pra `{AAMMDD}` — é de outra sessão em voo, **inclusive de OUTRA MÁQUINA** compartilhando o mesmo `data/` via OneDrive (o cenário concreto que motivou o #6328: uma 2ª máquina rodando `/diaria-overnight` no mesmo dia encontrava o plano da 1ª e assumia "é retomada" pelo `existsSync` puro, pulando o briefing e corrompendo mutuamente o plano da rodada viva, desassistida, sem alarme) — seguir para o passo 1 como sessão NOVA, briefing incluído; o `path` (já com sufixo, se `derived-after-collision`) é o mesmo que o passo 7 vai confirmar de novo antes da 1ª escrita — chamada idempotente, sem side effect, não há custo em resolver duas vezes. Se `plan.json` contém `resume_state` estruturado (ver passo 7), usar seus campos para retomada determinística (phase + pending_issues + next_action) em vez de parsing de texto livre, confirmando ao editor o que falta antes de prosseguir. **Re-armar o guard mecânico da Regra 1 no Resume (#4450):** toda linha da tabela abaixo pula direto pra Fase 1, Fase 1.5 ou Fase 2 — nunca pro briefing (passos 2-8) — mas o passo 1 (Sync, logo abaixo) ainda roda `--start` nesse caminho, que grava `phase: "briefing"` no marker. Como o passo 8 (onde normalmente se arma `--phase autonomous`) nunca é alcançado num Resume, rodar `npx tsx scripts/overnight-session-marker.ts --phase autonomous` explicitamente aqui, logo após o passo 1, **antes** de agir sobre qualquer linha da tabela — sem isso, o guard fica desarmado pelo resto de uma rodada retomada, justamente o cenário do incidente de origem (#4450: gap real de sessão seguido de retomada):

   | `findings_depth` | `review` do nível atual | Próxima ação |
   |---|---|---|
   | qualquer | issues sem status terminal existem | → Fase 1 (retomar issues pendentes) |
   | 0 | null ou ausente | → Fase 1.5 (review inicial) |
   | **0** | `"done (depth 0)"` ou começa com `"skipped:"` | → **Fase 2** (cadeia concluída — zero findings ou review pulado) |
   | N > 0 | null ou ausente, e há issues `finding-depth-N` sem status terminal | → continuar mini-rodada N (Fase 1 para essas issues) |
   | N > 0 | null ou ausente, sem issues `finding-depth-N` pendentes | → Fase 1.5 no nível N (iniciar review) |
   | **1** | `"done (depth 1)"` ou começa com `"skipped:"` | → verificar `review_1_5b_has_p2`: se `true` (review 1.5b encontrou ≥1 finding P2+) → mini-rodada 2; se `false` ou ausente → **Fase 2** (findings do 1.5b filados para próxima rodada) |
   | **2** | `"done (depth 2)"` ou começa com `"skipped:"` | → **Fase 2** (cadeia concluída — depth limit atingido) |

   **Compatibilidade de legado:** `review: "done"` (formato de plan.json anterior a este PR, sem `depth`) → tratar como `"done (depth {findings_depth})"` — cadeia concluída no nível corrente. `review_1_5b_has_p2` ausente em plan.json legado → tratar como `false` (encerra). Nunca re-perguntar o que já foi respondido. plan.json antigo sem `base_sha` → derivar do primeiro merge da rodada (`git log --reverse --format=%H --since="{started_at}" | head -1`, usar o pai dele) ou, falhando, pular a Fase 1.5 com warning no relatório.
1. **Sync**: `git checkout master && git pull` (o coordenador pode estar em outra branch — pull na branch errada faria os worktrees forkarem de HEAD não-master e os PRs da noite carregarem diff alheio). **Capturar `base_sha = git rev-parse HEAD` AGORA (pós-pull)** — é o ponto de partida do diff consolidado da Fase 1.5; capturado antes do pull, o diff incluiria commits alheios. **Capturar `started_at` = timestamp ISO 8601 REAL desta rodada AGORA** (`date -u +%Y-%m-%dT%H:%M:%SZ`, ou equivalente) — é o valor gravado em `started_at` no `plan.json` do passo 7 abaixo. **Nunca gravar a string `{AAMMDD}` nesse campo** (#3841 — bug de raiz do painel `/rodada` mostrando "INICIADA EM: 01/01, 00:00" e comparações quebradas tipo `created_at >= started_at` no passo 4: `{AAMMDD}` não é uma data parseável, `started_at` precisa ser). `{AAMMDD}` (com sufixo `b`/`c`/... quando há colisão do mesmo dia — desde o #6328, resolvido automaticamente por `scripts/resolve-overnight-plan-path.ts` nos passos 0 e 7, não mais convenção manual dependendo de alguém lembrar) continua nomeando só o DIRETÓRIO da sessão (`data/overnight/{AAMMDD}/plan.json`) — são dois campos com papéis diferentes, não o mesmo valor reaproveitado. Verificar `gh auth status`. **Marcar sessão overnight ativa nesta máquina (#3322)**: rodar `npx tsx scripts/overnight-session-marker.ts --start` — grava `data/overnight/.active-session-{hostname}.json`, o sinal que `.claude/hooks/pr-create-review.mjs` (`isOvernightRoundActive`) usa pra dar `low` effort no `/code-review` pós-PR independente de naming de branch (fallback do #3321). Idempotente — chamar de novo em resume não quebra nada. Removido na Fase 2 passo 7 (fim da rodada, #6758 — movido do passo 0 pra evitar recriação fantasma pelo `session-beacon.mjs` nas chamadas de ferramenta intermediárias da fase); se a rodada crashar antes de chegar lá, o marker expira sozinho depois de 24h (staleness embutida no hook — nunca fica "ativo" indefinidamente). **`--session-id` nunca é passado manualmente** — `.claude/hooks/inject-session-id.mjs` injeta automaticamente o `session_id` deste hook no comando antes de executar (#5156); isso é o que permite ao guard de `AskUserQuestion` e ao desconto de effort distinguirem esta rodada de uma sessão `/diaria-develop` rodando em paralelo na mesma máquina — ver seção "Paralelismo entre sessões (#5156)" no fim deste arquivo, nenhuma ação extra necessária aqui. **Registrar também no registro compartilhado de sessões**: `npx tsx scripts/lib/session-registry.ts register --kind overnight` (mesma injeção automática de `--session-id`) — é o registro que o item 3 (claim de issue), o item 4 (merge lock) e o item 9 (guard de cleanup de worktree) consultam; coexiste com o marker acima, não o substitui (ver seção final). **Esta skill chama `heartbeat` só para publicar `--active-worktrees` (#6299, abertura/fechamento de onda — ver passos 2 e 3 da Fase 1) — não para manter `lastHeartbeat` fresco por si; entre essas duas chamadas, quem mantém o registro fresco é o beacon** (#6327): `.claude/hooks/session-beacon.mjs` (`PreToolUse`) escreve no MESMO registro a cada chamada de ferramenta desta rodada, sem criar um paralelo. Não reduzir/desligar esse hook sem primeiro ler a docstring de `heartbeat` em `scripts/lib/session-registry.ts` — a claim desta rodada depende dele pra não ficar presa em `claimed_issues` de outra sessão por até 90min (`SOFT_STALE_MS`) além do necessário. **Log de startup do modelo/effort do coordenador (#2993):** logar no run-log um evento com o modelo/effort **CONFIGURADO** pelo frontmatter desta skill — não o auto-relatado pelo assistente, que não é 100% confiável (#2993) —, ex:
   ```bash
   npx tsx scripts/log-event.ts --edition {AAMMDD} --agent overnight --level info \
     --message "coordinator_model" \
     --details '{"model": "sonnet", "effort": "high", "source": "skill_frontmatter"}'
   ```
   Serve de confirmação verificável independente do relatório final — `/diaria-log {AAMMDD}` mostra o evento a qualquer momento da rodada. **Instrumentação de token do coordenador (#3453 Rec 1) — pré-requisito pra medir o efeito do effort `high`:** hoje ~50% do orçamento real da rodada (o lado do coordenador, que roda ~8h) é invisível — `plan.json` e `run-log.jsonl` só gravam token de subagente. Ao **fim de cada fase** (Fase 0 aqui, cada unidade da Fase 1 no passo 5, Fase 1.5 ao fechar cada nível, Fase 2 no relatório), emitir um evento `coordinator_tokens_estimate` no run-log **se o harness expuser `usage`/token count da sessão programaticamente**; na falta disso, registrar `context_size_estimate` (proxy — tamanho aproximado do contexto do coordenador no checkpoint). Forma:
   ```bash
   npx tsx scripts/log-event.ts --edition {AAMMDD} --agent overnight --level info \
     --message "coordinator_tokens_estimate" \
     --details '{"phase": "fase_0 | unidade #NNNN | fase_1_5 | fase_2", "tokens": N, "source": "harness_usage | context_size_proxy"}'
   ```
   Risco nenhum (só observabilidade). Desbloqueia comparar rodadas `xhigh` (pré-#3453) vs `high` (pós) de verdade, em vez de achismo. Se o harness não expuser nada estimável, logar `{"tokens": null, "source": "unavailable"}` uma vez na Fase 0 e não repetir — a lacuna fica registrada sem poluir o run-log. **Checar arming do watchdog externo (#2688, causa raiz #1 do incidente #2768):** rodar `npx tsx scripts/lib/check-watchdog-armed.ts` — em sessão local, verifica se a task "Diaria-Overnight-Watchdog" está registrada (Task Scheduler no Windows, systemd no Linux). **`cannot_verify_warn` (#4800) não é `not_armed_warn`:** a checagem então diz explicitamente "não deu pra verificar", não "não está armado". **Sem auto-arm (#5115, cutover final, 260812):** a proposta 3 do #2896 tentava armar automaticamente rodando um `.ps1` específico do Windows — removido junto com os outros 39 `.ps1` do repo (nenhuma tarefa `Diaria-*` roda mais no Windows, #5074), e armar systemd de verdade exige copiar units pra `~/.config/systemd/user/` + `daemon-reload` + `enable --now`, uma sequência mais invasiva demais pra rodar sem supervisão a partir do coordenador. Se o resultado for `not_armed_warn` (ou `cannot_verify_warn`), o comportamento é **só fail-soft**: loga warning no run-log — `agent: "overnight"`, `message: "watchdog_not_armed"` — e imprime a instrução manual (`docs/overnight-watchdog-setup.md`), sem tentar nada automaticamente. Em sessão cloud, o check inteiro é no-op (agendador é recurso local). Incluir o resultado final (`armed`, `not_armed_warn`, `cannot_verify_warn`, ou `skip_cloud`) no relatório da Fase 2 se `not_armed_warn` **ou** `cannot_verify_warn` — em ambos os casos o editor precisa saber que não há confirmação de que a segunda camada de proteção contra stall está ativa nesta máquina: em `not_armed_warn`, porque a task de fato não está armada; em `cannot_verify_warn`, porque a checagem não sabe consultar o agendador desta plataforma — não porque a task esteja de fato ausente. Note que esta é apenas uma das três sub-camadas de proteção contra stall (ver "Stall passivo — duas camadas (#2379 + #2688)" abaixo, que agora inclui uma terceira: fallback wake determinístico do coordenador, #2896).
2. **PRs abertos remanescentes**: listar PRs abertos. Auto-resolver **só os de autoria desta skill** (branch prefix `overnight/`): CI verde e não-draft → merge (fluxo da Fase 1, passo 3). Qualquer outro PR aberto (bot, WIP do editor, draft de noite anterior) → vira pergunta no briefing; **nunca** auto-mergear PR que a skill não criou. **Um PR alheio já aberto NÃO é por si só razão pra deixar a issue correspondente de fora do lote elegível (#5484)** — antes de tratar "já tem PR" como bloqueio na classificação do passo 4, aplicar o checklist de 3 perguntas de `context/overnight-dispatch-rules.md` item 16 (autor conhecido? CI rodando/verde? atualizado nas últimas ~24-48h?); falhando qualquer uma, a issue segue elegível normalmente, avaliando implementação independente.

   **2b. Pickup de PR órfão sem review externo do `continuo` (#6823, 31/08/2026).** Filtrar os PRs abertos do passo 2 por branch prefix `continuo/`. Para cada um, `npx tsx scripts/check-pr-review-authenticity.ts --pr N`: `exit 1` (`self_review`) **ou** `exit 2` (`no_review`) são sinal de pickup. `no_review` entra junto com `self_review` de propósito — é o caso da própria PR que motivou a #6823 (#6844): tick do `continuo` morreu antes de sequer postar o comentário de self-review, então não há marcador nenhum, só ausência total de review; tratar isso como "menos revisado que self-review" (não "review implícito ok") seria o oposto do que o gate do #6732 pretende. `exit 0` (`pass`, review independente já presente) e `exit 3` (`error` — `gh` falhou, PR sumiu, JSON malformado — estado ambíguo demais pra agir sozinho) **não** entram no pickup — tratar como PR alheio comum (checklist do item 16 acima). Para cada candidato de pickup:
      1. **Guard de caminho sensível** (mesmo gate do passo 2 §3 do `hermes-diaria-continuo`, fail-closed): `npx tsx scripts/lib/sensitive-path-guard.ts --base origin/master --json` sobre o diff do PR. `"sensitive": true` ou saída inválida → não mergear, comentar no PR o motivo, seguir para o próximo.
      2. **Review independente de verdade** — despachar via ferramenta **Agent** o agente `pr-review-toolkit:code-reviewer` (nome prefixado pelo plugin), `model: sonnet` explícito (#2019), somente leitura, com o range de diff explícito do PR (`{merge_base}...{branch}`, nunca o diff unstaged default) — mesmo mecanismo e mesma paridade de esforço (1 agente = `low`) que esta skill já usa pros próprios PRs. Se o autor do PR (a issue original) sinalizou algo específico pra conferir no corpo/comentários, incluir no prompt do review — não assumir, verificar.
      3. **CI genuína**: `npx tsx scripts/check-pr-checks-gate.ts --pr N` deve sair `exit 0`. `exit 4` (`blocked_by_conflict` — PR `CONFLICTING` com a base, GitHub nunca chegou a rodar os checks nesse SHA) é um veredito próprio, não "pendente": comentar pedindo rebase/atualização da branch, nunca "aguardando CI" (esperar não resolve, o PR precisa de ação, #6768).
      4. Review sem findings de alta confiança pendentes **e** gate de CI `exit 0` **e** guard de caminho não-sensível → squash-merge (mesmo comando/disciplina do passo 3 da Fase 1: 1 PR por vez, nunca dois merges concorrentes). Qualquer achado de alta confiança, CI vermelho/pendente/conflitante, ou path sensível → **não mergear**: comentar no PR com o motivo específico (achado, "aguardando CI", "precisa rebase — branch desatualizada com a base", ou "caminho sensível — requer revisão humana") e seguir para o próximo PR/issue — nunca travar a Fase 0 esperando.

      Registrar no `plan.json` (campo `continuo_pickups: [{pr, verdict}]`) e no relatório final da Fase 2 quantos PRs órfãos foram encontrados, revisados e o desfecho de cada um — mesma disciplina de auditoria do resto da Fase 0. Este passo não reduz a autonomia do `continuo` nem afrouxa o gate do #6732: o self-review dele continua correto (evita self-merge inseguro); este passo só fecha o "e depois?" que o #6823 identificou como faltante.
3. **Varredura**: `npx tsx scripts/fetch-open-issues-for-triage.ts` (#7018 — CLI sobre `scripts/lib/issue-triage-fetch.ts`, substitui o `gh issue list --json ...` escrito à mão) — devolve cada issue já com `execTrack` calculado via `classifyExecTrackFromListItem` (`scripts/lib/issue-exec-track.ts`), que **falha alto** (lança) se o `body` estiver ausente do payload, em vez de degradar `agendada`→`overnight` em silêncio (o próprio bug do #7018: rodada 260901b dispachou 4 issues fora da janela porque a varredura tinha saído sem `body`). **Nunca remontar a chamada `gh issue list` à mão nem reclassificar com `classifyExecTrack` sobre um corpo reconstruído** — o ponto do #7018 não é "lembrar de passar `body`", é tornar impossível classificar sem ele; só o CLI fino garante isso. `updatedAt` continua no retorno por outros consumidores (comparação com `decided_at`/`recorded_at` no passo 4 abaixo, exibição no plano); o passo 4 NÃO compara `decided_at`/`recorded_at` contra ele — #6961. `--bugs`/`--priority` continuam suportados como flags do script (filtragem idêntica à descrita abaixo). **Com `--bugs` (#3375)**: filtra o resultado para só issues com label `bug` — as demais nem entram na classificação do passo 4, não precisam de comentário/skip explícito (não são "puladas por bloqueio", estão simplesmente fora de escopo do modo). Registrar em `plan.json` (campo `bugs_only: true`) e no relatório final quantas issues foram excluídas por esse motivo, pra visibilidade. **Com `--priority` (#3499)**: filtra adicionalmente o resultado para só issues cujo label de prioridade ∈ conjunto passado (ex: `--priority P0,P1` mantém só `P0`/`P1`) — mesmo tratamento: as demais nem entram na classificação do passo 4, fora de escopo do modo, não "puladas por bloqueio". Registrar em `plan.json` (campo `priority_filter`, ex: `["P2"]`, ou `null` se a flag não foi passada) e no relatório final quantas issues foram excluídas por esse motivo. Compõe com `--bugs` — os dois filtros se aplicam em conjunto (interseção).
4. **Classificar cada issue** em:
   - `elegivel` — direção clara e completa na issue; dá pra resolver sem perguntar nada. Issue sem label de prioridade → tratar como `P2` e anotar no plano (não inventar prioridade alta nem descartar). **Issue escrita pela própria sessão overnight/develop/continuo (autoria própria) nunca recebe tratamento mais permissivo que issue de terceiro (#5484, `context/overnight-dispatch-rules.md` item 16)** — "exige decisão de design"/"melhor com mais contexto dedicado" só justifica adiar se bater literalmente um dos 4 critérios de "Perguntar é exceção" do `CLAUDE.md`; sem isso, classificar e dispatchar como qualquer outra issue elegível.
   - **Preflight de duplicidade ANTES do dispatch (#7020) — obrigatório pra toda issue que sair `elegivel` daqui.** Antes de abrir worktree/dispatchar o subagente implementador, rodar `npx tsx scripts/check-issue-duplicate-preflight.ts --issue N --updated-at {updatedAt da issue}` (CLI sobre `scripts/lib/issue-duplicate-preflight.ts`, que faz `git log origin/master --grep "#N"` — nunca `--all`). Custa um `git log`, não um subagente inteiro — o preflight que já existe do lado do subagente (item 14 de `context/overnight-dispatch-rules.md`) descobre a mesma coisa, mas só depois de já ter gasto o dispatch. Três vereditos: **`not-in-master`** (exit 0) — sem indício de duplicidade, dispatch normal, segue como `elegivel` sem mudança. **`closes-should-be-closed`** (exit 1) — algum commit em `origin/master` já usa `Closes #N` pra essa issue e ela segue aberta: não dispatchar o escopo completo, tratar como unidade de CLOSEOUT barata (confirmar que o commit de fato cobre o pedido, comentar e fechar — não abrir worktree/subagente pra reimplementar o que já está em master). **`refs-declared-residue`** (exit 1) — commit já cita `#N` sem `Closes` (resíduo declarado ou citação incidental): reler o corpo da issue contra o diff desse commit e dispatchar só o resíduo real que falta, nunca o escopo original inteiro. `resolvedAfterLastUpdate: true` no resultado é sinal reforçado de "resolvida depois, ninguém reavaliou" (#6852/#6858/#6879) — citar isso no comentário de closeout/redução de escopo. O preflight do subagente (item 14) continua como rede de segurança, não substituído por este passo.
   - **Antes de classificar qualquer issue como `precisa-resposta` (#5373):** checar se ela já tem a label `decisao-registrada` (custo zero — vem no mesmo fetch do passo 3) e, se sim (ou mesmo sem a label, como rede de segurança), rodar `npx tsx scripts/lib/issue-decisions.ts --issue N` (helper puro em `scripts/lib/issue-decisions.ts`). **Comparação concreta (#6961):** a decisão vale quando é o marcador MAIS RECENTE da thread (comparada contra `execution_block`, nunca contra `updatedAt` da issue — o próprio POST do marcador bumpa `updatedAt`, tornando `decided_at >= updatedAt` insatisfazível por construção). Decisão encontrada e mais recente que um eventual bloqueio → a issue **NÃO** vai ao briefing: usar a decisão como contexto e classificar como `elegivel` (execução direta) ou, se a execução em si esbarrar em bloqueio novo, `bloqueada-externa`/`requer-sessao-local` (nunca re-perguntar a decisão em si). Só reabrir como `precisa-resposta` se existir um `execution_block` mais recente que a decisão (ver item seguinte).
   - **Bloqueio de execução já registrado (#5373 item 5) — nunca re-perguntar a decisão.** O mesmo `npx tsx scripts/lib/issue-decisions.ts --issue N` acima também retorna `execution_block` (via `latestExecutionBlockFor`). Se a issue tem a label `bloqueio-execucao` (ou mesmo sem ela, como rede de segurança) e o `execution_block.recorded_at` é **posterior ou igual** a um eventual `decided_at` (#6961: os dois marcadores comparados entre si, nunca contra `updatedAt`) — a issue **nunca** entra no lote de perguntas nem é reclassificada como `precisa-resposta`: ela é `bloqueada-externa` (motivo = `execution_block.motivo`), reportada como "bloqueada por execução: {motivo}" na tabela do passo 4.5, preservando a decisão já registrada no contexto pra quando alguém (fora da sessão) resolver o bloqueio. Uma decisão nova, mais recente que o bloqueio → o bloqueio pode ter sido resolvido, reavaliar normalmente.
   - `precisa-resposta` — ambiguidade **trivial-mas-não-documentada** que cabe numa `AskUserQuestion` no briefing: escolha pequena, sem trade-off real de usuário final (ex: "formato A ou B de log", "opção técnica equivalente"). **Issue de trade-off-real de produto/editorial** (decisão genuína entre opções que afetam o usuário final — ex: design system vs documentar, CSS-only vs JS) → **não** vai ao briefing; marcar `pulada` motivo `ambígua/trade-off-real`, **usar `route-issue`** (`npx tsx scripts/route-issue.ts --issue N --track develop --reason "trade-off-real de produto/editorial (#2640)"` — #6196: substitui `gh issue edit N --add-label trade-off-real` que nunca tinha chamador no write-path; até #6196 mergear, `gh issue edit N --add-label trade-off-real` ainda aceitável como fallback), **comentar na issue** explicando que é trade-off-real e direcionando ao `/diaria-develop` (cat. C), e seguir para a próxima (com dedup — não comentar se já existe comentário overnight equivalente). Linha divisória: se a resposta depende de preferência sobre experiência do usuário final → trade-off-real → `/diaria-develop`; se é escolha técnica sem impacto diferencial em usuário → trivial → briefing.
   - **`sem-direcao-acionavel` (#5968)** — 3º desfecho de ambiguidade, distinto dos dois acima: a rodada reavaliou a issue e concluiu explicitamente "sem ação de código clara a tomar", sem ser ambiguidade trivial que caiba numa pergunta de briefing (`precisa-resposta`) nem trade-off real de produto/editorial (`trade-off-real`) — ex: investigação encerrada sem causa raiz identificável, issue cujo escopo evaporou sem virar código nenhum. Marcar `pulada` motivo `sem-direcao-acionavel`, **usar `route-issue`** (`npx tsx scripts/route-issue.ts --issue N --track fora-de-rodada --reason "sem-direcao-acionavel (#5968)"` — #6196: substitui `gh issue edit N --add-label sem-direcao-acionavel`), e comentar na issue explicando a conclusão (com dedup). Se o corpo/labels mudarem depois (novo dado, novo comentário do editor), a label deixa de se aplicar — reavaliar normalmente e remover a label ao reabrir a issue pro fluxo normal.
   - `bloqueada-externa` — precisa de ação que só o editor pode fazer (conta de terceiro, allowlist, credencial). **Usar `route-issue`** (`npx tsx scripts/route-issue.ts --issue N --track bloqueada --reason "..."`, #6196 — substitui `gh issue edit N --add-label external-blocker` que nunca tinha chamador no write-path das SKILLs; ressalva temporária até #6196 mergear: `gh issue edit N --add-label external-blocker` ainda aceitável como fallback). **Aplicar a label `external-blocker`** (`gh issue edit N --add-label external-blocker` — #5462: é ela que faz a Triagem do Studio classificar a issue como Bloqueada; sem a label a classificação só existe em prosa), **comentar na issue** o que falta (usando `route-issue` quando #6196 estiver mergeado; até lá, `gh issue comment` direto é aceitável como fallback), e pular (com dedup — checar antes se já existe comentário overnight equivalente — não comentar de novo).
   - **`requer-sessao-local`** — duas fontes, desde que a label `local` foi aposentada (#5462, 16/08/2026): **(a)** label **`windows`** — exige a máquina Windows do editor (Chrome logado, ComfyUI). O overnight roda no servidor Linux e **nunca** alcança essas, independente de sessão: marcar `pulada` motivo `requer-sessao-local`, comentar direcionando ao `/diaria-develop` (com dedup), e pular. **(b)** qualquer issue em sessão **cloud** (container efêmero, sem junction `data/`) que dependa de recurso machine-local — detectar o modo com `npx tsx scripts/lib/exec-mode.ts` (imprime `local` ou `cloud`; exit 0 sempre). Label **`server`** (systemd, tarefas agendadas, Studio, OneDrive) **não** entra aqui em sessão local: é exatamente a máquina onde o overnight já roda, então é elegível normalmente. Label `local` remanescente (issues antigas) é informacional — não classifica sozinha; reler o corpo pra decidir se é `windows` ou `server` e reetiquetar.
   - `not-this-week` / `fora-do-escopo` — labels ou critério explícito que exclui da rodada. **Usar `route-issue`** (`npx tsx scripts/route-issue.ts --issue N --track agendada --reason "..." --until AAAA-MM-DD` para agendada; `--track bloqueada --reason "not-this-week"` para deferimento) — #6196: substitui `gh issue edit N --add-label not-this-week` que nunca tinha chamador no write-path; ressalva temporária até #6196 mergear: `gh issue edit` ainda aceitável como fallback. **Aplicar a label `not-this-week`** (`gh issue edit N --add-label not-this-week` — #5777: é o sinal que `classifyExecTrack` lê pra rotear a issue pra Bloqueada em vez de Overnight na Triagem do Studio; sem a label, o julgamento desta rodada só existe em prosa e a issue continua aparecendo como elegível) quando ainda ausente, e **comentar na issue** explicando o motivo específico (com dedup — não comentar de novo se já existe comentário overnight equivalente).

   **Toda classificação `pulada` comenta na issue com o motivo específico (#5777, generaliza o padrão já aplicado a `bloqueada-externa`/`requer-sessao-local`/`ambígua-trade-off-real` acima)** — sem isso, a Triagem do Studio (que lê `classifyExecTrack` sobre labels/corpo do GitHub, nunca `plan.json`) continua mostrando a issue como elegível mesmo depois de avaliada e descartada nesta rodada. Vale tanto para a classificação feita aqui no passo 4 (varredura inicial) quanto para qualquer `pulada` decidida mid-round na Fase 1 ou em re-scans subsequentes — ver Fase 1 passo 1. Sempre com dedup (nunca comentar de novo se já existe comentário overnight equivalente pro mesmo motivo). **Exceções explícitas, preservadas de propósito:** (a) "decido depois" no briefing (Fase 0 passo 5) — a issue não foi prometida, então não recebe comentário; (b) `claimed-por-outra-sessao` (Fase 1 passo 2) — não é bloqueio, é corrida evitada entre sessões concorrentes, resolve-se sozinha quando a outra sessão termina.

   **Nenhum status genérico "bloqueada" cobrindo motivos distintos (#5376,
   15/08/2026 — mesma classe de bug observada ao vivo no `/diaria-continuo`
   com a fila de fim de rodada).** Cada issue leva o status específico da
   lista acima — `bloqueada-externa` (credencial/conta/allowlist) e
   `ambígua/trade-off-real` (decisão de produto pendente, cat. C do develop)
   nunca aparecem fundidas num rótulo vago tipo "decisão de produto, conta
   externa, ou ação manual" no relatório da Fase 2 nem no comentário da
   issue — o comentário/relatório sempre nomeia o status específico e o que
   falta. Antes de rotular qualquer issue como bloqueada, reler o corpo
   inteiro: se ele já descreve os passos de execução (ex: "scoping"/
   "investigação" com protocolo escrito), é `elegivel`, não bloqueio — o
   atalho de classificar pelo título/label sem checar o corpo foi a causa
   raiz do #5376.

   **Issue com checklist/múltiplos itens: classificar item por item, não a
   issue inteira (#5379, 15/08/2026)** — o mesmo atalho reaparece um nível
   abaixo, *dentro* de uma issue com corpo em lista/checklist: rotular a
   issue inteira como `bloqueada-externa`/`ambígua/trade-off-real` quando só
   parte dos itens depende do bloqueio, e o resto já tem caminho de execução
   claro (achado ao vivo no `/diaria-continuo`: #4555 tinha decisão editorial
   embutida junto de prospecção externa; #5237 tinha 4 de 5 itens de
   checklist codáveis já, com só 1 item preso a um developer token). Antes de
   rotular como bloqueada uma issue com múltiplos itens: percorrer cada item
   separadamente. Itens sem decisão/credencial pendente ficam `elegivel`
   (dispatch normal) mesmo que outro item da mesma issue siga bloqueado —
   dispatch parcial (unidade cobrindo só os itens elegíveis + comentário
   registrando o(s) item(ns) que segue(m) faltando, com o motivo específico
   de cada um) é o resultado esperado. Buscar no corpo por "a escolha é
   editorial", "decidir", "qual"/"quanto"/"quando" em frase interrogativa,
   "trade-off" antes de fechar qualquer item como bloqueado — sinal de que
   é `ambígua/trade-off-real` disfarçada de `bloqueada-externa` (heurística
   de atenção, não substitui reler o corpo inteiro).

   **Verificação de estado antes de classificar qualquer issue como
   bloqueada — `bloqueada-externa`, `requer-sessao-local`,
   `ambígua/trade-off-real`, ou `not-this-week` (#5383, generalizado em
   #5392).** "Escopo grande, scoping futuro" **não é um 5º status** — é
   sempre uma leitura que se resolve em `not-this-week` (o status já
   existente pra "trabalhável, mas não nesta rodada"), nunca um motivo
   paralelo aos 4 reais. Nenhuma dessas classificações é automática a partir
   de "a issue parece bloqueada" — o #5383 original restringia a
   verificação só ao caso "escopo grande"; o #5392 achou o mesmo atalho nas
   outras classificações (achado concreto: #5255 classificada
   `requer-sessao-local` sem checar `docs/audience-source-notes.md`, que já
   tinha a decisão completa registrada 2 dias antes da issue existir). Antes
   de aceitar qualquer uma dessas 4 leituras pra uma issue — inclusive
   quando a leitura inicial for "escopo grande demais" (→ `not-this-week`)
   — rodar as 4 checagens abaixo:
   1. `gh issue view N --json comments` — ler os comentários mais recentes
      **por inteiro**, não só o `body`. Procurar menção a PR já mergeado,
      unidade já dispatchada, ou progresso parcial registrado.
   2. `git log --oneline --all --grep "#N"` — trabalho já mergeado costuma
      citar o número da issue no commit message mesmo quando o comentário na
      issue não foi lido a tempo.
   3. Se algum comentário citar um doc de acompanhamento (`docs/*.md`), ler
      esse doc **inteiro** — a convenção deste repo é fechar cada rodada de
      trabalho com uma seção "estado após esta rodada"/"candidatas pra
      próxima rodada" já pronta (ex real: `docs/entity-page-candidates.md`).
   4. **`grep -il {palavra-chave do título/tema} docs/*.md`** — buscar um
      doc relacionado ao ASSUNTO da issue, mesmo sem link em nenhum
      comentário (o #5255 nunca citou `docs/audience-source-notes.md`).
      CLAUDE.md já documenta esse padrão de doc como "registro de decisão
      que evita reabrir investigação já concluída" (`docs/seo-notes.md`,
      `docs/audience-source-notes.md`). Achou um doc relacionado → ler **por
      inteiro** antes de aceitar a classificação de bloqueio — ele pode
      conter a decisão que torna a issue não-bloqueada.

   **Quando pular a checagem 4.** É barata o bastante pra rodar em toda
   classificação de bloqueio (um `grep`, não um fleet de agentes) — o skip
   por label é estreito, não vale a issue inteira: só pula quando o
   `npx tsx scripts/lib/issue-decisions.ts --issue N` (passo 4 acima)
   confirma que a decisão/bloqueio JÁ REGISTRADO cobre exatamente o motivo
   de bloqueio sendo avaliado agora, não qualquer outro item/sub-pergunta
   da mesma issue. Item isolado sem marker correspondente → roda a
   checagem 4 mesmo com a issue tendo `decisao-registrada`/
   `bloqueio-execucao` de outro item.

   Só se as 4 checagens não acharem nada (nenhum PR, nenhum comentário de
   progresso, nenhum doc de acompanhamento, nenhum doc relacionado por
   assunto) é legítimo classificar a issue como bloqueada — pelo motivo
   específico aplicável (`bloqueada-externa`, `requer-sessao-local`,
   `ambígua/trade-off-real`, ou `not-this-week` quando o motivo for escopo
   grande demais pra esta rodada). Caso contrário, o próximo passo já está
   documentado — classificar como
   `elegivel` e dispatchar essa fatia pequena nesta mesma rodada, ou, no
   mínimo, reportar o próximo passo concreto na tabela do passo 4.5 em vez de
   aceitar a leitura de bloqueio.

   **Herdar classificação de rodada anterior do MESMO DIA não dispensa esta
   verificação (#5586).** Este arquivo não tem um passo numerado dedicado a
   "ler o `plan.json` de uma rodada overnight anterior do mesmo dia" (ao
   contrário do `/diaria-develop`, que tem um passo explícito — Fase 0
   passo 2, "Herdar a triagem do overnight") — mas na prática uma rodada que
   começa depois de outra do mesmo dia já ter triado a fila
   (`data/overnight/{AAMMDD}[b/c/...]/plan.json` de horas antes, ou o
   contexto de comentários que a checagem 1 acima já leria) pode ser tentada
   a economizar trabalho reaproveitando o status já gravado em vez de
   reclassificar do zero — atalho razoável em teoria, mas que pula
   exatamente a verificação que este bloco existe pra impor. Não importa se
   essa herança é um passo codificado em algum lugar ou só um julgamento ad
   hoc da sessão no momento: herdar não é uma via paralela que escapa dele.
   Antes de aceitar
   qualquer `bloqueada-externa`/`requer-sessao-local`/`not-this-week`/
   `ambígua/trade-off-real` herdada de uma rodada anterior, rodar uma
   checagem ainda mais barata que as 4 acima (não é a 5ª delas, é um
   pré-filtro específico pra herança): `gh issue view N --json labels` e
   conferir se a **label** citada como motivo da classificação herdada
   **ainda existe** na issue (comparação de 1 campo, não uma investigação
   completa — cobre o caso de motivo por label; motivo registrado só em
   marcador de comentário exige reler o comentário, checagem 1 acima).
   Motivo batendo → aceitar a herança sem reabrir as 4 checagens. Motivo NÃO
   batendo (label removida, nunca existiu de fato,
   ou o corpo mudou desde a classificação original) → tratar a issue como se
   estivesse sendo classificada agora pela primeira vez e rodar a
   verificação completa das 4 checagens acima — nunca propagar a
   classificação stale pra mais uma rodada. Achado concreto (#5586): a
   rodada `260817c` herdou de `260817b` a classificação `bloqueada-externa:
   label external-blocker` pra #5237 sem reler a issue; a label não existia
   mais (a checagem de 1 campo já teria pego isso) e o corpo tinha 4 de 5
   itens de checklist já codáveis, só 1 preso a um developer token — mesmo
   padrão do #5379 acima, item por item. A mesma regra vale na direção
   `/diaria-overnight` → `/diaria-develop` (ver seção correspondente em
   `.claude/skills/diaria-develop/SKILL.md`, passo "Herdar a triagem do
   overnight").

   **O mesmo princípio vale pra julgamento em PROSA herdado de um
   relatório de sessão anterior, não só pra label mecânica (#5596).** O
   bloco acima cobre a superfície MECÂNICA — uma label do GitHub, checável
   em 1 campo. Mas a mesma tentação de economizar trabalho existe quando o
   que é herdado não é uma label e sim um trecho de prosa de um
   `data/{overnight,develop}/{AAMMDD}/report.md` de uma rodada anterior do
   MESMO DIA (ou o próprio resumo que a sessão atual escreveu num turno
   anterior da conversa) — ex: "pausei o cluster de anúncios a pedido do
   editor até X ser resolvido" citando um conjunto de issues por número.
   Generalizar "isso era verdade quando o relatório foi escrito" para "isso
   ainda é verdade agora" sem reabrir cada issue é o mesmo erro do bloco
   acima, só que sem label nenhuma pra checar mecanicamente — o filtro
   barato aqui é reabrir `gh issue view N --json comments` e conferir se
   existe comentário **mais recente que a referência usada**, não confiar
   no resumo em prosa. O risco cresce com o tamanho do cluster citado
   (quanto mais issues agrupadas na mesma frase do relatório antigo, mais
   convidativo tratá-las como uma unidade só, quando cada uma pode ter
   avançado num ritmo diferente desde então). Achado concreto (#5596): uma
   sessão `/diaria-develop` de 260817 herdou "cluster de anúncios pagos
   bloqueado" de um relatório de horas antes no mesmo dia e reportou 8
   issues como "genuinamente bloqueadas" sem reabrir nenhuma — 3 delas
   (#5522, #5500, #5543) já tinham avançado (uma reaprovada em comentário
   posterior ao que fundamentou o "bloqueado", outra com item de checklist
   sem bloqueio real, a terceira respondível na hora) e só foram destravadas
   depois de o editor perguntar por que ficaram sem dispatch.

   **`in_round` (#3131):** ao gravar cada issue em `plan.json` (passo 7), toda issue classificada `elegivel`/`precisa-resposta` **aqui neste passo 4** recebe `in_round: true` — ela genuinamente entrou no escopo de trabalho desta rodada, mesmo que uma `precisa-resposta` termine em "decido depois" (`pulada`) no briefing do passo 5, ou que uma `elegivel` seja pulada MID-RODADA já na Fase 1 (`sem-resposta`, `ambigua` — ver Fase 1 passo 1/5): a decisão de pular foi tomada trabalhando a fila, não antes dela. Já toda issue classificada `bloqueada-externa`, `requer-sessao-local`, `not-this-week`, `fora-do-escopo`, ou `ambígua/trade-off-real` **aqui neste passo 4** — ou seja, excluída ANTES de qualquer despacho, já na varredura inicial — recebe `in_round: false`: nunca foi trabalho real desta rodada. `scripts/overnight-statusline.ts` (`renderOvernightBar`) usa esse campo para excluir essas issues do denominador `done/total` da barra — sem isso, uma rodada com issues bloqueadas infla o denominador com trabalho que nunca entrou na fila (incidente 260707: `plan.json` tinha 57 issues, só 53 de fato "entraram" na rodada — a barra mostrava `6/57` quando o sinal útil era `2/53`). Issues sem o campo (plan.json legado, anterior a este PR) são tratadas como `in_round: true` (fail-open — mesmo padrão de `machine_id`/`review_1_5b_has_p2` ausentes).
4a. **Antes de classificar, reconciliar dependências entre issues (#7137, item 24 de `context/overnight-dispatch-rules.md`):** `npx tsx scripts/reconcile-issue-dependencies.ts --apply` — sincroniza a label `dependencia-aberta` com o marcador `depends-on:` de cada issue, pra que a classificação abaixo já leia o estado atualizado (sem isso, uma dependência fechada não desarma sozinha). **Classificar cada issue via `classifyExecTrack` e reportar divergência com o status em prosa do passo 4 (#6204, espelha o passo 6a do `/diaria-develop`, #5708).** A Triagem do Studio e esta Fase 0 derivavam a elegibilidade de issue por dois caminhos independentes que nunca se conferiam — o painel dizia "Overnight" e a rodada tinha classificado a mesma issue de outro jeito, sem nenhuma superfície reportar o delta (#6204). Pra cada issue já classificada no passo 4 (labels + corpo já em mãos, sem custo adicional de rede), rodar `classifyExecTrack({ labels, body, state })` de `scripts/lib/issue-exec-track.ts` (puro — import direto, sem CLI) e comparar o resultado contra o `ExecTrack` esperado pro status em prosa via `expectedTracksForProseStatus`/`isProseTrackConsistent` de `scripts/lib/overnight-prose-track-map.ts` (a tabela de correspondência travada por teste, #6204 item 3 — nunca re-derivar a correspondência de memória). Issue ainda `precisa-resposta` no momento deste passo não é comparável (`tracks: []` por construção — é efêmera, resolvida no passo 5 seguinte); rodar a comparação de novo depois do briefing, quando ela já virou `elegivel` ou `pulada`. **Divergência não é erro automático — precisa ficar VISÍVEL.** Gravar por issue no `plan.json` (`exec_track_painel`: o valor de `classifyExecTrack`; `exec_track_divergiu`: bool) e listar as divergências na tabela do passo 4.5 abaixo — issue cujo painel diz `develop`/`overnight` mas esta rodada está prestes a marcá-la `bloqueada-externa`/fora do escopo é candidata natural às 3 checagens do item 16 de `context/overnight-dispatch-rules.md` (não dispara sozinha; é o mesmo sinal que já levaria lá, só que agora comparável mecanicamente). **Gate de cobertura, antes do relatório final (Fase 2):** rodar `npx tsx scripts/check-exec-track-coverage.ts --plan data/overnight/{AAMMDD}/plan.json` — `exit 1` lista os números sem `exec_track_painel` ou com valor fora do enum de 6 tracks; backfill mecânico (rodar `classifyExecTrack` e gravar), sem julgamento, até `exit 0`. **Gate irmão pro campo `motivo` das issues `pulada` (#6438, passo 0.85 da Fase 2):** `motivo` é vocabulário LIVRE por natureza (não vem de `classifyExecTrack`) — `npx tsx scripts/check-overnight-plan-motivo.ts --plan data/overnight/{AAMMDD}/plan.json` fecha esse vocabulário contra `OVERNIGHT_PULADA_MOTIVOS` (`scripts/lib/overnight-plan-motivo.ts`), incluindo os 3 motivos fechados por esta issue: `mesmo-tema-sessao-ativa` (issue de mesmo tema de sessão peer ativa SEM claim registrado — avisar via `SendMessage` pedindo `claim-issue`; sem resposta em ~15min, dispatchar a própria rodada, nunca "deixar pra ela" sem claim), `session-finding-deferida` (finding de rodada anterior deferido → `route-issue --track agendada --until amanhã --reason "finding da rodada anterior, reavaliar amanhã"` — `--reason` é obrigatório desde #7288) e `stale-aguarda-reexecucao` (falha antes do fix mergear, #5653/#5615 → `route-issue --track agendada --until {próxima execução} --reason "aguardando a próxima execução da task relevante"`).

4.5. **Tabela da fila completa** — imprimir ANTES do briefing, para o editor ver o escopo inteiro da noite e poder resgatar exclusões imediatamente:

   **Entram na rodada** (elegível + precisa-resposta), ordenadas P0 > P1 > P2 > P3:
   ```
   #NNNN | P?  | elegivel / precisa-resposta | título resumido
   ```

   **Ficam de fora**, com motivo explícito por issue:
   ```
   #NNNN | P?  | motivo                                         | título resumido
   ```
   Motivos possíveis: `not-this-week`, `bloqueio-externo: {o que falta destravar}`, `requer-sessao-local: junction data/ ausente em cloud`, `dados imaturos até {data}`, `fora do escopo overnight`, `ambígua/trivial: {o que falta — próximo briefing}`, `ambígua/trade-off-real: → /diaria-develop (cat. C)`, `guard-de-execucao: vedado pelo guard de publicação — exige {o que a sessão não pode rodar} → /diaria-develop`. Usar o motivo mais específico — nunca só "pulada".

   **Coluna de divergência (#6204, passo 4a acima).** Toda linha da tabela leva `exec_track_painel` (o veredito de `classifyExecTrack`) ao lado do status em prosa; issue com `exec_track_divergiu: true` ganha marcador visível (ex: `⚠ painel diz {track}`) — não muda o dispatch desta rodada por si só, mas nenhuma divergência sai invisível do briefing.

   **O `motivo` é lido por máquina, não só por humano (#5955).** O gate de drift do passo 0.8 varre este campo em TODAS as issues do plano (inclusive `in_round: false`) procurando veredito que nunca virou label — é o caminho que pega o bounce por guard de execução mesmo quando a sessão não chegou a comentar na issue. Por isso o `{...}` em prosa não é ornamento: `bloqueio-externo` sozinho, sem o texto que explica o quê, não casa padrão nenhum e o veredito volta a morrer no plano. Escrever a frase inteira ("exige rodar envio real de campanha Clarice, vedado pelo guard de publicação"), não só o rótulo — o modelo do parágrafo acima já vem com o verbo de impedimento embutido justamente pra que uma cópia literal dele case.

   **Sem `AskUserQuestion` própria pra resgate (#5321, "Perguntar é exceção" — antes disso havia uma pergunta dedicada aqui).** A tabela impressa já dá toda a informação que o editor precisa pra agir; perguntar "alguma issue excluída deve entrar?" depois de já mostrar exatamente quais ficaram de fora e por quê é checagem de humor, não coleta de informação nova — o editor que quiser resgatar uma exclusão fala direto ("inclui a #1234"), e isso não é "fazer pergunta" (é responder ao editor). Se **houver** `precisa-resposta` simultâneas, o passo 5 já faz a única `AskUserQuestion` da Fase 0 — o resgate de exclusão continua encaixável ali quando o editor intervier por iniciativa própria, mas não é mais um item que a skill pergunta proativamente. Sem `precisa-resposta` nenhuma, a Fase 0 segue direto pro passo 6 sem parar.

5. **Briefing**: para as `precisa-resposta`, fazer **todas** as perguntas de uma vez via `AskUserQuestion`, agrupadas por issue (máximo 4 perguntas por chamada e 4 opções por pergunta; header = `#NNNN`). Cada pergunta oferece opções concretas + trade-off, **e sempre inclui a opção "decido depois (pular esta issue)"**. AskUserQuestion é bloqueante — não existe "detectar que o editor não respondeu"; o briefing pressupõe editor presente (limitação documentada: se ele sair no meio, a pergunta fica pendente até ele voltar). Issue respondida → promover a `elegivel` e **postar a resposta como comentário na issue** (`gh issue comment`) — é a fonte durável, visível de qualquer máquina/sessão; `plan.json` é cache. "Decido depois" → status `pulada` (sem comentário — a issue não foi prometida).

   **Registro machine-readable da decisão (#5373).** O comentário de decisão começa com o marcador gerado por `formatDecisionMarker` (`scripts/lib/issue-decisions.ts`) — `{decided_at: now() ISO, pergunta, resposta, sessao: "overnight"}` — seguido da prosa legível de sempre (a prosa não muda, o marcador é só o prefixo). Junto do comentário: (a) `route-issue` (`npx tsx scripts/route-issue.ts --issue N --track {track}` com `--reason` contendo a resposta resumida, #6196 — roteia o veredito); (b) `gh issue edit N --add-label decisao-registrada` — registra a decisão em label (não substituído pelo `route-issue`; até #6196 mergear, `gh issue edit` ainda aceitável como fallback, mas é passo **separado**, nunca substituto); (c) editar o CORPO da issue apendando `> Decidido em {data}: {resposta breve}` logo após o trecho que fazia a pergunta (ou no fim do corpo, se não houver trecho localizável) — é o corpo que a próxima varredura lê primeiro, então fechar o loop ali reduz a chance de nova pergunta mesmo sem o parsing de comentário. Sem isso o item 6 da correção (#5373) não é cumprido.

   **Registro do bloqueio de execução, quando distinto da decisão em si (#5373 item 5).** Se, ao tentar implementar uma issue já decidida (com ou sem `decisao-registrada`), a EXECUÇÃO esbarrar num impedimento novo que esta sessão não controla (acesso a painel de terceiro que a sessão não tem, guard de publicação proibindo ação real, feature gated por plano de plataforma), gravar isso como estado durável em vez de deixar a issue voltar pra fila com cara de "decisão pendente": `npx tsx scripts/route-issue.ts --issue N --track bloqueada --reason "{motivo}" --sessao overnight` — **desde #7270, o marcador `bloqueio-execucao` (`{recorded_at, motivo, condicao}` — `scripts/lib/issue-decisions.ts`) é embutido AUTOMATICAMENTE no mesmo comentário do roteamento; não construir o marcador à mão nem postar um comentário separado**, é exatamente esse 2º passo manual que 9 de 12 issues bloqueadas nunca recebiam. Bloqueio por dependência de outra issue aberta → `--depends-on N` (exige o marcador `depends-on:` #7137 já no corpo, aplica `dependencia-aberta`); qualquer outro impedimento → condição `externo` (default, sem flag extra). Status `bloqueada-externa` no `plan.json`/relatório com o `motivo` como texto. Nunca reabrir a pergunta da decisão em si — só reportar o bloqueio de execução restante.

   **Qual label aplicar — `develop-track` ou `bloqueio-execucao` (#5955).** As duas roteiam pra tracks diferentes em `classifyExecTrack`, e escolher errado enterra a issue: `bloqueio-execucao` está em `BLOCKED_LABELS` → **Bloqueada** ("nenhuma sessão destrava sozinha"); `develop-track` → **Develop** ("o editor destrava ao vivo"). A pergunta é só uma — *uma sessão `/diaria-develop` com o editor presente resolveria isto?*
   - **Sim → usar `route-issue`** (`npx tsx scripts/route-issue.ts --issue N --track develop --reason "credencial-escopo (cat. A, #5694)"` — #6196: substitui `gh issue edit N --add-label develop-track` que nunca tinha chamador no write-path; até #6196 mergear, `gh issue edit N --add-label develop-track` ainda aceitável como fallback). É o caso do guard de publicação/envio ao vivo: o guard é DESTA sessão, não do mundo. Exemplo canônico: #5140, cuja apuração exige envio Clarice ao vivo — a própria ATIVAÇÃO do teste foi feita por `/diaria-develop` 260816.
   - **Não → usar `route-issue`** (`npx tsx scripts/route-issue.ts --issue N --track bloqueada --reason "bloqueio de execução (conta de terceiro/plataforma/plano)"` — #6196: substitui `gh issue edit N --add-label bloqueio-execucao`). Conta de terceiro, feature gated por plano de plataforma, credencial que ninguém tem: nem o editor presente destrava numa sessão.

   Este parágrafo é anterior ao `develop-track` (#5948) e mandava `bloqueio-execucao` para os dois casos — sub-roteando pra Bloqueada tudo que o editor destravaria em minutos.

   **Ao REMOVER uma dessas labels, o comentário precisa dizer QUAL das duas coisas acabou (#5955).** "Esta execução foi feita" **não** é o mesmo que "a issue não precisa mais de sessão supervisionada" — se o que resta da issue ainda depende da mesma capacidade, a label FICA. Modo de falha real (#5140, 16/08/2026): a `bloqueio-execucao` foi removida porque o teste tinha sido ativado via `/diaria-develop`, mas a apuração restante exigia o mesmo envio ao vivo; sem label, a issue voltou a classificar como `overnight` e a rodada 260823 gastou dois ciclos pegando-a e devolvendo-a.

   **Plano de agrupamento no briefing (incluir na ÚNICA `AskUserQuestion`)**: formar o agrupamento do passo 6 **antes** de montar o `AskUserQuestion` (os passos 5 e 6 são logicamente concorrentes — o agrupamento precisa existir para ser apresentado no briefing). Exibir o plano de agrupamento — por unidade, incluindo:
   - issues que entram no lote (ou "solo")
   - subsistema/arquivos afetados (racional de coesão)
   - prioridade do lote (a mais alta entre suas issues)

   Exemplo de formato:
   ```
   Lote ds-email (#101, #103) — DS/email templates, sem conflito — P2
   Solo #105 — scripts/publish-facebook.ts, blast radius alto — P1
   Solo #108 — .claude/skills/diaria-overnight/SKILL.md, sem código executável — P3
   ```

   **Dobrar a aprovação do agrupamento na ÚNICA `AskUserQuestion` do briefing** (junto das `precisa-resposta` + loop-estendido), com as opções:
   - "a) aprovar agrupamento proposto"
   - "b) ajustar (diga quais issues juntar ou separar)"
   - "c) tudo solo (sem batching)"

   A aprovação acontece na janela de presença do editor — a pergunta já está no briefing, não requer interação extra. Resposta recebida → registrar `batch_approval: "editor_approved"` (opção a) ou `"editor_adjusted"` (opção b/c) em `plan.json`. **Fallback explícito → `batch_approval: "default_proposed"`** (proceder com o agrupamento proposto): aplica-se aos caminhos em que a pergunta de agrupamento **não chega a ser feita** ao editor — (a) modo `--dry-run`/auto sem gate humano; (b) Resume de um `plan.json` que não gravou o campo (rodada anterior a #2612 ou crash entre passos); (c) 0 issues `precisa-resposta` E o editor responde só ao loop-estendido sem o item de agrupamento. Nunca é um `AskUserQuestion` separado pós-briefing (Regra 1) — quando há briefing, o agrupamento entra na MESMA chamada das `precisa-resposta` + loop-estendido.

   **Pergunta padrão de loop estendido** (incluir ao final do briefing, ou como pergunta isolada quando não há `precisa-resposta`): "Incluir toda a fila desbloqueada nesta rodada? s/n". Se `s`: ativar modo loop estendido — após esgotar a fila do briefing, varrer TODA a fila open sem bloqueio (inclui analyses, scopings com direção clara, P3s, issues que entrariam como `mid-round`); **gravar `loop_estendido: true` em `plan.json` imediatamente** (antes de prosseguir). Se `n` (default): rodada limitada à fila do briefing + issues `mid-round` com direção clara que aparecerem naturalmente; gravar `loop_estendido: false` em `plan.json`. **Racional:** o loop estendido — não o depth-2 — é o principal fator de duração da rodada (rodada 260611: 47 issues / ~16h com loop estendido ativo). O briefing é o único momento de opt-in.
6. **Agrupamento em lotes (#2024, teto revisado em #2754, heurística mais agressiva em #3453)**: agrupar as `elegivel` em **lotes coesos**. Dois critérios de agrupamento, ambos válidos:
   - **(a) coesão de subsistema** — mesmo subsistema/arquivos, mesma natureza (ex: "DS/email", "playbooks Stage 4", "validator").
   - **(b) baixo-risco + baixo-blast-radius (#3453 Rec 3)** — issues pequenas e de baixo blast radius podem compartilhar 1 subagente **mesmo sem relação temática**: docs-only, comment-only, mudança isolada em 1 `.claude/agents/*.md`, tweak de 1 config. O ganho vem do **bootstrap amortizado** (`npm ci`, exploração de convenções), não da coesão editorial — então "não são do mesmo subsistema" **não** é motivo pra deixar solo se as duas são seguras e pequenas. Evidência: na rodada 260711, 5 issues solo desse perfil somaram ~630k tokens; agrupadas à taxa de lote observada teriam custado ~443k — economia da ordem de ~187k tokens **naquela rodada** (magnitude varia por rodada; direção é sólida — lote custou ~2,1× menos tokens/issue que solo). Ser propenso a propor esse tipo de lote no plano de agrupamento do briefing (passo 5).

   Critérios comuns a (a) e (b): o lote inteiro cabe numa revisão de diff única; nenhuma issue do lote conflita com outra (arquivos disjuntos). **Teto = cabe sem forçar compaction de contexto do subagente implementador, não um número fixo de issues** (#2754 — overnight otimiza tokens, não tempo; um subagente maior amortiza custo fixo de bootstrap — ler CLAUDE.md, `npm ci`, explorar convenções — sobre mais itens, e sai mais barato por item do que N subagentes solo repetindo esse bootstrap. Medido na 260630: lote de 16 sub-itens em 3 issues saiu ~26k tokens/item vs. ~114k tokens/item numa issue solo comparável). Sinal prático de teto estourado: o subagente reportar compaction no meio da sessão, ou a lista de arquivos tocados ultrapassar ~15-20. Issues grandes/arriscadas (P1, blast radius alto, migrações) ficam **solo** — o batching é só pras pequenas/médias. Cada lote vira 1 PR (`Closes #A, closes #B, ...`); como o merge fecha todas as issues do lote, o review leve do coordenador confere que o diff cobre de fato **todas** elas.
7. **Antes de escrever, resolver colisão de path (#6328 — porta pro overnight o mesmo mecanismo que o #6265/#6309 deu ao `/diaria-develop`)**: rodar `npx tsx scripts/resolve-overnight-plan-path.ts --aammdd {AAMMDD}` (`--session-id` auto-injetado, mesma convenção do item 18 de `context/overnight-dispatch-rules.md` — nunca em comando encadeado) — devolve `{ path, suffix, mode, collisions? }`. `mode: "fresh"`/`"resume"` → usar `path` normalmente (é o slot esperado, `data/overnight/{AAMMDD}/plan.json`). `mode: "derived-after-collision"` → **outra sessão overnight já tem um plano em voo neste mesmo dia** — nesta máquina OU noutra, compartilhando `data/` via OneDrive (`plan.json` existente com `session_id` diferente do desta sessão, ou sem `session_id` — plano legado tratado com a mesma cautela); usar o `path` com sufixo devolvido (`data/overnight/{AAMMDD}{suffix}/plan.json`, ex: `260826b`) em vez de sobrescrever o alheio, e mencionar a colisão no relatório final (Fase 2). **Gravar `session_id`** (do registro já feito no passo 1 — o `session_id` desta sessão) **e `machine_id`** (via `npx tsx scripts/lib/machine-id.ts`) **no nível raiz de `plan.json` a partir deste primeiro write** — mesmo padrão que o `/diaria-develop` já segue desde #6265/#3033. **Todo re-write subsequente do `plan.json` desta rodada usa o MESMO `path` resolvido aqui** — não re-resolver a cada write. **A checagem de colisão roda tanto aqui quanto no passo 0 (#6328)** — não é exclusiva da 1ª escrita: o passo 0 já resolveu o mesmo `path` antes de decidir se é resume; esta chamada aqui é a confirmação final antes do write em si (idempotente, mesmo resultado se nada mudou entre os dois passos).

   **Plano da rodada**: gravar em `data/overnight/{AAMMDD}/plan.json` (ou no `path` sufixado, se `derived-after-collision`). **`{AAMMDD}` = data local de início da rodada, fixada AQUI e relida de `plan.json` em todas as fases seguintes — nunca recomputada de today()** (toda rodada cruza a meia-noite). **`started_at` é o timestamp ISO 8601 REAL capturado no passo 1 (#3841) — NUNCA a string `{AAMMDD}`/`{AAMMDD}{sufixo}` do diretório.** Os dois campos coexistem com papéis distintos: `{AAMMDD}` (do path do diretório) é só o rótulo-de-dia que nomeia onde o plano mora; `started_at` (dentro do JSON) é a identidade temporal real da rodada — usada pelo painel `/rodada` do Studio pra ordenar TODAS as rodadas (overnight + develop) numa sequência cronológica única, e pela comparação `created_at >= started_at` do passo 4 (que só funciona se `started_at` for de fato uma data).
   ```json
   {
     "started_at": "2026-07-21T14:34:00Z", "base_sha": "a67520a3f...", "review": null,
     "session_id": "{session_id desta sessão, via session-registry.ts}",
     "machine_id": "{hostname, via scripts/lib/machine-id.ts}",
     "rescans_done": 0, "findings_depth": 0, "review_1_5b_has_p2": false, "loop_estendido": false,
     "bugs_only": false,
     "priority_filter": null,
     "batch_approval": "editor_approved | editor_adjusted | default_proposed",
     "stall_events": [],
     "resume_state": null,
     "preempted_by": null,
     "preempted_at": null,
     "preempted_pending": null,
     "issues": [{
       "number": 123, "priority": "P1", "status": "elegivel", "in_round": true,
       "batch": "ds-email | null (solo)", "pr": null,
       "briefing": "resposta do editor, se houve",
       "source": "initial | mid-round | finding-depth-1 | finding-depth-2",
       "timeline": {
         "dispatch": "ISO",
         "pr_opened": "ISO",
         "fix_iteration_1": "ISO",
         "fix_iteration_2": "ISO",
         "ci_green": "ISO",
         "merged": "ISO | null",
         "draft": "ISO | null",
         "pulada": "ISO | null"
       }
     }]
   }
   ```
   **`session_id`/`machine_id` (#6328, mesmo padrão do `/diaria-develop` desde #6265/#3033).** Gravados no nível raiz a partir do 1º write deste passo — `session_id` é o discriminador que o passo 0 usa pra decidir "este plano é meu ou é de outra sessão em voo?" (o mesmo valor já registrado no `session-registry` do passo 1); `machine_id` (hostname, via `scripts/lib/machine-id.ts`) é metadado adicional, útil pra quem inspecionar o plano depois (relatório, painel `/rodada`) saber de qual máquina ele veio — o resolver do passo 0/7 em si discrimina só por `session_id` (já é único por sessão, independente de máquina; ver docblock de `scripts/lib/plan-path-resolution.ts`). `plan.json` gravado ANTES do #6328 não tem nenhum dos dois campos — tratado com a mesma cautela de "plano legado" que o resolver já aplica a `session_id` ausente (nunca "livre pra tomar"), sem regressão.

   **`goal` (#5706) não faz parte deste schema inicial** — o exemplo acima não tem esse campo porque o `plan.json` do overnight nunca gravou `goal.target_set`/`goal.tiers` (isso é exclusivo do develop). A partir da primeira execução bem-sucedida do gate do passo 0.5, o `plan.json` passa a ter `goal: { last_convergence_scan: { at: "ISO", novas_encontradas: N } }` — criado do zero pelo gate, não um campo pré-existente sendo populado; nenhum outro subcampo de `goal` (`target_set`, `tiers`, `policy`, etc.) é gravado aqui.

   `bugs_only` (#3375) reflete se `--bugs` foi passado nesta rodada — `true`/`false`, gravado no passo 3 e relido em cada re-scan/mid-round da Fase 1. Ausente em plan.json legado → `false` (fail-open, comportamento pré-#3375). `priority_filter` (#3499) reflete se `--priority` foi passado nesta rodada — array de labels (ex: `["P2"]`, ou `["P0", "P1"]`), ou `null` se a flag não foi usada — gravado no passo 3 e relido em cada re-scan/mid-round da Fase 1, mesmo ciclo de vida de `bugs_only`. Ausente em plan.json legado → `null` (fail-open, comportamento pré-#3499). `base_sha` = o hash REAL capturado no passo 1 (nunca texto descritivo). `review` é atualizado pela Fase 1.5 (`null` → `"done (depth {N})"` / `"skipped: {motivo} (depth {N})"`); plan.json legado pode conter `"done"` (sem depth) — ver tabela do Resume. `review_1_5b_has_p2` é gravado pela Fase 1.5 ao finalizar o review 1.5b (`findings_depth == 1`): `true` se ≥1 finding de severidade P2 ou superior foi identificado; `false` caso contrário. É o guard da tabela do Resume para mini-rodada 2 — ver Regra 2. `batch_approval` (#2612) registra a fonte da decisão de agrupamento: `"editor_approved"` (editor aprovou na opção a do briefing), `"editor_adjusted"` (editor ajustou na opção b/c), `"default_proposed"` (fallback: editor não respondeu a essa parte do briefing). Análogo ao `source` do consent do Stage 5 — auditável, rastreável. `pr` recebe o número do PR no desfecho da issue (Fase 1 passo 5) — é a fonte pós-compaction do relatório. `rescans_done` conta quantos re-scans de issues novas já ocorreram após esgotar a fila principal (escopo: fila principal apenas — mini-rodadas da Fase 1.5 nunca incrementam este contador). **Desde #5272 é contador puro de observabilidade: sem cap, nenhuma lógica de parada lê o valor** — só a Fase 2 reporta. `findings_depth` registra o nível atual da cadeia de re-entrada de findings (0 = fila principal, 1 = mini-rodada 1 pós-1.5, 2 = mini-rodada 2 pós-1.5b; nunca excede o depth limit da tabela do passo 0). `source` indica a origem de cada issue no plano: `initial` (varredura Fase 0), `mid-round` (nova durante a Fase 1), `finding-depth-1` ou `finding-depth-2` (criada como `session-finding` e re-entrou). Status possíveis: `elegivel`, `elegivel_especial` (#3072, review do #3071 — EPIC deliberadamente deferido: fecha só quando a issue-filha mergear, nunca antes; usar exatamente esse literal, com underscore — os demais status usam hífen mas este é a exceção documentada; `scripts/overnight-statusline.ts` trata como terminal pra fins de barra/relatório mesmo sem `timeline` preenchido, já que a issue nunca chega a ser despachada; grava `in_round: false`, #3131 — nunca entrou na fila de trabalho da rodada, não deve contar no denominador da barra; **aplicar a label `epic-guarda-chuva`** — `gh issue edit N --add-label epic-guarda-chuva` <!-- route-issue-exempt: documentação do schema do plan.json (status elegivel_especial), citação de passagem, não instrução de write-path --> — se ainda ausente, #5968: é ela que faz `classifyExecTrack`/Triagem do Studio classificar a EPIC como Fora de rodada em vez de Overnight, mesmo racional do `trade-off-real`/`external-blocker` acima), `pulada` (motivo: `sem-resposta` | `bloqueio-externo` | `requer-sessao-local` | `ambigua` | `not-this-week` | `fora-do-escopo` | `sem-direcao-acionavel` (#5968 — ver bullet do passo 4 abaixo) — **`rescan-limit` saiu do enum em #5272**, junto com o cap que o produzia), e os terminais da Fase 1: `mergeada`, `draft-ci-vermelho`. `in_round` (#3131, ver passo 4) marca se a issue de fato entrou no escopo de trabalho desta rodada na classificação inicial: `true` para `elegivel`/`precisa-resposta` (mesmo que puladas depois, mid-rodada ou no briefing); `false` para issues excluídas ANTES de entrar (`bloqueada-externa`, `requer-sessao-local`, `not-this-week`, `fora-do-escopo`, `ambígua/trade-off-real`, todas classificadas no passo 4) e para o EPIC `elegivel_especial` (nunca despachado). Ausente em plan.json legado → `true` (fail-open). `renderOvernightBar` filtra por `in_round !== false` antes de calcular `done`/`total`. `timeline` registra os timestamps ISO por transição de cada unidade — os marcos omitidos ficam ausentes (não null); campo ausente = transição não ocorreu ou rodada anterior ao #2099. Lotes de N issues compartilham o mesmo `timeline` (o coordenador grava no objeto da issue representante — a que tem `dispatch`).

   **Campos de instrumentação adicionais do plan.json (#2379, #2380, #2382):**

   - **`stall_events`** (array, #2379): lista de eventos de stall passivo detectados durante a rodada. Cada entrada tem a forma `{ "at": "ISO", "reason": "rate_limit | context_exhaustion | standby | ci_timeout | unknown", "resumed_at": "ISO | null" }`. O coordenador acrescenta uma entrada sempre que detecta >45 min sem progresso em unidade em andamento, **antes** de emitir o halt banner. Começar a rodada com `stall_events: []`. Gravar `resumed_at` quando a rodada é retomada após o stall.

   - **`resume_state`** (objeto ou null, #2380): gravado toda vez que a rodada é interrompida (PC desligado, colisão editorial, deadline). Estrutura:
     ```json
     {
       "phase": "fila_principal | mini_rodada_N | review_1_5x | relatorio",
       "pending_issues": [123, 456],
       "next_action": "dispatch_mini_rodada_2 | iniciar_review_1_5b | ...",
       "interrupted_reason": "pc_desligado | colisao_editorial | deadline | unknown"
     }
     ```
     Valores de `phase`: `fila_principal` (Fase 1 em andamento), `mini_rodada_N` (mini-rodada N da Fase 1.5), `review_1_5x` (review consolidado da Fase 1.5), `relatorio` (Fase 2). `pending_issues` lista os números de issue sem status terminal ao momento da interrupção. Na retomada (passo 0 Resume), se `resume_state` não for null, usar seus campos para retomada determinística em vez de inferir do texto livre — confirmar ao editor `"Retomando: phase={phase}, pending={pending_issues}"` antes de prosseguir. Após concluir a rodada normalmente, gravar `resume_state: null`.

   - **`preempted_by`**, **`preempted_at`**, **`preempted_pending`** (#2382): gravados quando a rodada é encerrada por precedência editorial (guard de colisão com a manhã, Fase 1 passo 1). `preempted_by` recebe `"edicao_editorial"` (único valor atual); `preempted_at` recebe o timestamp ISO do encerramento; `preempted_pending` recebe a lista de números de issue sem status terminal. Rodadas encerradas normalmente têm esses campos como `null`. A política de precedência editorial é: a edição matinal (agendada pelo Task Scheduler — ver #2089) tem prioridade sobre a rodada overnight; o guard de colisão (detecção via `find-current-edition.ts`) aplica esta política automaticamente.
8. Confirmar o plano com o editor (a tabela completa já foi impressa no passo 4.5; os lotes e a aprovação do agrupamento já foram incluídos no briefing do passo 5; aqui confirmar o estado final antes de entrar na Fase 1). **Se 0 elegíveis, dizer isso agora e encerrar aqui** — é a última chance do editor destravar algo respondendo mais perguntas; não rodar uma noite vazia. Com `--dry-run`, parar aqui (sem comentários postados). A aprovação do agrupamento já foi resolvida no passo 5 (via AskUserQuestion do briefing, ou via fallback `default_proposed`) — **nunca** repetir a pergunta aqui (Regra 1); usar o que está registrado em `batch_approval` do plan.json. **Armar o guard mecânico da Regra 1 imediatamente antes de entrar na Fase 1 (#4450):** (não aplicável a `--dry-run`, que já parou no passo anterior) rodar `npx tsx scripts/overnight-session-marker.ts --phase autonomous` — atualiza `data/overnight/.active-session-{hostname}.json` (gravado com `phase: "briefing"` no passo 1) para `phase: "autonomous"`, o gatilho que arma `.claude/hooks/block-askuserquestion-overnight-autonomous.mjs` (`PreToolUse` sobre `AskUserQuestion`) a partir deste ponto — qualquer `AskUserQuestion` na Fase 1 em diante (incluindo re-scans, mini-rodadas e Fase 1.5) é negado mecanicamente, mesmo que a chamada seja anômala e não passe por nenhum raciocínio que o reforço de prompt da Regra 1 poderia interceptar (ver Regras abaixo). Este é literalmente o último passo da Fase 0 — nada mais roda depois dele antes da Fase 1.

## Fase 1 — Loop de resolução

**Uma ONDA de unidades por iteração (#6299)** — não mais uma unidade por vez. Cada **unidade de trabalho** é uma issue solo ou lote (#2024); a onda é composta dentro do passo 2 abaixo (teto 3 concorrentes) e as unidades são ordenadas por prioridade (P0 > P1 > P2 > P3 — prioridade do lote = a mais alta entre suas issues; empate → número menor = mais antiga), de modo que a onda seguinte só começa a ser composta quando a corrente esvazia. **No início de cada iteração, reler `plan.json`** — após compaction de contexto, ele é a única fonte confiável do briefing e dos status. **Os passos 2–3 abaixo descrevem o ciclo de UMA unidade da onda** (claim → dispatch no passo 2; fixer → review → CI → merge → cleanup no passo 3) — com a onda, até 3 desses ciclos estão em voo simultaneamente e cada unidade avança no seu próprio ritmo, exceto pelo **merge**, que é serial dentro do passo 3. A cada iteração:

1. **Re-checar a fila**: `gh issue list --state open --limit 200 --json number,title,labels,state,createdAt` (sem `body` — corpo só de issues novas, via `gh issue view {N} --json body`; o coordenador precisa ficar enxuto). Issue fechada externamente → marcar `pulada` no plano e não trabalhar. **Issues novas criadas durante a rodada** (qualquer prioridade, qualquer autor — humano ou automação) com direção clara no corpo entram como `elegivel` com `source: "mid-round"`, com as seguintes exceções: (a) issue ambígua → **não** fazer pergunta ao editor; postar comentário explicando o que falta na direção + status `pulada` motivo `ambigua` (a questão vira pergunta do briefing da próxima rodada — Regra 1); (a.1) **com `--bugs` (#3375) ativo**: issue nova sem label `bug` → não entra (mesmo critério do passo 3 — fora de escopo, não é skip por bloqueio); (a.2) **com `--priority` (#3499) ativo**: issue nova cujo label de prioridade ∉ conjunto passado → não entra (mesmo critério do passo 3 — fora de escopo, não é skip por bloqueio); (b) issue com label `external-blocker` ou `not-this-week` → `pulada` com motivo correspondente, **comentar na issue confirmando que esta rodada a viu e a manteve fora** (dedup contra comentário overnight equivalente — mesma lógica do motivo `ambigua` acima, generalizada em #5777: toda `pulada` mid-round comenta, não só `ambigua`); (c) issue com label `session-finding` **e `created_at >= started_at` do `plan.json`** (= gerada pela própria rodada) → **não entra agora** (fluxo da Fase 1.5 cuida da re-entrada controlada com depth limit); issue com label `session-finding` mas `created_at < started_at` (= finding de rodada anterior, pendente) → trata como `mid-round` normal. **Re-varredura de convergência — SEM CAP (#5272; escopo: fila principal apenas, mini-rodadas da Fase 1.5 não incrementam):** cada vez que a fila elegível da fila principal é esgotada (sem itens pendentes), fazer um re-scan para capturar issues novas **e** rodar `npx tsx scripts/check-block-staleness.ts --plan data/overnight/{AAMMDD}/plan.json` (#6259 — passo 0.9 abaixo é o gate final antes do relatório, mas rodar aqui também evita declarar a fila esgotada com bloqueio já caduco: PR em voo mergeado/fechado, claim de sessão vizinha liberado, ou label `bloqueio-execucao` removida — nenhuma dessas transições cria issue nova, então "0 issues novas" sozinho **não** é convergência real). **Gate MECÂNICO obrigatório antes de declarar convergência (#6435).** "Fazer um re-scan" deixou de ser só prosa — rodar `npx tsx scripts/check-overnight-fila-convergence-gate.ts --plan data/overnight/{AAMMDD}/plan.json` **sempre** que a fila parecer esgotada, ANTES de seguir pra Fase 1.5. Achado ao vivo que motivou (rodada 260827b): o coordenador declarou "fila esgotada" e seguiu direto pra Fase 1.5 sem re-scan nenhum — só pegou a issue nova (#6431) porque o editor perguntou diretamente; sem essa pergunta, a issue nunca teria entrado no relatório. `exit 0` = convergência CONFIRMADA (`gh issue list` fresco comparado contra `goal.target_set`/`tiers`/`issues[]` não achou nada fora do conhecido) → seguro declarar a fila esgotada e avançar. `exit 1` = issue(s) nova(s) listada(s) → classificar cada uma (`elegivel` com `source: "mid-round"`, ou `pulada` com motivo do vocabulário fechado de `scripts/lib/overnight-plan-motivo.ts`) e rodar o gate de novo — **não avançar pra Fase 1.5 enquanto o gate não sair `exit 0`**, mesma disciplina dos gates 0.5-0.9 da Fase 2 (este é o gate irmão deles, só que no MEIO do loop em vez do fim — reusa a mesma lógica pura de `scripts/lib/state-changed-tracker.ts`, não duplica). `gh` indisponível → fail-soft (#738): o gate sai `exit 0` mas com aviso explícito "não verificado" — tratar como convergência NÃO confirmada (nunca como "nenhuma issue nova" de verdade), e repetir a tentativa no próximo ponto natural do loop. **Aceitar todas as novatas com direção clara** e trabalhá-las; ao esgotar de novo, re-scan de novo — **repetir enquanto cada re-scan devolver issue nova sem status terminal** — bloqueio caducado conta como issue nova para este fim (#6259). `rescans_done` continua sendo incrementado a cada re-scan, mas como **contador puro de observabilidade** (reportado na Fase 2): nenhuma decisão de parada lê esse valor, e um `plan.json` retomado com `rescans_done >= 2` **não** encerra a rodada por causa disso. **O status `pulada` motivo `rescan-limit` deixou de existir** — não gravar esse motivo em nenhuma circunstância. **O cap K=2 foi eliminado (#5272)** porque matava uma noite produtiva por aritmética: a 3ª fila esgotada encerrava a rodada mesmo com issue nova e trabalhável esperando — e fila que volta a encher é justamente o sinal de que há trabalho real, não de que a rodada deveria parar. **O que garante terminação no lugar dele** (dois mecanismos, nenhum deles um contador): (i) **anti-livelock**, logo abaixo — issue com status terminal nunca é re-escolhida, então um re-scan só devolve trabalho se houver issue genuinamente nova, e a rodada converge sozinha no primeiro re-scan sem novidade (caso comum); (ii) **guard de colisão com a manhã**, também abaixo — a pipeline editorial preempta a rodada. **NÃO existe teto de relógio (decisão do editor, 260814):** um teto de 09:00 BRT chegou a entrar junto com o #5272 e foi retirado no mesmo dia — a rodada não tem deadline de tempo (#2039) e não deve ganhar um pela porta dos fundos. **Consequência aceita, registrada aqui de propósito:** em dia sem edição — quando o guard (ii) nunca aciona — uma rodada que siga recebendo issues novas continua trabalhando sem nenhum limite de horário. É o comportamento pedido: o freio é a fila secar. **Não reintroduzir teto nem contador sem decisão explícita do editor** (esta é a segunda vez que um limite mecânico é retirado desta seção; a próxima sessão que achar que isso é um bug está lendo a decisão, não um esquecimento). **Alinhado com o `/diaria-develop`** (#4319 já havia eliminado o mesmo cap de lá): a diferença que resta é quem segura a sessão — lá o editor presente, aqui os dois mecanismos acima. **O cap de `findings_depth` (2) NÃO muda** — continua valendo, ver Fase 1.5. **Só issues sem status terminal no `plan.json` são candidatas** — uma issue que já virou `draft-ci-vermelho` ou `pulada` nunca é re-escolhida na mesma rodada (anti-livelock). **Guard de colisão com a manhã**: se uma edição diária estiver em curso (`npx tsx scripts/lib/find-current-edition.ts` retorna candidato ou `data/editions/` de hoje/amanhã ganhou arquivos novos), encerrar a Fase 1 após a unidade corrente e ir pra 1.5/relatório — a pipeline editorial tem precedência. Quando este guard acionar, gravar `preempted_by: "edicao_editorial"`, `preempted_at: now()` e `preempted_pending: [lista de issues sem status terminal]` em `plan.json`, e emitir no run-log:
   ```bash
   npx tsx scripts/log-event.ts --edition {AAMMDD} --agent overnight --level info \
     --message "preempted" \
     --details '{"preempted_by":"edicao_editorial","preempted_pending":[123,456]}'
   ```
2. **Antes do dispatch, checar claim de outra sessão (#5156 item 3):** `npx tsx scripts/lib/session-registry.ts is-claimed --issue {N}` (`--session-id` auto-injetado, exclui a própria sessão da checagem) pra CADA issue da unidade — se `claimed: true`, outra sessão ativa (overnight de outra máquina, ou `/diaria-develop` supervisionado) já está trabalhando essa issue AGORA; pular esta unidade (status `pulada`, motivo `claimed-por-outra-sessao`, sem comentário na issue — não é bloqueio, é corrida evitada) e seguir pra próxima da fila. Registro vazio (nenhuma outra sessão adotou o registro ainda) nunca bloqueia — `claimed` sempre `false`. Após confirmar que nenhuma issue da unidade está claimed, reivindicar todas de uma vez: `npx tsx scripts/lib/session-registry.ts claim-issue --kind overnight --issue {N}` (uma chamada por issue do lote) — **antes** do dispatch abaixo, não depois. **Chamar sempre como comando standalone, nunca dentro de `&&`/`;`/pipe/heredoc** (`context/overnight-dispatch-rules.md` item 18, #5751) — comando encadeado quebra a injeção automática de `--session-id`, e o erro resultante precisa ser tratado como falha real, nunca ignorado. **Consultar `conflicts` antes de abrir o worktree (#6168 Parte C, ponto 1):** `npx tsx scripts/lib/session-registry.ts conflicts --paths {arquivos da unidade, separados por vírgula} --branch {branch atual}` — standalone, mesma disciplina de injeção do `is-claimed` acima. `exit 1` = sobreposição real com peer VIVO. O `is-claimed` responde por ISSUE; este responde por ARQUIVO, que é o buraco 2 da #6168: duas sessões em issues DIFERENTES — sem colisão de claim nenhuma — editando o mesmo arquivo. É **consulta**, nunca aquisição: não cria arquivo nem reserva nada. Sobreposição com peer alcançável → avisar via `SendMessage` antes de dispatchar; peer inalcançável → registrar em `plan.json` e seguir (silêncio nunca trava a rodada). **Check de SAÍDA (ponto 3)**: na Fase 2, antes do `end`, conferir `git status --porcelain` limpo e `conflicts --branch {atual}` sem `branch-drift` — as duas evidências mais caras da #6168 são de saída (registro não encerrado segurando claims de trabalho já mergeado; e trabalho não commitado deixado em `master` num checkout compartilhado, com a rodada reportando "concluído").

**Compor a onda antes de dispatchar (#6299) — o overnight deixou de ser serial.** Até esta issue, cada unidade era despachada sozinha e a seguinte só começava depois do desfecho da anterior. A justificativa era blast-radius ("sem supervisão, paralelo elevaria o risco"), e ela **não se verifica**: o que torna o paralelo seguro é um conjunto de mecanismos determinísticos — worktree isolado por unidade, cluster de conflito, fusão de colidentes (#4319), `is-claimed` check-and-set (#6236), `conflicts` por arquivo (#6168), merge lock, Gate 2 determinístico e o guard do #5716 — e **nenhum deles exige o editor presente**. **O fleet review pré-merge (#4383) NÃO entra nesta lista, de propósito:** ele é mecanismo do `/diaria-develop`, cujas branches `develop/*` resolvem `max` no hook; aqui as branches `overnight/*` resolvem `low` (1 agente) por construção, e o review consolidado da Fase 1.5 entrega o equivalente a esse 1 agente por PR. Creditar ao overnight um mecanismo que ele não executa enfraqueceria justamente o argumento que este parágrafo faz — o paralelo aqui se sustenta pelos mecanismos de ISOLAMENTO acima, não por profundidade de review. A supervisão humana cobre o Gate 1 cat. C e o Gate B, que são outra coisa e continuam fora do escopo desta skill.

Antes do fan-out, rodar a mesma análise de cluster que o `/diaria-develop` já usa (ver "Paralelização segura" lá — mapear arquivos por issue a partir do corpo + grep dos símbolos citados): issues que colidem em arquivo **fundem numa unidade só** (1 worktree, 1 branch, 1 PR, `Closes` por issue); as que não colidem são singletons. **Onda = 1 unidade por cluster fundido + todos os singletons.**

**Teto de concorrência: 3** — deliberadamente menor que os 6 do develop. Não é por blast-radius de colisão (esse a fusão de cluster já elimina), é porque o overnight roda desassistido: com 3, uma rodada que degrade de forma imprevista queima menos trabalho antes de alguém olhar. Ajustável com medição, não com palpite.

**O que NÃO muda, e é o ponto:**
- **merge continua estritamente serial** — `master` recebe um squash por vez, sob `merge-lock-acquire`, e o próximo só depois do `git pull` do anterior;
- **unidade que exigiria Gate B (blast-radius) continua fora do overnight** — sem editor, ela é pulada como sempre. O paralelismo não abre essa porta;
- **`heartbeat --active-worktrees N`** deixa de ser cosmético: com o overnight abrindo múltiplos worktrees, é ele que impede a soma real na máquina de estourar quando overnight e develop rodam juntos. **Os dois call sites são obrigatórios e estão escritos literalmente onde acontecem** — no passo 2 (logo após dispatchar a onda) e no passo 3 (logo após o cleanup do worktree de cada unidade). Antes do #6299 este comando não tinha call site em skill nenhuma (#5156 item 6): declarar a intenção aqui sem escrevê-la nos dois pontos deixaria `active_worktrees` ausente na prática, que é exatamente o estado que esta issue fecha.

**Dispatchar um subagente implementador por unidade da onda** — issue solo ou lote inteiro (#2024) — (`Agent`, `subagent_type: "general-purpose"`, `isolation: "worktree"` — nunca um agente especializado do repo: eles têm toolset restrito e não conseguem commitar/pushar).

**Imediatamente após dispatchar a onda, publicar a contagem de worktrees (#6299 — call site de ABERTURA, obrigatório):** rodar `npx tsx scripts/lib/session-registry.ts heartbeat --kind overnight --active-worktrees {N}`, onde `{N}` = worktrees desta sessão vivos agora. **Standalone, nunca em `&&`/pipe/heredoc nem dentro de um bloco de código multi-linha** (`context/overnight-dispatch-rules.md` item 18) — encadear quebra a injeção do `--session-id` e a publicação vira no-op silencioso; é por isso que este comando aparece aqui em span inline e não em bloco cercado (`test/skill-chained-session-command-guard-6232.test.ts` reprova o bloco, pelo mesmo critério que o hook usa). Sem esta chamada, `list-active` soma zero pra esta sessão e uma `/diaria-develop` concorrente abre os 6 dela por cima dos 3 daqui.

O prompt do subagente inclui, obrigatoriamente:
   - **instrução literal de ler `context/overnight-dispatch-rules.md` no início da própria sessão (#3453 Rec 4)** — é o checklist canônico das regras obrigatórias (guard de publicação, convenção de branch, bootstrap, disciplina de testes #2959, #633, marcador `no-regression-test`, self-review #2038, não-executar-review-multi-agente). Citar o **path** no prompt em vez de reproduzir o texto completo das regras encurta o prompt de dispatch do coordenador (o custo cresce ~19× por noite). Os bullets abaixo continuam sendo a fonte que espelha esse arquivo — mantê-lo em sincronia ao editar qualquer regra de dispatch;
   - corpo de TODAS as issues da unidade + respostas do briefing (lidas de `plan.json`);
   - regras do repo: #633 (bugfix exige teste de regressão), convenções de commit/PR do CLAUDE.md;
   - **guard de publicação**: editar código de publisher é ok; **EXECUTAR é proibido** — nunca rodar `scripts/publish-*`, `clarice-schedule-sends`, `clarice-import-*`, `close-poll` ou qualquer script que toque Beehiiv/LinkedIn/Facebook/Brevo ao vivo, nem em "teste";
   - **convenção de branch (#3321) — instrução literal, não implícita**: "nomeie a branch como `overnight/fix-{issue}-{slug}`" (issue solo) ou `overnight/batch-{slug}` (lote). O prefixo `overnight/` é o sinal preferido (mais barato — sem I/O de disco) que o hook `.claude/hooks/pr-create-review.mjs` (`resolveEffort`) usa pra decidir `low` vs `max` effort no `/code-review` pós-`gh pr create`, e o cleanup de fim de rodada (Fase 1 passo 3) assume esse padrão pra deletar a branch remota — **causa raiz do incidente 260710 (#3321)**: essa convenção estava documentada mais abaixo neste mesmo passo (parágrafo narrativo, não nesta lista "obrigatoriamente") e nenhum PR da rodada a seguiu, então todo PR pagou o fleet `max` completo (10 finders + verify + sweep) em vez do `low` token-otimizado. **Não é mais o único guard** (#3322: o hook também detecta uma sessão overnight ativa nesta máquina via marker dedicado, independente do naming — ver passo 1 abaixo), mas seguir a convenção continua correto e evita o `[aviso: ...]` que o hook anexa quando o naming diverge apesar da sessão ativa, e é o que o cleanup do passo 3 usa pra deletar a branch remota corretamente;
   - bootstrap do worktree: **primeiro passo é `npm ci`** (worktree novo não tem `node_modules/` nem a junction `data/`); testes = **`npx tsc --noEmit`** (typecheck) **+ só os arquivos de teste afetados/novos** (`npx tsx --test test/<arquivo-tocado>.test.ts test/lib-boundary.test.ts`) — **NUNCA a suíte completa `npm test` local (#2959)**: o CI já roda a suíte inteira e é o gate autoritativo antes do merge (#636/#633); rodar os ~11k testes/~3min de novo no worktree é redundante com o CI e é justamente o comando que dispara o auto-background do harness;
   - **`npx tsc --noEmit` e os testes afetados rodam SEMPRE em FOREGROUND, aguardando o resultado antes de retornar — NUNCA em background (#2896).** Por serem rápidos (~10-30s) eles não disparam o auto-background do harness — ao contrário da suíte completa, que **em TODOS os ~11 subagentes das rodadas 260703+260704** disparou o padrão: harness auto-backgrounda o comando longo mesmo "rodado em foreground na mesma chamada" → o subagente entra num Monitor-loop esperando notificação em vez de retornar → stall (260702-r2: coordenador ~8h parado) ou, na melhor hipótese, 1 round-trip inteiro desperdiçado pra o coordenador reperguntar o resultado (#2959 ataca a causa raiz removendo o próprio gatilho, não só mitigando o stall). **Se um full-run local for genuinamente necessário** (raro — mudança ampla em `scripts/lib/` com risco cross-file que os testes afetados não cobririam), pipe por `| tail -40` para forçar o resultado a voltar na própria chamada em vez de ser silenciosamente backgroundado;
   - **self-review obrigatório antes de retornar (#2038) — tratado como ETAPA DE LISTAGEM**: após o `gh pr create`, o subagente faz UMA passada adversarial no próprio `git diff` contra a(s) issue(s) + briefing, checando: o diff cobre TODOS os pontos da issue (não só os fáceis)? Sobrou referência órfã de refactor (grep pelos símbolos renomeados)? O arquivo carrega (`npx tsc --noEmit` se coberto pelo tsconfig, senão import smoke via tsx)? O cenário REAL da issue tem teste (não só a aritmética adjacente)? **O output esperado do self-review são os findings listados — comentários inline no PR são o canal correto** (não fixes imediatos). Finding grande demais para o coordenador aplicar direto → comentário inline no PR detalhando a mudança necessária. Racional empírico (260610/260611): subagentes consistentemente postam findings como comentários em vez de corrigir — em vez de lutar contra o padrão, o fluxo de 2 agentes o codifica. **NUNCA fazer `AskUserQuestion` durante o self-review** — Regra 1.
   - **se um hook pós-`gh pr create` exigir code-review multi-agente, NÃO executar** — o self-review acima é a resposta; anotar no body do PR e retornar (subagente não pode dispatchar Agent, #207; o review pesado roda UMA vez, consolidado, na Fase 1.5).
   - **PROIBIÇÃO EXPLÍCITA (#6762): NUNCA rode `gh pr merge`, `gh pr merge --auto`, ou qualquer variante — merge é ação EXCLUSIVA do coordenador, mesmo com CI já verde.** Após o self-review, o passo é retornar — nunca esperar CI, nunca checar `gh pr checks`, nunca mergear (`context/overnight-dispatch-rules.md` item 11).
   - **`Closes #NNNN` é obrigatório POR ISSUE totalmente resolvida no lote — nunca ambíguo entre "esqueci" e "decidi que não fecha" (#5010).** Confirmado ao vivo na rodada 260811: 4 de ~13 PRs saíram sem nenhum `Closes` no body, deixando issues já resolvidas presas abertas até uma triagem manual na rodada seguinte — custo que se repete a cada ocorrência. Regra: para CADA issue do lote, o PR body precisa conter OU `Closes #NNNN` (issue totalmente resolvida por este diff) OU a declaração explícita `REFS #NNNN, NÃO CLOSES ({motivo})` (escopo genuinamente parcial — ex: issue tinha mais de uma frente e só uma foi atacada nesta unidade). O padrão correto já existe no repo (PR #4969, lote geo-baratos) — seguir esse exemplo. Nunca deixar uma issue do lote sem nenhum dos dois marcadores.
   O subagente implementa, roda `npx tsc --noEmit` + os testes afetados (nunca a suíte completa local, #2959), commita em branch **`overnight/fix-NNNN`** (solo) ou **`overnight/batch-{slug}`** (lote) com `(#NNNN)` / `(#A, #B, ...)` no título, push, abre PR com `Closes #NNNN` para cada issue totalmente resolvida (ou `REFS #NNNN, NÃO CLOSES ({motivo})` para as que ficam com escopo remanescente — nunca omitir os dois marcadores pra nenhuma issue do lote, #5010), faz o self-review listando findings como comentários inline no PR, e retorna: o número do PR **+ a linha "self-review: N findings"** — o coordenador usa isso para decidir se despacha o agente fixer (0 findings em diff não-trivial é sinal de passada rasa, não de perfeição).

   **Ainda dentro deste passo 2 — emissão de timestamp `dispatch`**: imediatamente após dispatchar o subagente (antes de aguardar a resposta), o coordenador registra `timeline.dispatch = now()` na issue (ou nas issues do lote, em todas) em `plan.json`, e emite no run-log:
   ```bash
   npx tsx scripts/log-event.ts --edition {AAMMDD} --agent overnight --level info \
     --message "dispatch" \
     --details '{"unidade": "#NNNN | lote {slug}", "issues": [123, 456], "pr": null}'
   ```
   **Fallback wake determinístico (#2896) — agendar imediatamente após o dispatch**: na mesma respiração, o coordenador chama `ScheduleWakeup` com delay ~1200s (20 min) para a unidade recém-dispatchada. Ver detalhe completo na seção "Stall passivo — duas camadas" abaixo (terceira sub-camada). O mesmo agendamento se repete sempre que o coordenador **resume** um subagente via `SendMessage` (mesmo passo 3, agente fixer, ou retomada pós-CI-vermelho) — todo dispatch ou resume reagenda o próprio wakeup.
3. **Agente fixer + revisar, esperar CI e mergear** (coordenador, nunca o subagente implementador):

   **Agente fixer (fluxo de 2 agentes por unidade)**: ao receber o retorno do subagente implementador, o coordenador verifica o self-review:
   - **Findings acionáveis (>0)**: dispatchar um segundo agente `general-purpose` no **mesmo branch** com o prompt: "Leia os comentários inline no PR #{N} (branch `{branch_name}` — `overnight/fix-NNNN` para issues solo, `overnight/batch-{slug}` para lotes), aplique cada finding acionável no código, rode `npm test`, e faça re-push. Não abra novo PR — o PR #{N} já existe. Retorne: 'fixer: M findings aplicados, npm test OK/FAIL'". O coordenador substitui `{branch_name}` pelo branch real da unidade antes de dispatchar. Worktree do fixer = mesmo worktree do implementador (checkout da branch existente, não novo worktree).
   - **Findings triviais (≤3 linhas, cosméticos — ex: typo em comentário, espaçamento)**: o coordenador pode aplicar diretamente no worktree do implementador sem dispatchar o fixer.
   - **Zero findings**: prosseguir direto para o review leve + CI.

   **Review leve antes do merge**: ler o diff do PR (`gh pr diff {N}`) e sanity-checkar contra a issue + briefing — substitui o review pesado pulado pelo subagente; se o diff parecer errado, tratar como CI vermelho (tentativa de fix).
   - Ao receber o número do PR do subagente, registrar `timeline.pr_opened = now()` em `plan.json` e emitir:
     ```bash
     npx tsx scripts/log-event.ts --edition {AAMMDD} --agent overnight --level info \
       --message "pr_opened" \
       --details '{"unidade": "#NNNN | lote {slug}", "issues": [123], "pr": {N}}'
     ```
   - Esperar CI com `gh pr checks {N} --watch` em background (`run_in_background: true`) — um acordar por PR, sem poll. Interpretar com cuidado: **exit 8 / checks pendentes / lista vazia = PENDENTE, nunca verde nem vermelho** (logo após o push os jobs podem nem estar registrados). Verde = os checks do CI **presentes E concluídos com sucesso**. **O gate é um passo SEPARADO do merge (#2031 — incidente 260610: merge encadeado com `&&` após o output dos buckets passou com check vermelho):** NUNCA encadear `gh pr merge` na mesma chamada Bash que imprime os checks. **Prefira `scripts/lib/wait-pr-checks.sh {N}` a um laço escrito à mão** (#6921: 5 laços `while true` sem timeout ficaram órfãos no `helios` por até 15h, batendo na API do GitHub sem parar, porque a vida do laço estava atada à sessão que o criou, não a um teto mecânico) — o helper embute o mesmo teto de 30min desta seção diretamente no processo, então sobrevive mesmo se a sessão que o lançou morrer no meio.

   **Timeout de CI (ci_timeout, #2381):** se o CI não transita para verde/vermelho em **30 min** (a partir do `pr_opened`), emitir evento run-log **e imediatamente tratar como CI vermelho** (tentativa de fix ou draft) — 30 min é o único limite, conforme a regra hard da seção "Regras" ("Timeout por espera de CI = 30 min; estourou → tratar como CI vermelho"). O evento `ci_timeout` é a companhia de observabilidade desse limite: distingue "CI devagar" de "sessão dormiu" no run-log, registrando elapsed_min e last_status no momento do vencimento:
   ```bash
   npx tsx scripts/log-event.ts --edition {AAMMDD} --agent overnight --level warn \
     --message "ci_timeout" \
     --details '{"pr": {N}, "unidade": "#NNNN | lote {slug}", "elapsed_min": 30, "last_status": "pending"}'
   ```
   Emitir `ci_timeout` e aplicar a regra hard são a mesma ação — acontecem ao cruzar os 30 min. Registrar `ci_timeout` no relatório.

   **Retry de GitHub API (#2383):** qualquer chamada ao GitHub (graphql, REST, `gh pr merge`, `gh pr checks`) que retornar **401 ou 429** (erros transitórios de autenticação ou rate-limit) **não deve ser tratada imediatamente como falha**. O coordenador tenta ao menos 3 vezes com backoff exponencial (30 s, 60 s, 120 s) antes de tratar como falha permanente ou emitir halt banner. Em cada tentativa, emitir:
   ```bash
   npx tsx scripts/log-event.ts --edition {AAMMDD} --agent overnight --level warn \
     --message "github_api_error" \
     --details '{"status": 401, "endpoint": "graphql/pullRequest", "retry_attempt": 1}'
   ```
   Após 3 tentativas sem sucesso → tratar como falha permanente (CI vermelho / halt banner). Este retry generaliza o padrão introduzido em #2060 para `check-pr-bugfix` para todo o gate da Fase 1. **Caso distinto — FORBIDDEN:** `errors[].type == "FORBIDDEN"` em chamadas de resolução de thread é um erro **estrutural** (a thread foi criada por um revisor externo — o overnight não tem permissão para resolvê-la). FORBIDDEN **nunca é retentável** e **não conta no gate overnight** — é tratado separadamente pela lógica de carve-out FORBIDDEN na seção de resolução de threads abaixo (anotação no PR body para o editor). Não confundir com 401/429: são mecanismos distintos e mutuamente exclusivos.

   **Resolução das review threads** (passo obrigatório entre "fixer concluiu" e "gate determinístico"): após o fixer aplicar/decidir sobre os findings do self-review (#2038), o coordenador resolve as threads via GraphQL. **Guard de paginação (>100 threads — #2222)**: antes de iniciar o loop de resolução, a query já inclui `pageInfo { hasNextPage endCursor }`. Se `hasNextPage == true`, **abortar o gate automático imediatamente** (converter PR pra draft + comentar na issue explicando que >100 threads exigem resolução manual pelo editor) — não tentar paginar durante a resolução autônoma; o risco de false-green em páginas não percorridas supera qualquer conveniência. (a) Coletar resposta com guard de erro e de paginação, e extrair IDs não-resolvidas:
     ```bash
     QUERY='{ repository(owner:"vjpixel",name:"diaria-studio"){ pullRequest(number:{N}){ reviewThreads(first:100){ nodes{ id isResolved } pageInfo{ hasNextPage endCursor } } } } }'
     RESP=$(gh api graphql -f query="$QUERY")
     # Guard de erro: se a query falhou (rede/auth/campo ausente), abortar — nunca tratar erro como 0 threads
     if ! echo "$RESP" | jq -e '.data.repository.pullRequest.reviewThreads' > /dev/null 2>&1; then
       echo "ABORT: gh api graphql falhou ou retornou erro — tratar como CI vermelho/pendente, não como 0 threads."
       exit 1
     fi
     HAS_NEXT=$(echo "$RESP" | jq -r '.data.repository.pullRequest.reviewThreads.pageInfo.hasNextPage')
     if [ "$HAS_NEXT" = "true" ]; then
       echo "ABORT: PR #{N} tem >100 review threads — gate automático desativado. Converter pra draft e comentar na issue."
       exit 1
     fi
     TIDS=$(echo "$RESP" | jq -r '[.data.repository.pullRequest.reviewThreads.nodes[] | select(.isResolved==false) | .id] | @sh')
     ```
   (b) Resolver cada ID em loop, rastreando separadamente as threads que retornarem FORBIDDEN — o coordenador substitui o ID real, nunca envia a string literal `{threadId}`:
     ```bash
     FORBIDDEN_TIDS=""
     for tid in $TIDS; do
       tid=$(echo $tid | tr -d "'")
       RESULT=$(gh api graphql -f query="mutation { resolveReviewThread(input:{threadId:\"$tid\"}){ thread{ id isResolved } } }")
       # Detectar FORBIDDEN pelo campo estruturado .errors[].type — nunca por grep no body inteiro
       if echo "$RESULT" | jq -e '[.errors[]? | select(.type == "FORBIDDEN")] | length > 0' > /dev/null 2>&1; then
         FORBIDDEN_TIDS="$FORBIDDEN_TIDS $tid"
         echo "INFO: thread $tid FORBIDDEN (criada por terceiro) — anotada no PR body para o editor; não bloqueia o gate."
       fi
     done
     # Se houver threads FORBIDDEN, adicionar seção "Pendências para o editor" no PR body listando cada ID
     ```
   Resolver as threads cujos findings foram endereçados pelo fixer (aplicados no código) ou conscientemente aceitos (finding válido mas não-bloqueante — anotar no PR body antes de resolver). No caso mais comum (fixer tratou todos os findings e não há reviewer externo), resolver todas as threads de uma vez é correto. **Carve-out FORBIDDEN (#2222)**: threads que retornaram FORBIDDEN são de revisores humanos externos — o overnight não pode resolvê-las e **elas não contam na condição (2) do gate**; porém são anotadas no PR body para que o editor as resolva manualmente. Jamais tentar forçar a resolução de thread de terceiro.

   **Gate determinístico** — **2 condições independentes, ambas obrigatórias** (#2210, #2222): (1) `npx tsx scripts/check-pr-checks-gate.ts --pr {N}` deve sair com `exit 0` (cobre fail/pending/skipping de uma vez — ver `scripts/lib/pr-checks-gate.ts`; substitui `gh pr checks {N} --json bucket --jq '...'`, que não roda no `gh` 2.46.0 do `helios`, achado #6225: `--json` só chegou em `gh pr checks` numa versão posterior, e a falha do comando dentro de `$(...)` vira string vazia — fácil de ler por engano como "0 checks reprovados". O script lê `gh pr view --json statusCheckRollup` e nunca confunde "comando falhou" com "zero achados": `exit 1` = check reprovado, `exit 2` = pendente/sem checks registrados ainda, `exit 3` = erro de comando/payload malformado, `exit 4` = `blocked_by_conflict` (#6768 — PR `CONFLICTING` com a base e nenhum check chegou a começar; o GitHub não computa merge ref pra rodar `pull_request` nesse SHA, então esperar não resolve — merge/rebase com a base primeiro, não repetir o poll até o timeout de 30 min do #2381) — só `exit 0` autoriza); (2) número de review threads não-resolvidas **excluindo as FORBIDDEN** deve ser `0` — fazer uma **nova query GraphQL pós-loop** para capturar o estado real após a etapa de resolução (não reutilizar `$RESP` do pré-loop, que seria stale e contaria threads já resolvidas):
     ```bash
     # Query fresca após o loop de resolução — $RESP do pré-loop está stale
     RESP2=$(gh api graphql -f query="$QUERY")
     if ! echo "$RESP2" | jq -e '.data.repository.pullRequest.reviewThreads' > /dev/null 2>&1; then
       echo "ABORT: query pós-loop falhou — tratar como CI vermelho/pendente."
       exit 1
     fi
     # Guard de paginação em RESP2 (#2232): se uma thread foi adicionada DURANTE o loop de
     # resolução (total passa de 100), RESP2 retorna só 100 e hasNextPage==true — thread #101
     # não contada → false-green. Aplicar o mesmo abort da query pré-loop.
     HAS_NEXT2=$(echo "$RESP2" | jq -r '.data.repository.pullRequest.reviewThreads.pageInfo.hasNextPage')
     # Guard triplo: "true" = há próxima página; "null" = pageInfo ausente (resposta malformada);
     # qualquer outro valor != "false" é conservador tratar como erro (#2232).
     if [ "$HAS_NEXT2" != "false" ]; then
       echo "ABORT: RESP2 hasNextPage=$HAS_NEXT2 — não é possível garantir gate seguro. Converter pra draft."
       exit 1
     fi
     UNRESOLVED_TOTAL=$(echo "$RESP2" | jq '[.data.repository.pullRequest.reviewThreads.nodes[] | select(.isResolved==false)] | length')
     FORBIDDEN_COUNT=$(echo "$FORBIDDEN_TIDS" | wc -w)
     # max(0, ...) evita UNRESOLVED_BLOQUEANTES negativo quando uma FORBIDDEN é resolvida
     # externamente entre o loop e RESP2 (#2232): a thread some de UNRESOLVED_TOTAL mas
     # FORBIDDEN_COUNT ainda a conta → subtração negativa → false-RED.
     _DIFF=$(( UNRESOLVED_TOTAL - FORBIDDEN_COUNT ))
     UNRESOLVED_BLOQUEANTES=$(( _DIFF < 0 ? 0 : _DIFF ))
     # Gate da condição (2): passa se $UNRESOLVED_BLOQUEANTES == 0
     # Threads FORBIDDEN já anotadas no PR body — não bloqueiam o gate overnight,
     # mas o ruleset do GitHub (required_review_thread_resolution) pode ainda bloquear o merge:
     # se o merge falhar por thread FORBIDDEN pendente, converter pra draft e comentar na issue.
     ```
   O gate da condição (2) usa `$UNRESOLVED_BLOQUEANTES` (não `$UNRESOLVED_TOTAL`). Se `$UNRESOLVED_BLOQUEANTES > 0`, há threads não-FORBIDDEN não-resolvidas — tratar como CI vermelho (tentativa de fix ou draft). **Nota de ruleset**: o GitHub ruleset `required_review_thread_resolution: true` exige que TODAS as threads estejam resolvidas para merge, incluindo as FORBIDDEN; se o ruleset bloquear o merge mesmo com `$UNRESOLVED_BLOQUEANTES == 0`, registrar `timeline.draft = now()` em `plan.json`, emitir o log-event `draft` (mesmo bloco do caminho CI-vermelho abaixo), converter o PR pra draft (`gh pr ready --undo`) e comentar na issue com os IDs FORBIDDEN para resolução manual pelo editor. Só após ambas as condições satisfeitas, em chamada própria separada, o merge. Bônus do incidente #2031: subagente que tocar qualquer arquivo listado em `ORCHESTRATOR_FILES` (topo de `test/orchestrator-prompt.test.ts` — hoje `orchestrator.md` + `orchestrator-stage-{0-preflight,1-research,2,3,4,5,6}.md` em `.claude/agents/`, lista que pode crescer) deve rodar `NODE_TEST_SNAPSHOTS=1 npx tsx --test test/orchestrator-prompt.test.ts` (snapshot + budget de linhas, #634) antes do push. **Não confiar num glob citado de cabeça (#6767, achado ao vivo 260829b, 2×)** — o teste também quebra quando uma sincronização de `.claude/skills/*/SKILL.md` obriga a atualizar a menção espelhada dentro de um desses arquivos do orchestrator (ex: `orchestrator-stage-3.md`/`orchestrator-stage-4.md`/`orchestrator-stage-2.md`); é essa 2ª edição, não o SKILL.md em si, que dispara o guard — ver `context/overnight-dispatch-rules.md` item 4 para o detalhe completo.

   **Guard de `Closes`/`REFS` antes do merge (#5010).** Imediatamente antes do `gh pr merge` abaixo, pra CADA issue da unidade (`N` = 1 em solo, `N` > 1 em lote), grep o body do PR (`gh pr view {N} --json body`) por `[Cc]loses #{issue}` OU `REFS #{issue}, N[ÃA]O CLOSES`. Se alguma issue do lote não tiver nenhum dos dois marcadores, é sinal de que o self-review do subagente esqueceu de declarar (não é, por si só, motivo pra tratar como CI vermelho) — o coordenador decide, a partir da própria leitura do diff/self-review já feita no "review leve" acima: se a issue está de fato totalmente resolvida por este PR, adicionar `Closes #{issue}` ao body **via `npx tsx scripts/lib/gh-pr-safe-edit.ts --pr {N} --body-file {arquivo-com-o-novo-corpo, escrito em /tmp ou no scratchpad da sessão — NUNCA na raiz do checkout, #6971}`, nunca `gh pr edit --body`** (#6292 — `gh pr edit` roda uma mutação que falha em silêncio: sai `exit 0` sem alterar o corpo; o helper usa REST + releitura pós-escrita, falhando alto se a escrita não pegou) ANTES do merge; se houver dúvida genuína sobre se está completa, **não forçar** — deixar sem marcador e seguir (a issue permanece aberta pra triagem, comportamento seguro por padrão). Nunca adicionar `Closes` a uma issue que o coordenador não confirmou como resolvida.

   **Roteamento obrigatório pós-merge pra issue com escopo residual (#6437).** Imediatamente depois do merge (mesmo passo, logo após confirmar `mergedAt` via `gh pr view`), pra CADA issue cujo marcador ficou `REFS #{issue}, NÃO CLOSES` (permanece ABERTA), rodar `npx tsx scripts/route-issue.ts --issue {issue} --track {agendada --until {data} | develop | fora-de-rodada}` de acordo com a natureza do escopo remanescente — **nunca deixar a issue sem roteamento**, que é exatamente o buraco que fez #6340/#6169/#6185/#6186/#6051 ficarem presas "Overnight sem sinal" pra sempre (rodadas seguintes repetiam `pulada`/"escopo residual" em prosa, sem nenhum comando rotear de fato). Escolher o track: **agendada** se o residual depende de tempo/evento conhecido (`--until` a data); **develop** se depende de decisão do editor (aplicar também a label `develop-track`, ou `trade-off-real` se for cat. C); **fora-de-rodada** se o residual não tem próximo passo de código claro (label `sem-direcao-acionavel`). Se o residual é dispatchável NESTA MESMA rodada (ex: 2ª frente pequena, sem dependência externa), não rotear — deixar como candidata normal do próximo re-scan da Fase 1 passo 1 em vez de tratar como `pulada`. Comentar na issue citando o PR mergeado + o track escolhido (mesma disciplina do #5010/#5816 acima) — status `escopo-residual` no `plan.json` (`scripts/lib/overnight-prose-track-map.ts`) documenta a correspondência esperada.

   **Trem de merge (#6300, decisão do editor 26/08/2026, default ATIVO).** Se ≥2 unidades da ONDA corrente (#6299 — teto 3) chegam ao Gate 2 verde ao mesmo tempo e não colidem em arquivo (a mesma análise de cluster de conflito que já compõe a onda), preferir `npx tsx scripts/run-merge-train.ts --session-id {sessionId} --kind overnight --prs {N1,N2,...}` a mergear cada uma isoladamente abaixo — 1 run de CI valida a combinação em vez de 1 por PR, mesmo merge lock. `--session-id` **explícito e obrigatório** (o script chama `session-registry.ts` como subprocesso próprio — fora do hook de auto-injeção). Onda de 1 unidade só → segue o caminho de sempre abaixo, sem trem. Degrada sozinho: CI do lote vermelho ou timeout bissecta até o piso (nunca prende um PR bom por causa de outro ruim no mesmo lote) — ver `scripts/lib/merge-train.ts`/`merge-train-live.ts`. Commit final é 1 squash por lote com `Closes` de todas as issues (mesma convenção de fusão de cluster do develop, #4319).
   - Verde (CI verde **e** threads_não_resolvidas == 0) → registrar `timeline.ci_green = now()` em `plan.json`. **Adquirir o merge lock cross-sessão antes do merge (#5156 item 4):** `npx tsx scripts/lib/session-registry.ts merge-lock-acquire --pr {N}` (`--session-id` auto-injetado). **`--pr` não é opcional na prática, mesmo com esta sessão registrada como coordenadora E o lock em mãos** — achado ao vivo #7169/#7223 (rodada helios, PR #7217): seguir o texto sem `--pr` fez o `gh pr merge` ser bloqueado 4× seguidas pelo guard #5716; `--pr {N}` explícito destravou de primeira, e o mesmo padrão se repetiu em #7219/#7221/#7224/#7218. **O lock só é exclusão mútua real entre sessões da MESMA máquina; entre máquinas via OneDrive ele é advisory (#6182) — `acquired` não prova que a outra máquina não está mergeando agora.** **Mitigação adicional pós-#7169:** rodar `git fetch origin master` e conferir se o head mudou desde a última leitura antes de confiar no `acquired` — é sinal que vem do GitHub, não do OneDrive, e por isso atravessa máquina de verdade mesmo se `data/sessions/` estiver com sync degradado (achado ao vivo #7169: `onedrive.service` morto por 1h13 sem alarme deixou o registro cross-máquina cego). Se `denied` (outra sessão — overnight ou develop — está no meio do próprio merge+pull agora, TTL 2 min), esperar ~15s e tentar de novo (poucas tentativas — o TTL é curto de propósito, nunca deveria travar por muito tempo); se persistir negado além de ~5 tentativas, tratar como halt banner (mesma classe do #738, motivo "merge lock preso — outra sessão pode ter crashado segurando o lock além do TTL esperado, investigar antes de prosseguir"). Registro vazio (nenhuma outra sessão adotou o registro ainda, caso comum hoje) sempre adquire de primeira. **O guard do #5716 agora CONSULTA o lock (#6296)**: uma coordenadora que não o detém enquanto OUTRA coordenadora ativa existe é bloqueada — pegar o lock deixou de depender desta skill lembrar. Lock ausente com esta sendo a única coordenadora, ou estado do lock ilegível (I/O do OneDrive), nunca bloqueiam. **Conceder janela a sessão não-coordenadora (#6296):** quando uma sessão interativa precisar mergear durante esta rodada, o guard a bloqueia corretamente (ela não é coordenadora, e não vira uma por relabel) — o caminho legítimo é ESTA sessão conferir colisão por arquivo contra os PRs abertos da rodada e conceder: `npx tsx scripts/lib/session-registry.ts grant-merge --kind overnight --granted-to {sessionId dela} --pr {N}` (standalone, `--session-id` auto-injetado; TTL 10 min, uso único, ninguém concede a si mesmo). **`--pr` é obrigatório na prática:** a concessão é ESCOPADA àquele PR (#6303) — omitir o flag a torna válida pra qualquer merge daquela sessão durante o TTL, anulando a conferência de colisão que justificou concedê-la. **No `SendMessage` que avisa a beneficiária, inclua explicitamente que o `gh pr merge` dela precisa citar o número do PR** (`gh pr merge {N} ...`, nunca deixar o `gh` inferir pela branch corrente) — concessão escopada só cobre o PR exato dela (#6322): sem o número explícito, `extractGhPrMergeTargetPr` não consegue casar a chamada com a concessão e o guard bloqueia mesmo com janela válida em mãos. A beneficiária **não precisa consumir à mão** — o hook `PostToolUse` `.claude/hooks/consume-merge-grant-on-merge.mjs` consome sozinho após o merge. **Nunca** orientar alguém a rodar `register --kind overnight` pra se destravar — isso fabrica identidade de coordenadora e fura o guard. Depois `gh pr merge {N} --squash --subject "{título} (#NNNN)" --body "...\n\nCo-Authored-By: Claude Fable 5 <noreply@anthropic.com>"` — **sem `--delete-branch`** (a branch está checked out no worktree do subagente; a deleção local falharia e o exit non-zero seria lido como merge falho). **Confirmar o estado real via `gh pr view {N} --json state,mergedAt` SEMPRE — inclusive (principalmente) quando `gh pr merge` retornar erro** (#573): merge remoto pode ter sucedido com falha local. **Com o merge confirmado, soltar a claim de cada issue da unidade na mesma respiração:** `npx tsx scripts/lib/session-registry.ts unclaim-issue --kind overnight --issue {N}` por issue (#6317) — sem isso a claim só cai quando a sessão inteira termina (`end`), segurando issue já mergeada e fechada por até 90min à toa (mesma classe de resíduo que a #6168 documenta). Após confirmar merge: registrar `timeline.merged = now()` em `plan.json` e emitir:
     ```bash
     npx tsx scripts/log-event.ts --edition {AAMMDD} --agent overnight --level info \
       --message "merged" \
       --details '{"unidade": "#NNNN | lote {slug}", "issues": [123], "pr": {N}}'
     ```
     Depois: limpar o worktree do subagente (`git worktree prune` + remover diretório) e deletar a branch remota (`git push origin --delete overnight/fix-NNNN`). **Em seguida, republicar a contagem (#6299 — call site de FECHAMENTO, obrigatório, par do call site de abertura no passo 2):** rodar `npx tsx scripts/lib/session-registry.ts heartbeat --kind overnight --active-worktrees {N}` com o `{N}` já decrementado. **Standalone**, mesma disciplina de injeção do call site de abertura — span inline, nunca bloco cercado (`test/skill-chained-session-command-guard-6232.test.ts` reprova o bloco, pelo mesmo critério de `isChainedCommand` que o hook usa). Só o par abertura+fechamento mantém o número honesto: publicar só ao abrir faz a sessão parecer permanentemente cheia e sufoca a concorrente pelo resto da noite.
   - Vermelho (de verdade) → registrar `timeline.fix_iteration_{K} = now()` (K = 1 ou 2) em `plan.json` e emitir:
     ```bash
     npx tsx scripts/log-event.ts --edition {AAMMDD} --agent overnight --level warn \
       --message "fix_iteration" \
       --details '{"unidade": "#NNNN | lote {slug}", "issues": [123], "pr": {N}, "tentativa": K}'
     ```
     Depois: até **2 tentativas de fix**: continuar o mesmo subagente via `SendMessage` com o **tail do log de falha** (`gh run view --log-failed`, últimas ~100 linhas por step — nunca o log inteiro); se o subagente não estiver mais disponível (ou `SendMessage` não existir no harness), dispatchar um novo **fazendo checkout da branch existente** (nunca refazer de master). Num **lote**: se a falha é atribuível a uma issue específica, a tentativa 2 pode **remover o item problemático do lote** (revert das mudanças dele + re-push), comentando na issue removida o diagnóstico — o resto do lote segue. Persistiu vermelho → registrar `timeline.draft = now()` em `plan.json` e emitir:
     ```bash
     npx tsx scripts/log-event.ts --edition {AAMMDD} --agent overnight --level warn \
       --message "draft" \
       --details '{"unidade": "#NNNN | lote {slug}", "issues": [123], "pr": {N}}'
     ```
     Converter pra draft (`gh pr ready --undo`), comentar na(s) issue(s) com diagnóstico + link, marcar `draft-ci-vermelho` no plano, e seguir pra próxima. **Resolução de threads NÃO é pré-requisito do draft** — o draft não mergeia, então `required_review_thread_resolution` não se aplica; resolver threads só é obrigatório antes do merge (gate determinístico acima).
4. **Manter #636 no que ele de fato garante (#6299).** O invariante real é **"nunca 2 PRs que COLIDEM abertos ao mesmo tempo"**, e desde o #6299 ele é satisfeito por CONSTRUÇÃO (análise de cluster + fusão de colidentes), não por serialização — mesma leitura que o `/diaria-develop` já aplicava desde o #4319. Dois PRs de unidades disjuntas abertos simultaneamente **não** violam o #636. O que continua estritamente serial é o **MERGE**: `master` recebe um squash por vez, sempre sob `merge-lock-acquire`, e o próximo merge só começa depois do `git pull` do anterior. Drafts de CI-vermelho ficam abertos para triage do editor — são exceção consciente, sinalizados no relatório.
5. **Atualizar `plan.json`** com o status terminal **e o número do PR** (campo `pr`) da unidade. Os eventos de run-log (merged/draft/fix_iteration) são emitidos em tempo real dentro do passo 3 acima; aqui apenas confirmar que `plan.json` foi persistido. Para unidades **puladas** (bloqueio externo, sem-resposta, ambígua), gravar `timeline.pulada = now()` em `plan.json` e emitir:
   ```bash
   npx tsx scripts/log-event.ts --edition {AAMMDD} --agent overnight --level info \
     --message "pulada" \
     --details '{"unidade": "#NNNN | lote {slug}", "issues": [123], "motivo": "bloqueio-externo | sem-resposta | ..."}'
   ```
   Assim `/diaria-log` enxerga a noite inteira — desfechos de sucesso, fix-iterations e unidades puladas.

   **Instrumentação de token por unidade (#4815 — mesma mecânica do #3453 Rec 1/#3454 Rec 1, aqui aplicada por unidade de implementação em vez de só pelo coordenador):** no mesmo ponto acima (unidade atingiu status terminal — merged/draft/pulada), emitir também um evento `subagent_metrics`: `duration_ms` sai de graça do `timeline` já gravado (fim menos `dispatch`); `subagent_tokens`/`tool_uses` vêm do `harness_usage` no retorno do `Agent` que despachou a unidade **se o harness os expuser por invocação** (caso não, gravar `null` com `source: "unavailable"` — nunca omitir o evento):
   ```bash
   npx tsx scripts/log-event.ts --edition {AAMMDD} --agent overnight --level info \
     --message "subagent_metrics" \
     --details '{"unidade": "#NNNN | lote {slug}", "issues": [123], "subagent_tokens": N, "tool_uses": N, "duration_ms": N, "source": "harness_usage | unavailable"}'
   ```
   É a fonte da categoria **Implementação** da seção "Custo em tokens" (Fase 2, mandatória desde #4815) — sem este evento por unidade, aquela categoria fica sempre `unavailable`.
6. `git pull` em master após cada merge, antes da próxima issue. **Liberar o merge lock logo em seguida (#5156 item 4):** `npx tsx scripts/lib/session-registry.ts merge-lock-release --pr {N}` (`--session-id` auto-injetado; mesmo `--pr {N}` do acquire, #7169/#7223) — libera a janela pra próxima sessão que esteja esperando. A janela protegida pelo lock é exatamente `gh pr merge` (passo 3) até este `git pull` (passo 6) — nunca mais que isso, pra não segurar outra sessão além do necessário.

**Condições de parada:** fila elegível esgotada → **Fase 1.5**. Erro irrecuperável (auth do gh expirada, rede fora por > 30 min) → se houver PR em CI em voo, levá-lo até merge/draft se possível (senão, comentar o estado na issue); renderizar halt banner (`npx tsx scripts/render-halt-banner.ts --stage "overnight" --reason "..." --action "..."`) + relatório antecipado com o motivo, **pulando a Fase 1.5** (estado pode estar inconsistente).

**Stall passivo — três camadas (#2379 + #2688 + #2896):** stall passivo (sessão parada sem sinal explícito) é a causa mais frequente de buracos no run-log (incidentes 260611, 260616b, 260702-r2). Há três camadas independentes de detecção:

- **(i) Detecção-no-wake (#2379 — coordenador):** o coordenador, quando acordado por um evento (CI, task-notification), verifica se houve >45 min sem progresso em unidade em andamento. Cobre o caso em que a sessão existe mas a issue está travada.

  **Guard mecânico obrigatório (#2768) — nunca "vibes-based":** o incidente 260630/260701 aconteceu porque o coordenador recebeu 5 notificações consecutivas de "ainda esperando" de um subagente preso (~4h30 sem progresso real) e nunca comparou o tempo decorrido contra o threshold — só continuou esperando. A partir de agora, **toda vez** que uma `task-notification` de subagente em andamento reportar "ainda aguardando" ou nenhum progresso concreto novo, o coordenador deve, **ANTES de continuar esperando**, executar estes 3 passos na ordem:
  1. Ler `timeline.dispatch` da unidade correspondente em `plan.json`.
  2. Calcular `elapsed = now() - dispatch_time` (comparação explícita de timestamps, não estimativa).
  3. Se `elapsed > 45 min` (`OVERNIGHT_STALL_THRESHOLD_MIN`), aplicar o fluxo de stall abaixo **imediatamente** — nunca esperar mais uma notificação "pra ter certeza".

  **Contador de notificações repetidas (#2768):** independentemente do cálculo de tempo acima, se o coordenador receber **3 notificações consecutivas do mesmo subagente com essencialmente o mesmo conteúdo** (ex: "ainda rodando `npm test`", sem novo output/progresso entre elas), tratar como sinal de stall e aplicar o fluxo abaixo mesmo que o cálculo de `elapsed` ainda não tenha estourado 45 min — 3 notificações idênticas é, por si só, evidência de que a unidade não está avançando.

  Quando detectar (por qualquer um dos dois critérios acima): (1) gravar entrada em `stall_events` no `plan.json` com `at` e `reason` estimado; (2) emitir evento run-log:
  ```bash
  npx tsx scripts/log-event.ts --edition {AAMMDD} --agent overnight --level warn \
    --message "stall_detected" \
    --details '{"reason": "rate_limit | context_exhaustion | standby | ci_timeout | unknown", "unidade": "#NNNN"}'
  ```
  (3) **imediatamente** renderizar halt banner:
  ```bash
  npx tsx scripts/render-halt-banner.ts \
    --stage "overnight — Fase 1" \
    --reason "stall passivo detectado: >45 min sem progresso em {unidade}" \
    --action "reconecte e responda 'retry' para retomar, ou 'abort' para encerrar"
  ```
  Stall silencioso >60 s é inaceitável — regra derivada do CLAUDE.md (#738), aqui especializada para o loop overnight. Ao retomar após stall, gravar `resumed_at` na entrada correspondente de `stall_events`.

- **(ii) Detecção-por-tempo (#2688 — watchdog externo):** o coordenador é event-driven e não cobre o caso em que **nenhum evento chega** (todos os subagentes em silêncio total). O script `scripts/overnight-watchdog.ts` roda via systemd timer a cada 10 min (o antigo par Windows/Task Scheduler foi removido no #5115, cutover final), independente da sessão Claude Code, e detecta stall medindo `max(mtime plan.json, último evento run-log agent:overnight)`. Se inatividade > 45 min, também grava em `stall_events`, emite evento run-log e exibe halt banner. Setup local one-time: ver `docs/overnight-watchdog-setup.md`. **Esta camada só protege se a task estiver de fato armada** — o incidente #2768 descobriu que ela nunca tinha sido registrada nesta máquina; a Fase 0 passo 1 checa isso (`scripts/lib/check-watchdog-armed.ts`) e, se não armada, avisa (fail-soft) sem tentar armar automaticamente — ver detalhe na Fase 0 passo 1 acima.

- **(iii) Fallback wake determinístico do coordenador (#2896):** cobre o buraco que restava mesmo com (i) e (ii): o incidente 260702-r2 (coordenador ~8h parado) aconteceu com o watchdog externo desarmado (causa raiz #1, endereçada pelo arming assertivo acima) **e** com zero eventos chegando ao coordenador — o gatilho foi um subagente resumido que rodou `npm test` em background, abriu o PR, e silenciou sem emitir notificação terminal, então (i) nunca teve um "wake" pra rodar o guard #2768 sobre. Fix: ao **dispatchar OU resumir** qualquer subagente (passo 2 e passo 3 da Fase 1), o coordenador agenda um `ScheduleWakeup` com delay ~1200s (20 min). Ao acordar — **mesmo sem ter recebido nenhum evento da unidade** — o coordenador lê `timeline.dispatch` de `plan.json` e computa `elapsed = now - timeline.dispatch` via `shouldWakeCheck(dispatchISO, nowISO)` — chamar **sem** o 3º argumento, pra herdar `OVERNIGHT_STALL_THRESHOLD_MIN` (45 min, `scripts/lib/overnight-stall-threshold.ts`) em vez de re-fixar o número aqui (helper puro em `scripts/lib/overnight-fallback-wake.ts`, coberto por `test/overnight-fallback-wake.test.ts`). Se `shouldWakeCheck` retornar `true`, aplicar o fluxo de stall do #2768 (gravar `stall_events`, emitir `stall_detected`, renderizar halt banner) **exatamente como em (i)** — a única diferença é que este wake não depende de receber notificação alguma, fechando o caso "silêncio total do lado do coordenador" que (i) inerentemente não cobre (é event-driven por definição) e que (ii) só cobre se a task estiver de fato armada.

  **Sinal de resume vivo-mas-sem-garantia (`classifyResumeSignal`):** quando o coordenador resume um subagente via `SendMessage`, o retorno textual da chamada é classificado com `classifyResumeSignal(sendMessageResult)` (mesmo módulo): um retorno contendo "queued" (ex: "queued for delivery at its next tool round") vira `'queued'` — o agent está vivo, mas **sem garantia de notificação terminal** quando terminar (era exatamente o padrão do incidente) — então o `ScheduleWakeup` desta unidade é reagendado normalmente e o re-check ativo é o único jeito de saber se ela avançou. Um retorno contendo "resumed"/"stopped" vira `'resumed'` — o agent foi de fato retomado e uma notificação é esperada pelo caminho event-driven normal (o fallback wake ainda roda como rede extra, mas o caminho (i) tem mais chance de disparar primeiro). Qualquer outro texto vira `'unknown'` — tratado com a mesma cautela de `'queued'` (nunca assumir que o coordenador vai ser notificado).

  **Interação entre as três camadas:** todas gravam em `stall_events` e emitem `stall_detected` no run-log. A dedup interna do watchdog (janela de `max(limiar/2, 15 min)` — 22,5 min com o limiar atual de 45) evita spam se o coordenador ainda estiver rodando e já tiver registrado. Ao retomar, o coordenador grava `resumed_at` na entrada correspondente de `stall_events` normalmente — vale para as três camadas.

## Fase 1.5 — Code-review consolidado pós-rodada (#2039)

A Fase 1.5 segue a cadeia de re-entrada cuja terminação é garantida pelo depth limit definido na **tabela de estados do passo 0** (Resume). `findings_depth` em `plan.json` rastreia em qual nível estamos (0 = review inicial, 1 = review pós-mini-rodada 1, 2 = review pós-mini-rodada 2); o que fazer em cada nível está nos bullets de "Re-entrada de findings" abaixo, e quando parar está na tabela. **Este cap de profundidade 2 (e a exclusão de `session-finding` recém-criado da re-checagem principal, ver Fase 1 passo 1) NÃO existem no `/diaria-develop`** (#4319) — lá um finding vira issue como qualquer outra, classificada por tier, sem mini-rodada numerada. A diferença é proposital: overnight roda desassistido e a cadeia de findings se auto-alimenta (o review do código recém-escrito gera mais trabalho sobre esse mesmo código), então aqui o depth limit segue sendo o freio; develop é supervisionado, quem segura a sessão é o editor. **Este parágrafo vale só para `findings_depth`** — o cap de re-varredura (`rescans_done` K=2) que ficava ao lado dele foi eliminado em #5272, ver Fase 1 passo 1; não ler a divergência de um como se cobrisse o outro.

**Passo 1.5 — Review consolidado** (roda sempre que `findings_depth` avança para este nível):

Com a fila esgotada (sem PR **não-draft** aberto — drafts de CI-vermelho não bloqueiam esta fase e ficam FORA do diff consolidado, pois não mergearam), rodar **UM** code-review pesado sobre o **diff acumulado**. No `findings_depth == 0` (review inicial): diff = `{base_sha}..HEAD`. Nos níveis subsequentes (1.5b, 1.5c): diff = do SHA imediatamente antes do início da mini-rodada anterior até HEAD. **Como calcular o SHA base das mini-rodadas:** ao iniciar cada mini-rodada, gravar `minirodada_base_sha_{N}: git rev-parse HEAD` no `plan.json` ANTES de qualquer merge; no review subsequente (1.5b/1.5c), usar esse campo como base. **Fallback quando a mini-rodada não produziu nenhum merge** (todas as issues viraram `draft-ci-vermelho`): diff ficaria vazio ou idêntico ao do review anterior — **pular** com `review: "skipped: diff vazio (mini-rodada {N} sem merges) (depth {N+1})"` no plan.json e nota no relatório (mesmo comportamento do "diff < 50 linhas"). Forma executável (#4034 — `/code-review` deixou de ser invocável via Skill tool desde 260724, gate de plataforma `disable-model-invocation`, não config nossa; #4234 — o dispatch passou a usar os agentes do plugin `pr-review-toolkit`): dispatchar um Agent com `subagent_type: "pr-review-toolkit:code-reviewer"` (nome PREFIXADO pelo plugin — sem o prefixo dá `Agent type not found`) e `model: sonnet` explícito (#2019, imune a esse gate — é a ferramenta Agent, não Skill), passando no prompt (a) o `git diff {sha_range}` do repo como escopo **explícito** — o agente do plugin revisa `git diff` unstaged por default, que não é o diff da rodada — e (b) a restrição **somente leitura**: proibido editar arquivo, rodar `git checkout`/`switch`/`stash`/`reset` ou commitar, porque o checkout é compartilhado com o coordenador (incidentes 260703/260708). Sem `isolation: worktree`. **Fallback obrigatório:** se o dispatch resolver `Agent type ... not found` (plugin ausente — sessão cloud, clone fresco, plugin desabilitado), refazer com `general-purpose` + rubrico de review inline (correctness, simplification/efficiency, test-coverage, security) — o comportamento pré-#4234. Degradar pra review pior é aceitável; pular o review em silêncio, não. Instruir o agente a reportar os achados mais graves primeiro em texto na resposta final (lista vazia — nunca achado inventado — se não encontrar nada; `ReportFindings` não está confirmado disponível em subagentes desta sessão, então não depender dele). **Sem `--comment`** — não há PR aberto pra ancorar threads nesta fase; os findings retornam ao coordenador e a triagem abaixo é o destino deles. O `base_sha` vem de `plan.json`. É a rede pós-merge — 1 review consolidado custa uma fração de N por PR e enxerga interações ENTRE os PRs da mesma janela (nota: o diff inclui também merges alheios — findings sobre código alheio viram issue, nunca hotfix). Esta fase é **read-only + filing**: nenhum PR novo de feature. **Instrumentação de token do review (#4815):** ao receber o retorno do Agent do code-review (inicial ou de cada mini-rodada), emitir um evento `review_metrics` — `review_tokens`/`tool_uses` do `harness_usage` do retorno **se o harness os expuser**, senão `null` com `source: "unavailable"` (nunca omitir o evento):
```bash
npx tsx scripts/log-event.ts --edition {AAMMDD} --agent overnight --level info \
  --message "review_metrics" \
  --details '{"depth": N, "review_tokens": N, "tool_uses": N, "source": "harness_usage | unavailable"}'
```
É a fonte da categoria **Review pós-rodada** da seção "Custo em tokens" (Fase 2, mandatória desde #4815) — separada da Implementação de propósito (#4815 item 2: foi essa separação, numa sessão develop, que tornou visível o rateio 66/34 entre as duas categorias).

Saídas explícitas: diff vazio ou < ~50 linhas (e não foi mini-rodada sem merges — coberto acima) → **pular** com `review: "skipped: diff trivial (depth {N})"` no plan.json + nota no relatório (o review leve por PR já cobriu). Zero findings → registrar `review: "done (depth {N})"` com nota "0 findings" (silêncio é indistinguível de não-rodou). Review falhou (Agent indisponível, timeout) → NÃO stall (#738): logar warn, `review: "skipped: {erro} (depth {N})"`, seguir pra Fase 2.

Triagem dos findings:
- **Crítico em produção** (corrupção de dado, publicação quebrada, master vermelho — confirmado deterministicamente, ex: `gh run list --branch master --limit 1`, nunca só pelo texto do finding, #573) → **hotfix imediato** seguindo o fluxo COMPLETO da Fase 1 passos 2–3 (subagente em worktree, testes conforme #2959 — typecheck + afetados, nunca suíte completa local —, regressão #633 quando bugfix, branch `overnight/hotfix-*`, CI + gate determinístico #2031, verify #573). A exceção é só ao "nenhum PR novo" — **nunca ao processo**.
- Demais findings, **filtrados por barra de filing (#2754)**: só vira issue se for **P2+** (mesma heurística do passo abaixo: corrupção de dado, funcionalidade quebrada, bug de segurança, ou bug que deveria ter sido capturado por #633 mas passou) **ou** tiver **cenário de falha concreto** (inputs/estado que reproduzem o problema, não só "poderia ser melhor"). Findings P3 genéricos/nice-to-have/estilo (a maioria de um review `max`) **não viram issue** — ficam listados no relatório da noite (Fase 2) como "findings do review, não filados", pro editor decidir se algum merece virar issue manualmente. Racional: cada issue filada aqui é dívida que uma rodada futura vai queimar tokens resolvendo, que por sua vez gera novo self-review, que fila mais issues — o ciclo estava compoundando token spend entre rodadas sem produzir valor proporcional (observado na 260630: issues #2684/#2691/#2693, todas follow-up de follow-up de self-review anterior, custaram ~413k tokens numa única rodada). Findings que passam na barra → **issues filadas** seguindo o protocolo do auto-reporter (`.claude/agents/auto-reporter.md` + `scripts/lib/auto-reporter-dedup.ts`: dedup via `gh search issues` com fallback gracioso, labels tipo + prioridade), com label extra **`session-finding`** e corpo citando o PR de origem do diff.
- **Re-entrada de findings** (ver tabela do passo 0 para o depth limit e condição de parada): após filar as issues, verificar `findings_depth` em `plan.json`:
  - Se `findings_depth == 0` (review inicial, 1.5): issues `session-finding` com direção clara → registrar com `source: "finding-depth-1"`, gravar `minirodada_base_sha_1: git rev-parse HEAD` no plan.json, incrementar `findings_depth` para 1, e **rodar mini-rodada 1** (fluxo da Fase 1 **fechado sobre essas issues** — `rescans_done` NÃO é incrementado dentro de mini-rodadas — o contador é exclusivo da fila principal (e, desde #5272, só observabilidade); **timestamps `timeline.*` emitidos normalmente** — os passos 2/3/5 da Fase 1 se aplicam sem modificação dentro das mini-rodadas); depois voltar a este passo — será o review 1.5b.
  - Se `findings_depth == 1` (review pós-mini-rodada 1, 1.5b): classificar os findings por severidade. **Heurística de classificação P2+** (o output do `/code-review` não tem campo de prioridade — o coordenador infere da descrição): finding é P2+ se descreve **corrupção de dado, funcionalidade quebrada, bug de segurança, ou bug que deveria ter sido capturado por #633 mas passou** — style/cleanup/simplificação são P3 por padrão. Em caso de dúvida, o finding é P3 (não ativa mini-rodada extra). **Ordem de escrita obrigatória**: primeiro gravar `review_1_5b_has_p2: true/false` no `plan.json`, **só depois** gravar `review: "done (depth 1)"` — assim uma falha entre os dois writes deixa `review_1_5b_has_p2` escrito mas `review` como null, e o Resume re-executa 1.5b de forma segura (em vez de pular findings com dados perdidos). Se `review_1_5b_has_p2 == true`: issues `session-finding` P2+ com direção clara → registrar com `source: "finding-depth-2"`, gravar `minirodada_base_sha_2: git rev-parse HEAD` no plan.json, incrementar `findings_depth` para 2, e **rodar mini-rodada 2** (mesmas restrições: fluxo Fase 1 fechado, sem incremento de `rescans_done` e sem re-varredura de convergência; **timestamps `timeline.*` emitidos normalmente**); depois voltar — será o review 1.5c. Se `review_1_5b_has_p2 == false`: **não rodar mini-rodada 2** — aplicar a **barra de filing (#2754)** aos findings P3 do 1.5b, igual à Fase 1.5 nível 0: só vira issue o que tiver cenário de falha concreto; o resto fica listado no relatório, sem issue. O que passar na barra → **filado via o protocolo do auto-reporter** (label `session-finding`) antes de ir para Fase 2; em seguida registrar `review: "done (depth 1)"` e ir para Fase 2 (o 1.5b é o review final desta noite). Registrar no relatório "cadeia encerrada no depth 1 (nenhum finding P2+ no 1.5b)".
  - Se `findings_depth == 2` (review pós-mini-rodada 2, 1.5c): issues `session-finding` com direção clara **não re-entram** — ficam para a próxima rodada (ver tabela do passo 0 — depth limit atingido). Registrar no relatório "findings da 1.5c ficam para próxima rodada".
  - Issues `session-finding` ambíguas (qualquer nível): **não fazer pergunta ao editor** — postar comentário explicando o que falta + `pulada` motivo `ambigua` (Regra 1).
- Ao final de cada nível: gravar `review: "done (depth {N})"` (+ contagem de findings/issues) no `plan.json` — é o que torna a fase idempotente no resume. Tudo listado nas seções de findings do relatório (Fase 2).

## Fase 2 — Relatório

0. **Encerrar sessão overnight ativa — adiado pro fim da fase (#6758).** O encerramento (`overnight-session-marker.ts --end` + `session-registry.ts end --kind overnight`) NÃO roda aqui — ver step 7 (o último desta fase, depois do cleanup de worktrees). Rodar aqui era o bug original: os passos 0.5–6 abaixo ainda fazem ~7 chamadas de ferramenta, e cada uma dispara `session-beacon.mjs` (hook `PreToolUse`), que — não achando mais o registro `overnight-*` já removido por um `end` precoce — cria um registro NOVO `interactive-{tag}-{sessionId}.json` do zero, fazendo a sessão overnight "ressuscitar" como `interactive` viva em `list-active` mesmo após a rodada já ter terminado de verdade.
0.5. **Gate de re-triagem pendente + convergência (#5476, fundido em #5706) — bloqueia a compilação do relatório, não só um lembrete.** Rodar `npx tsx scripts/check-state-changed-pending.ts --plan data/overnight/{AAMMDD}/plan.json`. Desde #5706 o mesmo comando também faz o `gh issue list` completo e compara contra o `issues[]` já conhecido pelo plano (overnight não grava `goal.target_set`/`goal.tiers` — esses campos são exclusivos do `plan.json` do develop; `issues[]` é a ÚNICA fonte de "já conhecido" aqui), reportando issue aberta não-triangulada (mesmo mecanismo do develop, escopo adaptado ao schema do overnight) — reforça, não substitui, a re-varredura de convergência já descrita na Fase 1. Cada varredura bem-sucedida grava `goal.last_convergence_scan: {at, novas_encontradas}` no `plan.json` — como `goal` **não existe hoje** no schema do overnight (ver exemplo no passo 7 abaixo), a PRIMEIRA varredura da rodada CRIA esse objeto só com esse campo; não é um schema pré-existente sendo populado. `exit 0` ("ok — nenhuma pendência de re-triagem, nenhuma issue nova fora da varredura") → seguir pro passo 1. `exit 1` (lista de issues pendentes e/ou novas) → **voltar pra Fase 1** e reavaliar dispatch pra cada issue listada antes de continuar (ver regra correspondente na seção "Regras" acima) — o relatório só é compilado depois que a lista voltar a `[]`. Sem `gh`/rede, o comando degrada sozinho pra só a pendência explícita (fail-soft #738) — e o stdout de sucesso NESSE caso é explícito que a re-varredura NÃO rodou, nunca afirma "nenhuma issue nova fora da varredura" sem ter checado; `--skip-convergence` faz o mesmo degradê proativamente.
0.6. **Gate de cobertura de comentário (#5816) — bloqueia a compilação do relatório, mesma filosofia do passo 0.5.** Rodar `npx tsx scripts/check-overnight-comment-coverage.ts --plan data/overnight/{AAMMDD}/plan.json`. Confere que toda issue `pulada` (regra #5777) e toda issue `mergeada` cujo PR usou `REFS #N, NÃO CLOSES` em vez de `Closes` (regra #5010 — "PR mergeado com REFS" ≠ "issue fechada") de fato recebeu um comentário overnight-visível na issue (via `gh issue view N --json comments`). `exit 0` → seguir pro passo 1. `exit 1` (lista de issues sem comentário) → comentar em cada issue listada com o que foi feito/o que falta antes de continuar — só então o relatório é compilado. Sem `gh`/rede, o comando degrada sozinho pra warning + `exit 0` (fail-soft #738).
0.7. **Gate de estado terminal de PR (#5831) — bloqueia a compilação do relatório, mesma filosofia dos passos 0.5/0.6.** Rodar `npx tsx scripts/check-pr-terminal-state.ts --plan data/overnight/{AAMMDD}/plan.json` (achado ao vivo que motivou: `docs/orchestrator-stage-narrative-history.md#overnight-5831-pr-terminal-260820`). O comando cruza `gh pr list --state open` contra `plan.json`: (1) todo PR registrado (`issues[].pr`) que segue aberto no GitHub precisa de status `mergeada`/`draft-ci-vermelho`/`fechada-sem-merge` na issue correspondente — sem isso, é uma divergência confirmada (o coordenador esqueceu de fechar o loop); (2) PR aberto em branch `develop/`/`overnight/`/`fix/` que não aparece em nenhum `issues[].pr` vira candidato pra revisão humana (autoria não confirmada só pelo nome do branch — outra sessão pode estar rodando em paralelo, #5156), listado separado, nunca tratado com a mesma certeza do cenário 1. `exit 0` → seguir pro passo 1. `exit 1` → checar cada PR listado (`gh pr view N`), mergear/fechar/atualizar `plan.json` com o status correto antes de continuar — só então o relatório é compilado. Sem `gh`/rede, o comando degrada sozinho pra warning + `exit 0` (fail-soft #738).
0.8. **Gate de drift de label de decisão (#5892) — bloqueia a compilação do relatório, mesma filosofia dos passos 0.5/0.6/0.7.** Rodar `npx tsx scripts/check-decision-label-drift-gate.ts --plan data/overnight/{AAMMDD}/plan.json`. Detecta drift entre a prosa da rodada e as labels estruturais atuais (racional completo, incluindo as duas fontes de prosa do #5955 e o procedimento de resolução: `context/overnight-dispatch-rules.md` item 25). `exit 0` (nenhum drift) → seguir pro passo 1. `exit 1` (drift detectado) → aplicar as labels estruturais faltantes reportadas (procedimento no item 25 citado acima) antes de continuar — o relatório NÃO compila enquanto houver achados não resolvidos.
0.85. **Gate de vocabulário fechado do `motivo` de issues `pulada` (#6438) — bloqueia a compilação do relatório, mesma filosofia dos passos 0.5-0.8.** Rodar `npx tsx scripts/check-overnight-plan-motivo.ts --plan data/overnight/{AAMMDD}/plan.json`. Espelha `validate-develop-plan-motivo.ts` do `/diaria-develop` (#5708): confere que TODA issue `status: "pulada"` tem `motivo` dentro de `OVERNIGHT_PULADA_MOTIVOS` (`scripts/lib/overnight-plan-motivo.ts`) — motivo inventado ad-hoc ("gated no D0", "timing do editor") escapava de qualquer verificação antes deste gate, deixando a issue "Overnight sem sinal" pra sempre (achado ao vivo rodada 260827b, #6425/#6427/#6259/#5653). `exit 0` → seguir pro passo 1. `exit 1` (lista de issues com motivo fora do vocabulário) → reclassificar cada uma num motivo real do vocabulário (ou, se o motivo descreve algo genuinamente novo, abrir issue pra fechar o vocabulário, mesmo padrão que originou este gate) antes de continuar.
0.9. **Gate de bloqueio caducado (#6259) — bloqueia a compilação do relatório, mesma filosofia dos passos 0.5-0.8.** Rodar `npx tsx scripts/check-block-staleness.ts --plan data/overnight/{AAMMDD}/plan.json`. Cobre a lacuna que o passo 0.5 e a re-varredura de convergência da Fase 1 passo 1 não fecham: os dois só perguntam "apareceu issue NOVA?" — uma issue JÁ CONHECIDA cujo motivo `pulada` era `pr-em-voo`/`claimed-por-outra-sessao`/`bloqueio-execucao` fica igualmente disponível quando o PR mergeia, a sessão dona do claim encerra, ou a label de bloqueio é removida, sem que nenhuma dessas transições crie issue nova (achado ao vivo que motivou: `docs/orchestrator-stage-narrative-history.md#overnight-6259-bloqueio-caducado-260826`). `exit 0` ("ok — nenhum bloqueio... caducado") → seguir pro passo 1. `exit 1` (lista de issues com bloqueio caducado) → **voltar pra Fase 1** e reavaliar dispatch pra cada uma antes de continuar — só então o relatório é compilado. Motivos fora desse conjunto fechado (`bloqueio-externo`, `requer-sessao-local`, `ambigua`, `trade-off-real`) nunca são reabertos por este gate — ver `scripts/lib/block-staleness.ts` pro racional. Sem `gh`/`session-registry`, o comando degrada sozinho pra fail-soft (#738) — nunca reporta caducidade sem ter conseguido confirmar. **O mesmo comando também checa (#6436) claims envelhecidas sem PR aberto** — varre `data/sessions/` inteiro (não só `plan.json`) por issues reivindicadas há mais de `CLAIM_STALE_AGE_MS` (~6h, `scripts/lib/claim-staleness.ts`) sem nenhum PR aberto citando o número; cobre o caso da sessão `continuo` (cron horário do Hermes) que re-reivindica sem nunca soltar, ficando `claimed-por-outra-sessao` pra sempre sem trabalho visível — `exit 1` lista as claims envelhecidas; adicionar cada issue listada como pendência de re-triagem (`npx tsx scripts/check-state-changed-pending.ts --add-pending {N} --plan {path}`) antes de continuar. A Triagem do Studio (`/triagem`) também passou a mostrar essas claims como "em andamento — {kind} {machine}" em vez de "Overnight sem sinal" (`attachClaims`, `scripts/studio-ui/studio-issues.ts`).
0.95. **Lint de prosa de dependência sem marcador (#7137 item 4) — AUDITORIA, nunca bloqueia (diferente dos passos 0.5-0.9 acima).** Rodar `npx tsx scripts/check-dependency-prose-lint.ts`. Reporta issues abertas que citam prosa de dependência ("pré-requisito", "depende do #N", "depois que #N", "só depois") sem carregar o marcador `<!-- depends-on: #N -->` (item 24, `reconcile-issue-dependencies.ts`) — mesmo erro que abriu a #7137 (17 issues-filhas do #7112 escritas com dependência só em prosa). Correção exige julgamento (é dependência genuína ou frase comum sem relação?), por isso **sempre `exit 0`**: os achados entram como uma linha no relatório da rodada (passo 1 abaixo, seção "Achados de auditoria" se houver algum) — nunca voltam a Fase 1, nunca travam a compilação. Sem `gh`/rede, degrada sozinho pra warning + `exit 0` (fail-soft #738). Ver `scripts/lib/dependency-prose-lint.ts` pro racional completo de por que isto é auditoria e não gate.
1. Compilar o digest da noite a partir de `plan.json` + run-log (filtrado por `agent: "overnight"` + AAMMDD da rodada), **agrupado por PR** (lotes listam suas issues juntas):
   - **Modo da rodada** (#3375): se `bugs_only: true` em `plan.json`, abrir o digest com uma linha `Modo: --bugs (só issues com label bug)` — inclui a contagem de issues excluídas por não terem a label (registrada nos passos 3/1 da Fase 0/1), pra o editor saber que a rodada foi deliberadamente restrita, não que o backlog de enhancements ficou vazio. **Modo de prioridade** (#3499): se `priority_filter` não for `null` em `plan.json`, adicionar (ou combinar na mesma linha, se `--bugs` também ativo) `Modo: --priority {lista} (só issues com label de prioridade ∈ {lista})` — inclui a contagem de issues excluídas por não baterem a prioridade (registrada nos passos 3/1 da Fase 0/1). Com as duas flags ativas: `Modo: --bugs --priority {lista} (só bugs com prioridade ∈ {lista})`, com a contagem de exclusões de cada filtro.
   - **Coordenador** (#2993): linha `Coordenador: {model} / {effort}` com os valores **CONFIGURADOS** no frontmatter desta skill (ex: `Coordenador: sonnet / high`) — não auto-relatado (ver o evento `coordinator_model` logado na Fase 0 passo 1). Se algum `stall_events`/intervenção do editor mid-rodada sugerir reversão de modelo/effort (limitação #2941 — troca de turno), anotar a ressalva na mesma linha. Esta linha entra tanto no resumo do terminal (passo 5) quanto no relatório registrado no Studio (passo 3, #3714) — mesma fonte, os dois derivam deste digest,
   - **Seção "Custo em tokens" (#4815) — MANDATÓRIA, nunca omitida.** A instrumentação da #3453 Rec 1 (custo por rodada) vinha sendo aplicada de forma inconsistente entre sessões — a tabela só aparecia quando o coordenador "lembrava" de incluí-la, best-effort, e isso quase impediu a auditoria de custo do fleet review em 260809 (#4815, achado numa rodada `/diaria-develop`, mesmo princípio vale aqui). A partir de agora esta seção **sempre** entra no relatório, com as 3 categorias sempre presentes — cada uma escreve `unavailable` explicitamente quando o dado não existe, nunca omite a linha/tabela (ausência silenciosa é indistinguível de "esqueci"):
     0. **Checagem determinística prévia (#5009).** Antes de montar as 3 categorias abaixo, rodar
        ```bash
        npx tsx scripts/check-overnight-token-instrumentation.ts --edition {AAMMDD}
        ```
        e colar a linha impressa no início desta seção do relatório. O script conta os eventos `subagent_metrics`/`coordinator_tokens_estimate`/`review_metrics` de fato gravados em `data/run-log.jsonl` pra esta rodada e distingue mecanicamente os dois motivos que hoje colapsam no mesmo `unavailable`: "harness não expôs o dado" (eventos foram emitidos com `source: "unavailable"`) vs. **"coordenador esqueceu de chamar `log-event.ts` nos checkpoints"** (zero eventos de algum tipo). Em `warning`, o script já devolve a frase exata a colar — não reformular nem suavizar (achado que motivou: `docs/orchestrator-stage-narrative-history.md#overnight-observability-eventos-260811`).
     1. **Implementação** — tabela `unidade | subagent_tokens | tool_uses | duração` a partir dos eventos `subagent_metrics` (um por unidade, ver Fase 1 passo 5) + total da rodada. Rodada sem nenhuma unidade trabalhada → tabela com a nota "nenhuma unidade" (mesmo tratamento de `render-overnight-timeline.ts` pra plano vazio).
     2. **Review pós-rodada** — tabela SEPARADA da anterior (`nível (depth) | review_tokens | tool_uses`, a partir dos eventos `review_metrics`, ver Fase 1.5) + total. A separação em relação à Implementação é o que torna visível o rateio entre as duas categorias (#4815 item 2) — nunca agregar as duas na mesma linha/tabela.
     3. **Coordenador** — linha `Coordenador (tokens estimados): ~N (fonte: {harness_usage | context_size_proxy})` a partir de `coordinator_tokens_estimate` (#3453 Rec 1); se todos os eventos dessa categoria vierem `unavailable` (ou não existirem na rodada), a linha vira `Coordenador (tokens): unavailable` — nunca `escrever` só que "não instrumentável" e nunca omitir a linha inteira. **Se o item 0 acima reportou `warning` para `coordinator_tokens_estimate`**, não usar o `unavailable` genérico aqui — usar a frase explícita do script ("instrumentação de token não foi emitida nesta rodada (coordenador esqueceu os checkpoints)"), que é distinta de "harness não expôs".
   - **Razão adição:remoção (#7113) — mesma proeminência das issues fechadas abaixo.** Antes de gravar `Closes`/merges do último PR da rodada, rodar `npx tsx scripts/measure-round-diff-stats.ts --base {commit-base da rodada} --session-kind overnight` (persiste em `data/run-log.jsonl`), depois `npx tsx scripts/round-diff-stats-report.ts --check-alarm` — colar a tabela `janela | rodadas | adições | remoções | razão | líquido/dia` (7d/30d/90d) impressa nesta seção do relatório. `--check-alarm` cruza a razão de 7d contra o limiar (`ROUND_DIFF_RATIO_ALARM_THRESHOLD`, hoje 10:1) e reconcilia sozinho uma issue de alarme via `scripts/lib/alarm-issues.ts` quando cruzado — nada a decidir aqui além de colar o output.
   - resolvidas (com links de PR e commits de merge — estados confirmados via `gh pr view`, nunca de memória),
   - puladas e por quê (sem briefing, bloqueio externo, CI vermelho persistente — com link do draft),
   - **entraram mid-round**: issues com `source: "mid-round"` incorporadas durante a Fase 1 (número, título, PR de resolução ou motivo de exclusão),
   - **convergência da fila (#5272)** — linha `Re-varreduras de convergência: {rescans_done}` (contador de observabilidade; desde #5272 nunca é motivo de a rodada ter parado). Dizer também **como** a fila fechou: convergência real (último re-scan sem novidade) ou preempção pelo guard de colisão editorial (`preempted_by` preenchido) — são desfechos diferentes, e só o primeiro significa "a fila esgotou de verdade",
   - **review noturno + mini-rodada(s) de findings**: hotfixes críticos aplicados (se houver) + cadeia completa com PRs — para cada nível ativo (1.5 → mini-1 → 1.5b → mini-2 → 1.5c): findings identificados, issues filadas, issues resolvidas na mini-rodada, issues que não re-entraram (depth-limit, sem P2+, ou ambíguas); se a cadeia terminou antes do depth limit (1.5b sem P2+, ou depth-2 sem findings), indicar em qual nível parou e o motivo. **Findings do review que NÃO passaram na barra de filing (#2754)** — P3 sem cenário de falha concreto — entram numa lista à parte "findings do review, não filados" (descrição breve + arquivo:linha), pro editor decidir manualmente se algum merece virar issue,
   - **Timeline da noite** — gerar com o helper determinístico:
     ```bash
     npx tsx scripts/render-overnight-timeline.ts \
       --plan data/overnight/{AAMMDD}/plan.json
     ```
     O script lê o campo `timeline` de cada issue no `plan.json` e imprime a tabela markdown `unidade | início | fim | duração | fix-iterations` + total da rodada + unidade mais lenta. Degrada graciosamente: issues sem campo `timeline` (rodadas anteriores ao #2099 ou unidades interrompidas) aparecem na tabela com `—` nos campos de horário e duração — a tabela nunca quebra. Esta seção é a fonte primária de observabilidade de tempo; `plan.json` é a fonte do relatório (pós-compaction o run-log pode ser grande),
   - estado final da fila (`gh issue list` fresco).
2. Salvar em `data/overnight/{AAMMDD}/report.md` (AAMMDD do `plan.json`, não recomputado).
3. **Registrar o relatório na superfície de Relatórios do Studio (#3714, decisão do editor 260720 — substitui o antigo draft de Gmail, não soma a ele):**
   ```bash
   npx tsx scripts/register-report.ts --kind overnight --id {AAMMDD} \
     --title "diar.ia.br overnight {AAMMDD} — {U} unidades, {X} issues, {Y} puladas, {Z} findings" \
     --html-path data/overnight/{AAMMDD}/report.md
   ```
   (`{U}` = linhas da tabela de unidades, `{X}` = issues distintas que elas cobrem — os dois são CONFERIDOS, ver abaixo; omitir `{Z} findings` se o review não rodou; acrescentar `+ hotfix` se houve). **Use "unidades"/"issues" literalmente: "N resolvidas" é ambíguo entre os dois e o comando recusa um título que não declare nenhuma contagem conferível.** **A tabela de unidades do relatório precisa ser precedida do marcador literal `<!-- unidades-mergeadas -->` (#5521)** — numa rodada sem NENHUMA unidade mergeada, omita o marcador (não existe tabela a conferir; marcador sobre tabela vazia é lido como formato quebrado e bloqueia o registro) — é a âncora que permite conferir o título automaticamente; sem ele o comando avisa em stderr que a conferência NÃO rodou. **Com o marcador, as contagens do título são CONFERIDAS contra a tabela e o comando sai `exit 1` sem registrar nada se divergirem** — número de unidades tem que bater com a quantidade de linhas da tabela, e número de issues com a quantidade de issues distintas que essas linhas cobrem (1ª coluna = issues da unidade, 2ª = PR). Só contagens inequívocas são conferidas — `"N unidades"`/`"N PRs"` e `"N issues"`; `"N resolvidas"` é ambíguo entre os dois e por isso ignorado. Este é o único ponto NÃO fail-soft do comando, de propósito: o título vira o assunto do e-mail que o editor recebe, e assunto errado não tem desfazer. Se o comando reprovar, ele imprime o sufixo correto derivado da tabela — use-o em vez de reescrever de memória. Se alguma issue da tabela ficou aberta (escopo residual), **diga isso no título** ("17 issues, 1 residual aberta") em vez de baixar o número em silêncio, que é o que produz a divergência (achado que originou o guard: `docs/orchestrator-stage-narrative-history.md#overnight-5521-titulo-divergente-260816e` — a tabela do relatório é a fonte que bate com o git, então é contra ela que o título é conferido). **File-based no registro em si** — grava em `data/reports/index.jsonl` e funciona igual com o Studio (`npm run studio`) parado ou no ar, o relatório aparece em `/relatorios` na próxima vez que a página carregar/pollar. **Mas o comando também manda e-mail real pro editor a cada invocação** (o título vira o assunto) — não é "nunca uma chamada de rede"; **use `--no-email` em reexecução/auditoria/teste** pra não notificar (#5521 — sem isso, uma auditoria de 73 relatórios históricos disparou ~50 e-mails de uma vez em 17/08/2026). O comando imprime a URL em stdout — capturar pro resumo do terminal (passo 5). **Não criar mais draft via `create_draft` aqui** — esse call site foi removido.
4. **Fail-soft**: falha do `register-report.ts` (rara — a gravação em disco em si) → NÃO travar. Mesma exceção consciente e local ao #738 já documentada aqui: avisar no terminal que o relatório ficou só local (`report.md`) e encerrar normalmente. Não citar esta exceção como precedente fora do relatório overnight.
5. Imprimir o resumo no terminal — é a primeira coisa que o editor vê ao voltar. Incluir a linha `Relatório: {URL do Studio}` (capturada no passo 3); se o registro falhou (passo 4), `Relatório: só local (data/overnight/{AAMMDD}/report.md) — registro no Studio falhou, ver warn acima`.
6. **Limpeza de worktrees mergeados (#4335, follow-up do #4326):** rodar `npx tsx scripts/cleanup-merged-worktrees.ts` — varre `.claude/worktrees/`, confirma via `gh pr list --head {branch} --state merged` quais branches já foram mergeadas e remove (`git worktree remove --force`) só essas. **FAIL-SOFT**: o script já é fail-soft internamente (nunca lança, nunca sai não-zero por falha de `gh`/permissão), mas mesmo assim tratar qualquer stderr/falha de invocação como warning — **nunca travar o encerramento da sessão** por causa desta limpeza. Roda por último, depois do relatório já estar salvo/registrado. **Guard de sessão compartilhada embutido no próprio script (#5156 item 9):** ele já consulta `session-registry.ts` (`listActiveSessions`) e PULA a varredura inteira, sozinho, se detectar qualquer outra sessão ativa nesta máquina — nada extra a fazer aqui além de rodar o comando normalmente; se o script imprimir esse aviso (por exemplo, uma sessão `/diaria-develop` supervisionada ainda ativa em paralelo), não insistir com `--confirm-shared` sem ter certeza de que os worktrees dela não colidem — a omissão é o comportamento seguro por padrão.
7. **Encerrar sessão overnight ativa (#3322, movido pro FIM da fase em #6758)** — último passo da Fase 2, depois de esgotadas todas as chamadas de ferramenta que a fase precisa fazer (passos 0.5-6 acima): rodar `npx tsx scripts/overnight-session-marker.ts --end` — remove `data/overnight/.active-session-{hostname}.json` gravado na Fase 0 passo 1, pra `pr-create-review.mjs` parar de dar `low` effort em PRs abertos nesta máquina depois que a rodada de fato terminou (manuais, `/diaria-develop`, etc). Idempotente — no-op se o marker já não existir. Rodar mesmo nos caminhos de encerramento antecipado (guard de colisão editorial, erro irrecuperável) — sempre que esta fase é alcançada, a rodada está terminando. **Junto, encerrar também o registro no `session-registry.ts` (#5156)**: `npx tsx scripts/lib/session-registry.ts end --kind overnight` — sem isso, o registro fica visível (até expirar por staleness, 24h) pro guard de cleanup de worktree (item 9 da seção "Paralelismo entre sessões") e pro claim de issue (item 3) de outra sessão, mesmo com esta rodada já encerrada. **Por que aqui e não no passo 0:** rodar cedo demais faz cada chamada de ferramenta seguinte (`check-*.ts`, `Write` do report, `register-report.ts`, `cleanup-merged-worktrees.ts`) disparar `session-beacon.mjs` (hook `PreToolUse`) sem achar mais o registro `overnight-*` — o beacon cria, por desenho (#6326), um registro NOVO `interactive-{tag}-{sessionId}.json`, fazendo a sessão overnight "ressuscitar" como `interactive` viva em `list-active` mesmo já tendo terminado (achado ao vivo, rodada 260829).

## Regras

- **NUNCA fazer perguntas ao editor durante a execução (Regra 1 — HARD RULE).** Toda interação com o editor acontece exclusivamente no briefing da Fase 0. Depois do briefing, nenhum `AskUserQuestion` em nenhuma fase (Fase 1, re-scans, mini-rodadas, reviews) — **incluindo decisões de baixo risco disparadas por trabalho ad-hoc do editor mid-round** (ex: "devo criar uma issue pra esse achado?" → sempre sim, crie direto com prioridade justificada, nunca pergunte; incidente de referência 260706/07, #3037/#3038, travou a rodada ~8h). Issue ou finding ambíguo mid-round → status `pulada` + comentário na issue explicando qual decisão falta (vira pergunta do briefing da próxima rodada). Única exceção: o editor intervém por iniciativa própria — responder não é "fazer pergunta", mas isso não dá licença pra encaixar uma pergunta própria na resposta, e a rodada segue sem aguardar follow-up. **Guard mecânico além do reforço de prompt (#4450):** o texto acima cobre decisões de raciocínio ruim (caso do #3038); não cobre uma chamada de ferramenta anômala que não passa por raciocínio nenhum (caso da rodada 260801/02 — `AskUserQuestion` com conteúdo literalmente placeholder). Desde #4450, o hook `PreToolUse` `.claude/hooks/block-askuserquestion-overnight-autonomous.mjs` nega mecanicamente qualquer `AskUserQuestion` sempre que `data/overnight/.active-session-{hostname}.json` estiver com `phase: "autonomous"` (armado no passo 8 da Fase 0, ao entrar na Fase 1) — independe do coordenador "lembrar" da regra no momento da chamada.
- **Nunca** disparar publicação (Beehiiv/LinkedIn/Facebook/Brevo) ou rodar stages da pipeline editorial durante a rodada — e o guard vai **dentro do prompt de cada subagente**, não só aqui.
- #636 — desde o #6299, lido como o invariante que ele de fato garante: **nunca 2 PRs que COLIDEM abertos ao mesmo tempo**, satisfeito por construção (fusão de cluster) e não por serialização. O que segue estritamente serial é o MERGE. #633 (teste de regressão em bugfix) e validação determinística de estado externo (#573) valem a noite inteira, sem exceção.
- **Fallback wake é INVARIANTE MECÂNICO, não um "should" (#2945 — HARD RULE).** Todo `Agent` dispatch OU `SendMessage` resume de subagente é IMEDIATAMENTE seguido — na MESMA leva de tool-calls, junto do `timeline.dispatch` + log-event — de um `ScheduleWakeup` (~1200s) pra aquela unidade. Não é opcional, não é lembrável, não depende de "achar que vai ser notificado": é passo mecânico como o registro de timestamp. Um retorno de resume que contenha `"queued"` / `"waiting for background"` / `"monitor"` (classificar com `needsActiveRecheck`/`classifyYieldText` de `scripts/lib/overnight-fallback-wake.ts`) **EXIGE re-check ativo** — nunca confiar em notificação terminal após um resume `queued`. Esquecer esse agendamento foi a causa do stall de ~10h em 260703 (o #2896 introduziu o mecanismo mas como recomendação esquecível; aqui vira regra dura). O watchdog externo (#2688) é a 2ª camada, mas só protege se `check-watchdog-armed.ts` reportar `armed` **de verdade** (não `armed_but_disabled`/`armed_but_stale`/`armed_but_never_run`, #2944).
- Toda issue **trabalhada ou bloqueada** recebe comentário com o que foi feito ou o que falta (com dedup — nunca repetir comentário equivalente). Issues resolvidas dispensam comentário extra: o merge com `Closes #NNNN` já conta a história na timeline.
- `data/overnight/` segue o blanket gitignore de `data/` — relatórios não vão pro repo.
- Stall passivo é inaceitável: toda espera usa `gh pr checks --watch` em background ou poll com intervalo explícito. Timeout por espera de CI = **30 min**; estourou → tratar como CI vermelho (tentativa de fix/draft), nunca esperar indefinidamente.
- **Ação do coordenador que muda elegibilidade de issue JÁ CONHECIDA vira pendência de re-triagem (#5476).** Toda vez que o coordenador, em QUALQUER fase (não só Fase 0/1), rodar `gh issue edit N --add-label {not-this-week|trade-off-real|external-blocker|...}` <!-- route-issue-exempt: define o GATILHO da regra #5476, não instrui rodar o comando --> fora do fluxo normal de classificação já descrito acima, OU encerrar/remover manualmente uma claim de `session-registry` (`session-registry.ts end`, ou apagar um arquivo de `data/sessions/*.json` à mão), isso conta como sinal equivalente a "issue nova apareceu" — a re-varredura de convergência (Fase 1 passo 1) não cobre esse caso, só issues genuinamente novas em `gh issue list`. Registrar a pendência na mesma respiração: `npx tsx scripts/check-state-changed-pending.ts --add-pending {N} --plan data/overnight/{AAMMDD}/plan.json`. **Antes de fechar a rodada** (Fase 2 passo 1, compilação do relatório), rodar `npx tsx scripts/check-state-changed-pending.ts --plan data/overnight/{AAMMDD}/plan.json` — se sair `exit 1` (pendências listadas), voltar pra Fase 1 e reavaliar dispatch pra cada issue listada (checar `is-claimed`/label atual) antes de prosseguir: dispatchar se elegível, ou `--remove-pending {N}` se confirmadamente ainda não-dispatchável nesta rodada. Só depois disso escrever o relatório. Achado ao vivo na rodada 260816e: labels aplicadas na reconciliação da Triagem e um cleanup de `session-registry` liberaram issues elegíveis que ficaram na mesa até o editor perguntar por elas — nenhum dos dois foi tratado como gatilho de reavaliação, só como nota de relatório.

## Paralelismo entre sessões (#5156)

Esta skill e `/diaria-develop` podem rodar **ao mesmo tempo** — mesma máquina (develop supervisionado numa janela, overnight drenando a fila noturna em outra) ou máquinas diferentes sincronizadas pelo mesmo junction OneDrive `data/`. Nenhuma das duas skills assume mais "sou a única sessão automatizada rodando" — mecanismo e convenção por item da auditoria original (#5156):

1. **`AskUserQuestion` do develop negado por um overnight ativo na mesma máquina** — MECANIZADO. `.claude/hooks/block-askuserquestion-overnight-autonomous.mjs` só bloqueia quando a chamada pertence à MESMA sessão que gravou o marker (`session_id` comparado, injetado automaticamente por `.claude/hooks/inject-session-id.mjs` — nenhuma ação da skill). Marker sem `session_id` (formato antigo) preserva o comportamento pré-#5156. Nada a fazer nos passos acima além de continuar chamando `overnight-session-marker.ts --start`/`--phase` normalmente.
2. **Effort do review por PR cai pra `low` incorretamente com overnight ativo** — MECANIZADO, mesmo mecanismo do item 1 (`.claude/hooks/pr-create-review.mjs`, `isOvernightRoundActive` session-aware).
3. **Claim de issue entre sessões** — MECANIZADO via `session-registry.ts` (`is-claimed`/`claim-issue`), ver Fase 1 passo 2 acima.
4. **Serialização de merge cross-sessão** — MECANIZADO via `session-registry.ts merge-lock-acquire`/`merge-lock-release`, ver Fase 1 passos 3/6 acima. **Nota (#6182): entre máquinas via OneDrive, o lock é advisory — `O_CREAT|O_EXCL` vê inodes diferentes, não garante exclusão real.**
5. **Checkout principal compartilhado** — SEM fix de código razoável além do merge lock do item 4 (que já mitiga a janela crítica de merge+pull). `git checkout master && git pull` na Fase 0 passo 1 e após cada merge continuam sem lock próprio — se outra sessão estiver escrevendo no checkout no MESMO instante, é uma corrida rara e de baixo blast-radius (o pior caso é um `git pull` pegando um estado intermediário, resolvido no próximo pull). Documentado, não mecanizado.
6. **Teto de concorrência por sessão** — MECANIZADO DESTE LADO desde o #6299; **assimétrico, e é isso que importa saber:** o read-path sempre foi real (`list-active` soma `active_worktrees` das sessões ativas nesta máquina), e o write-path (`heartbeat --active-worktrees N`) agora tem call site REAL no overnight — dois, obrigatórios: ao dispatchar a onda (Fase 1 passo 2) e após o cleanup de cada worktree (Fase 1 passo 3). Antes disso nenhuma skill chamava o comando e `active_worktrees` ficava sempre ausente, tornando a soma inútil (#5161 fleet review item 7). **O `/diaria-develop` ainda NÃO publica o dele** — logo, quando as duas rodam juntas, o overnight enxerga os worktrees do develop como zero e pode abrir seus 3 por cima dos 6 dele. A assimetria é conservadora na direção certa (quem publica é quem tem o teto menor), mas não é simetria: fechar o lado develop é trabalho pendente, não pressuposto desta seção. Teto de 6 do develop: ver seção equivalente em `diaria-develop/SKILL.md`.
7. **Herança de plano em voo** — não se aplica ao overnight (é quem GERA o `plan.json` que o develop herda, não o contrário). Ver `diaria-develop/SKILL.md` pro tratamento do lado que consome.
8. **Fase 1.5 revisa diff de sessão alheia** — já é o comportamento esperado, agora formalizado: com paralelismo, o diff acumulado da Fase 1.5 PODE incluir merges de uma sessão `/diaria-develop` concorrente. **Findings sobre esse código viram issue, nunca hotfix** (mesma regra de sempre) — com paralelismo isso é o CASO COMUM, não uma exceção rara.
9. **Cleanup de worktree global** — MECANIZADO, embutido no próprio `cleanup-merged-worktrees.ts` (ver Fase 2 passo 6 acima).
10. **Marker overnight por-máquina vs por-sessão** — coexistência deliberada: `data/overnight/.active-session-{hostname}.json` (marker antigo, #3322/#4450) continua por-máquina, mas ganhou o campo opcional `session_id` que os itens 1/2 usam pra distinguir chamadores; `data/sessions/*.json` (registro novo, #5156) é por-sessão desde o início. Migrar o marker antigo pra dentro do registro novo foi avaliado e descartado — quebraria qualquer rodada overnight já em progresso no momento do rollout (ver docblock de `scripts/lib/session-registry.ts`).
11. **StatusLine mistura sessões da mesma máquina** — não se aplica diretamente ao overnight (a barra dele já é por-máquina). **Nota (#6328):** rodadas overnight simultâneas (mesma máquina ou máquinas diferentes via OneDrive) já são um caso SUPORTADO desde o #6328 — cada uma resolve seu próprio `data/overnight/{AAMMDD}{suffix}/plan.json` via `resolve-overnight-plan-path.ts` e grava `session_id`/`machine_id` no plano — mas nenhuma statusLine/painel consome esses dois campos pra discriminar visualmente ENTRE rodadas overnight concorrentes ainda (o consumo equivalente, `isForeignDevelopPlan`, existe só pro `plan.json` do develop). Sem issue própria aberta pra esse follow-up — a barra correta por-plano já funciona, só falta o discriminador visual entre planos. Ver `diaria-develop/SKILL.md` pro campo `session_id` opcional em `plan.json` (item 11, `isForeignDevelopPlan` em `scripts/overnight-statusline.ts`).
