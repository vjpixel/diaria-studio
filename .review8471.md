Review automatizado (1 agente, effort low): fix reintroduz o falso positivo que o próprio teste de regressão descreve, e o teste novo nunca roda — reject.

## Achados

### 1. [P1, confiança ALTA] `_OTHERS_CLAIM` não casa mais quando há um verbo entre a ref e "reivindicad..." — regressão no próprio caso que a PR alega corrigir
**Arquivo:** `hermes/scripts/detect-tick-claim-fabrication.py`, linha ~237-243

A regex antiga usava `[^#]{0,80}?` entre a ref e o keyword — tolerava qualquer texto no meio (ex: "foi", "está"). A nova troca isso por `\s*` estrito:

```python
_OTHERS_CLAIM = re.compile(
    r"(?P<refs>" + _REF_LIST.pattern + r")"
    r"\s*reivindicad\w*\s+(?:por|pelo|pelas)\s+"
    ...
)
```

Testei a regex diretamente (Node, mesmo padrão traduzido) contra os 3 casos do teste novo:

- `"#8355 ficou bloqueada porque #8354 foi reivindicada pelo Overnight"` → **null** (não casa — o "foi" entre `#8354` e `reivindicada` quebra o `\s*` estrito)
- `"#8355/#8354 reivindicadas pelo Overnight"` → casa OK (refs adjacentes ao keyword)
- `"#7807 coberto por #7808, #7809 reivindicada pelo Overnight"` → casa OK

O primeiro caso é justamente o 1º assert do teste novo (`test_regressao_8463_falsos_positivos_lista_atribuida_outro_ator`, linha ~596): `assert 8354 not in refs`. Como `_OTHERS_CLAIM` não casa nessa linha, `others_n` fica vazio pra ela, `#8354` não é excluído, e — se o `attached` (lógica de proximidade linhas 420-433) selecionar `#8354` como candidato mais próximo do keyword `reivindicada`, o que ele faz — `#8354` **entra** em `refs`, violando o assert. Ou seja: o fix regride exatamente o padrão "X **foi** reivindicada por Y" (verbo entre ref e particípio), que é a construção mais comum em português pra essa frase, e que o teste novo tenta cobrir.

**Cenário concreto de falha:** relatório real do tick contendo `"#8354 foi reivindicada pelo Overnight"` (ou qualquer variante com verbo entre a ref e "reivindicada") volta a ser lido como claim PRÓPRIO deste coordenador — o falso positivo que o #8463 deveria eliminar reaparece pra esse padrão específico.

### 2. [P1, confiança ALTA] O teste de regressão adicionado nunca é executado — não roda nem como parte do `main()`, nem por descoberta automática
**Arquivo:** `hermes/scripts/detect-tick-claim-fabrication.test.py`, linhas 591-617

`test_regressao_8463_falsos_positivos_lista_atribuida_outro_ator` é adicionada **depois** do bloco `if __name__ == "__main__": sys.exit(main())`. Este arquivo (como os demais `*.test.py` do repo, ex. `detect-context-truncation.test.py`) não usa pytest/unittest com auto-discovery — `main()` é uma função procedural com `assert_true(...)` explícitos, chamada só de dentro do próprio `if __name__ == "__main__"`. A nova função `test_regressao_...`:
- não é chamada por `main()` (que não faz `globals()` scan por `test_*`);
- fica posicionada *depois* do `sys.exit(main())`, então nem é alcançada quando o arquivo roda como script.

Resultado prático: `python3 hermes/scripts/detect-tick-claim-fabrication.test.py` roda o `main()` de sempre e retorna sucesso independentemente do que o teste novo afirma — o teste é código morto. Isso viola a regra do CLAUDE.md (#633, "PR de bugfix exige teste de regressão... sem teste novo demonstrando que o bug não voltaria → não merge") em espírito: o teste existe no diff mas não protege nada, e — como o achado 1 mostra — se ele *rodasse*, falharia.

**Correção sugerida:** mover as asserções para dentro de `main()` (no padrão `assert_true(...)` já usado no arquivo) ou adicionar uma chamada explícita a essa função antes do `return 0` final, e então corrigir a regex do achado 1.

### 3. [P3, confiança baixa] Comentário da linha 227-236 descreve o comportamento pretendido ("um claim próprio no mesmo segmento... é reconhecido") mas não há caso de teste cobrindo esse ramo positivo (só os 3 negativos)
Não bloqueia, mas caso a correção do achado 1 mude a regex, vale adicionar um caso positivo (ex: "#8356 reivindicada pelo mesmo tick" deve continuar sendo claim próprio quando não houver outro ator) pra não perder esse ramo em silêncio.

<!-- continuo-review: run=1789854868-2653065-30654 at=2026-09-19T21:54:28Z verdict=reject head=6261472812cbf234932e5d7230c27025e67b8baa -->
