Review automatizado (1 agente, effort low): mudança bem escopada e testada (133/133 verdes localmente), sem achado de confiança alta/média em P0/P1 — approve.

## Achados

**1. [baixa, P3] `--check-suspended` só olha uma página de suspended sem paginação explícita no chamador**
`fetchCampaignsByStatus` (em `scripts/lib/brevo-client.ts`) já pagina internamente (o comentário do diff cita `limit=50`, paginado) — não é um bug real, só anotando que não vi o corpo da função neste diff para confirmar que o novo status `"suspended"` reutiliza o mesmo laço de paginação dos demais. Vale conferir rapidamente que `suspended` não tem algum tratamento especial de resposta na API Brevo (histórico do projeto já documenta 2+ armadilhas de paginação da Brevo).

**2. [baixa, P3] Task nova roda com `--apply` direto em produção, sem período de observação em dry-run**
`Diaria-Clarice-Unblock-Suspended` já nasce com `args: ["--check-suspended", "--apply"]` diário às 08:45, sem uma fase manual/dry-run prévia. Dado que o próprio código trata isso como idempotente e reversível (email só sai de `sent-or-queued.json`, volta a ser elegível), o risco é baixo — mas é uma automação nova mexendo em estado de elegibilidade de envio sem qualquer telemetria/alarme dedicado além do log. Não bloqueia, mas registra o achado.

**3. [baixa, P4/nit] Comentário do registro (`scheduled-tasks.ts`) diz "Livre entre Diaria-Clarice-Sync (08:30) e ... 09:00" mas não há teste que trave a data 08:30 do Sync** — o teste de colisão só verifica ausência de outra daily exatamente às 08:45, não a alegação de estar "entre" 08:30 e 09:00. Cosmético; não afeta correção.

## Pontos positivos
- Fail-hard correto em `--check-suspended` sem `BREVO_CLARICE_API_KEY` (aborta em vez de degradar para "nada encontrado") — consistente com o princípio do projeto de nunca mascarar ausência de dado como resultado negativo.
- `readGroupCsvEmails`/`loadGroupCampaigns`/`findSuspendedCampaignEmails` são puras e testadas isoladamente, além do teste E2E com fetch mockado.
- Regra #633 (teste de regressão em bugfix) satisfeita com sobra — testes cobrem o caso positivo, o caso de CSV intacto mas campanha suspensa, o caso sem key, e o caso de flag desligada preservando comportamento antigo.
- Edição em `clarice-unblock-orphaned-selections.ts` manteve a docstring existente e só estendeu, não reescreveu — cirúrgico (#495).
- `--cycle` virar opcional com fallback documentado (`computeExpectedEnvioCycle`) e testado explicitamente é uma mudança de comportamento visível, bem sinalizada no stderr.

<!-- continuo-review: run=1789424417-2622080-3374 at=2026-09-14T22:20:17Z verdict=approve head=ef853e7bd20d2dfe0fdb146b2d45e45ff6eb0eb3 -->
